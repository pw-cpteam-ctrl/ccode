# 메가하우스 신제품 예고 메이커 — 업데이트 내역

## 2026-06-25 (V1.0 최초 버전)
- 포털(허브) 페이지 추가, 도구 이름을 megahouse.html로 변경
- 블러 배경: 원본 사진을 복제해 흐림 배경으로 깔고 원본은 선명하게 유지
- 동시 작업으로 중복된 드래그 핸들/블러 변수 정리

## 2026-06-30 (V1.1 2페이지 지원)
- 2페이지에 사진이 있으면 스테이지에 1·2페이지 나란히 표시
- 나란히 배치 시 포스터 래퍼 크기/오버플로우 문제 수정

## 2026-07-01 (V1.2 대규모 기능 확장)
- 그리드 셀에 삭제(×) 버튼 추가, 드래그 핸들은 좌측으로 이동
- 사진 업로드 12장 제한 로직 정리 → 항상 현재 탭 페이지에 추가
- 페이지 수 제한 없이 ＋ 버튼으로 페이지 무제한 추가 가능하게 변경
- 12장 초과 시 다음 페이지로 자동 분리(오버플로우) 기능 재도입
- 포스터 미리보기 간 페이지 넘나드는 드래그&드롭(크로스페이지) 지원
- 푸터 높이 슬라이더 추가, 레이아웃 섹션 기본 접힘 처리, 푸터 로고 그리드 끝에 정렬
- 푸터 로고 정렬 방식을 scale 대신 height 기준으로 수정
- 셀 드래그 핸들·삭제 버튼 크기 2배로 확대
- 새 이미지 업로드 시 블러 배경 기본 ON
- 블러 전체 ON/OFF 일괄 버튼 추가
- Ctrl+Z 실행취소(최대 30단계) 지원 — 페이지/이미지 변경 전반에 적용
- 포스터 클릭 시 해당 페이지 탭으로 전환
- 재판 뱃지 블러 처리 시 레이어 분리(z-index) 버그 수정
- 저장/불러오기 시 제품명·뱃지색·커스텀 로고가 누락되던 문제 수정
- 재판 체크박스 상태가 저장 안 되던 버그 수정
- 재판 배지 옵션을 3열 레이아웃(배경색/글자색/글자블러 ON-OFF)으로 재편
- 재판 배지 글자블러 ON/OFF 토글이 실제로 동작하도록 수정 (하드코딩된 값 제거)
- 글자블러 OFF 적용 대상을 배지가 아닌 셀 배경(blur-bg)으로 재수정
- "글자블러"를 배경 블러가 아닌 배지 텍스트 자체 blur로 제어하도록 수정
- "글자그림자" ON/OFF 옵션 추가 (기본 OFF)
- 그리드 이름 영역에 마우스 올리면 텍스트 커서로 변경
- 사진 교체 버튼 추가 (그리드 셀 + 패널 아이템 행 양쪽)
- 이름 영역 pointer-events 수정으로 텍스트 커서 정상 작동
- 사진 교체 버튼 위치를 패널 제거 → 셀 중앙 hover 버튼으로 변경
- 사진 교체 버튼을 다시 그리드에서 제거하고 패널 썸네일 hover 버튼으로 이동
- 그리드 버튼 크기 66px로 확대, 교체 버튼을 삭제 버튼 아래로 배치
- 줄별 글자크기 설정을 페이지별 → 전체 페이지 공유로 변경
- 하단 버튼 3열 배치: 초기화 / 개별 다운로드 / 전체 다운로드
- 제품 행에 메모 체크박스 + 입력칸 추가
- 메모 기능 확장: 글자크기·색상·위치(위/옆) 조절 가능
- 메모 컨트롤 UI를 한 줄로 정리 (크기 축소)
- 재판 배지 배경색/글자색 입력을 텍스트 → 컬러피커로 교체
- 메모 입력행을 썸네일 포함 전체 너비로 확장
- 드래그 핸들 + 순서번호 통합, 특전 체크박스 추가
- 블러/흰배경 옵션을 드롭다운으로 통합, 특전 배지(검정 배경/흰 글자) 추가
- 블러/흰배경 스테퍼를 숫자 입력 스피너로 변경 (특전 배지 텍스트 버그 수정)
- 코드 오류(bgSel → bgStepper) 수정 — 포스터 전체가 하얗게 나오던 버그
- 블러 모드 스테퍼를 < 블러 > 형태로 변경
- 블러 스테퍼를 세로 배치(라벨+▲▼)로 변경, 드래그 핸들과 동일한 구조로 통일
- 미리보기 확대 시 이미지 잘림 현상 제거, 베이지 배경에 grab 커서 추가
- 베이지 배경 드래그로 화면 이동(패닝) 기능 추가
- 패닝 조건 수정 — 배경 직접 클릭 시에만 반응하도록 보정
- 패닝 가능 영역을 스테이지 전체로 확장 (포스터/툴바 제외)
- 스테이지 좌우 여백 확장 (28px → 80px)
- 스테이지 정렬 방식 수정 — 확대해도 좌측 여백이 사라지지 않도록 보정
- 도움말 섹션 추가 (접힘 상태, 8개 항목)
- 재판/특전 뱃지 위치 로직 여러 차례 조정 — 최종적으로 "메모 있으면 항상 위, 없으면 넘칠 때만 위로" 규칙 확정
- 베이지 배경에서 마우스 휠로 미리보기 확대/축소
- 그리드 셀 클릭 시 패널의 해당 항목이 하이라이트되고 스크롤 이동
- 하이라이트를 다른 셀 클릭 전까지 유지되도록 수정

## 2026-07-02 (V1.3 팀 공유 기능)
- 📤 내보내기 / 📥 불러오기 버튼 추가 — JSON 파일로 작업 상태를 팀원과 공유 가능

## 2026-07-03 (V1.4 ~ V1.9 안정성 개선 및 신규 기능)
- localStorage 저장 실패(용량 초과) 시 조용히 무시되던 문제 → 경고 토스트 표시, 이미지 압축률 조정
- 저장 방식을 localStorage → IndexedDB로 전환 — 5MB 용량 제한 해제
- **오픈용 헤더** 신규 기능 추가 (검정 그라데이션 오버레이 스타일의 두 번째 헤더 옵션)
- 오픈용 헤더를 그리드 위에 덮어씌우는 2계층 오버레이 구조로 개선, 스토어명 박스 반투명 처리
- 오픈용 헤더 그라데이션 농도/퍼짐 조절 슬라이더 추가
- 오픈용 헤더 스토어명 박스 굵기 선택, 투명도 슬라이더, 크기 범위 2배 확대
- 오픈용 헤더 관련 옵션들을 3열 레이아웃으로 정리
- 사진 드래그·확대 시 window 이벤트 리스너가 계속 누적되던 메모리 누수 버그 수정
- 그리드 빈 공간 드롭 시 이벤트가 중복 실행되던 버그 수정, 죽은 코드 정리
- 도움말 8개 항목 전체에 실제 화면 스크린샷 + 강조 표시(화살표/박스) 이미지 추가
- 도움말 정리: "미리보기 확대/이동" 섹션 삭제, "위치조정" 섹션 이미지 제거, "오픈용 헤더"·"팀공유" 안내 추가
- **월별 저장 슬롯** 기능 추가 — 달마다 독립된 저장 슬롯 생성/전환, 팀 인수인계 시 JSON과 병행 사용
- **클립보드 붙여넣기(Ctrl+V)** 지원 — 이미지 붙여넣으면 항상 마지막 페이지 빈 칸에 자동 추가
- "오픈용 헤더"를 별도 섹션에서 "레이아웃 수정" 섹션 내부로 통합해 패널 구조 정리
- 구분선 제목("헤더" 등) 글자 크기 확대, "헤더" → "프리뷰용 헤더"로 명칭 변경
- 오픈용 헤더 스토어명 박스를 완전한 알약형(pill)으로 변경, 로고 비율 왜곡(세로로 찌그러짐) 수정
- 오픈용 헤더 세부 레이아웃 재배치, 스토어명 글자 굵기 절반으로 축소, 타이틀 폰트를 페이퍼로지 → 프리텐다드로 변경
- 오픈용 헤더 날짜 문구 굵기를 고정값(400)으로 단순화
- GitHub Pages 배포 실패 이슈 대응 (인프라 오류로 인한 재배포 트리거 다수)
