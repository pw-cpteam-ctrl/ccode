# WORKLOG - megahouse-matome (메가하우스 마토메 메이커)

`WORKLOG-작성-규칙.md` 기준으로 작성.

---

<details open>
<summary>2026-09-03 (오늘)</summary>

## 2026-09-03

### UI 전반 리뉴얼 요청 — 먼저 숫자로 재고 나서 손댐
- **무슨 문제/요청이 있었나** ▶ 레포 공통 지침(`UI-리뉴얼-시-신경쓸것.md`)을 참조해서 이 도구를 리뉴얼하고 싶다며, 무엇부터 하면 좋을지 물어봄
- **확인해보니** ▶ 지침이 "고치기 전에 실제 쓰는 분량으로 채워서 숫자로 재라"고 정해두고 있어, 사진 8장 + 실제 길이 제품명을 넣고 1920 / 1440 / 1366 세 폭에서 측정함
  - 조작요소가 앉은 줄 77개 중 40개(52%)가 혼자 한 줄을 차지, 그중 26개는 폭이 패널 절반도 안 씀 (헤더 표시 체크박스 13px=3%, 슬롯 추가 ＋ 42px=11%, 배지 스텝퍼 ▲▼ 28px=7%)
  - 설정 묶음 4개를 동시에 펼 수 있어 패널 스크롤이 3157px까지 늘어남 — 1366 화면의 4.1배
  - 사진 목록 항목당 조작요소 13개 × 8장 = 104개가 늘 화면에 떠 있음
  - 반면 흔히 의심하는 "설명문이 너무 많다"는 해당 없음 (설명 대 조작 면적비 0.66, 가장 큰 설명 블록이 전체 설명 면적의 5.7%뿐)
- **어떻게 처리했나** ▶ 지침 순서대로 ① 진단 보고 → ② 버그만 따로 먼저 → ③ 클릭되는 목업으로 합의 → ④ 실제 코드 반영으로 진행. 목업은 레포를 건드리지 않는 링크(Artifact) 형태로 만들어 여러 차례 수정하며 방향을 맞춤
- **결과** ▶ 모바일은 거의 안 쓴다고 해서 범위에서 제외하기로 확정. 개편 강도는 "구조까지 재편"으로 정함

### 하단 버튼 바가 패널 글씨를 덮고 있던 버그 (배치 변경과 분리해 먼저 처리)
- **문제** ▶ 진단 중 스크린샷에서 "스토어명 텍스트" 입력칸 위에 "초기화" 버튼이 올라타 보임
- **확인** ▶ 원인은 화면에 붙는 배치(`position:sticky`)가 아니라, 보조 버튼 세 개의 배경이 아예 투명이었던 것 (`button.ghost{background:transparent}`) — 버튼 면적을 그대로 통과해 뒤 패널 내용이 읽혔음
- **처리** ▶ 보조 버튼 배경을 다른 입력 요소와 같은 흰색으로 지정 (`var(--ui-card)`), 버튼 바 배경이 35% 지점부터 불투명해지던 것을 10px 안에 불투명해지도록 좁힘
- **결과** ▶ 계산된 배경색이 흰색으로 잡히는 것과 겹침이 사라진 것을 스크린샷으로 확인. 지침대로 배치 변경과 섞지 않고 별도 커밋으로 분리

### 좌측 패널을 사진 / 디자인 / 도움말 3개 탭으로 재편
- **처리** ▶ 진단에서 나온 축에만 처방을 적용함
  - 동시에 펼 수 있던 설정 묶음 4개를 탭 3개로 바꿔 한 번에 한 묶음만 보이게 함 (`switchGroup()`, `.gpane`)
  - 라벨·조작·값을 항상 같은 줄에 앉힘 (`.frow`) — 짝인 값(시작/퍼짐, 농도/압축)은 한 줄에 나란히
  - 셀 배경 블러·하단 그라데이션을 로고 옆에서 떼어내 사진 쪽으로 옮김 — 페이지 전체 사진에 걸리는 설정이라 목록과 같은 화면에 있어야 맞음
  - 사진 목록은 썸네일·이름만 남기고 세부 설정은 눌렀을 때만 펼치게, 교체·삭제는 마우스 올릴 때만 보이되 터치 기기는 항상 보이게 예외 처리 (`@media (hover:none)`)
  - 개선사항 메모장을 도움말 안쪽에서 꺼내 결과물 버튼 옆으로 옮김 — 안 보이는 곳에 있으면 있는 줄도 모름
  - 결과물 버튼을 2줄로 나누고, 되돌리기 어려운 초기화는 작은 링크로 분리
- **결과** ▶ 상시 노출 조작요소 127→48개, 혼자 한 줄 차지 40→14개(그중 좁은 것 26→8개), 항목 상시 조작요소 104→32개, 패널 스크롤 3157→1341px(사진)/993px(디자인). 새 항목이 없던 예전 저장본을 브라우저 저장소에 직접 넣고 새로고침해 사진·배지·월·타이틀 크기가 그대로 복원되는 것까지 확인. 포스터(아트보드) 쪽은 한 줄도 건드리지 않음

### 버튼 바가 목록 중간을 덮던 문제 (같은 작업 중 발견)
- **확인** ▶ 버튼 바가 화면에 붙는 배치인데 패널 아래쪽 여백 100px 때문에 화면 맨 아래가 아니라 그보다 100px 위에서 멈췄음 (`.panel`의 `padding-bottom:100px`). 버튼이 한 줄일 땐 티가 덜 났는데 3줄이 되며 드러남
- **처리** ▶ 붙이는 방식을 걷어내고 패널을 [머리 | 스크롤 본문 | 버튼] 3단으로 나눠 구조적으로 겹칠 수 없게 함 (`.pane-scroll`)
- **결과** ▶ 목록이 버튼 바 위에서만 스크롤되고 가려지는 항목이 없어짐

### 이름만 보고 무슨 값인지 모르던 항목 이름 정리
- **문제** ▶ "블러"와 "그라데이션"이 무엇에 걸리는 설정인지 패널에서 구분이 안 된다는 지적. 그라데이션은 오픈용 헤더에도 따로 4개가 있어 같은 단어가 두 곳에 나왔음
- **처리** ▶ "그라데이션" → "하단 그라데이션", "블러 설정" → "셀 배경 블러 (셀 배경이 '블러'인 칸에만 적용)"로 바꿔 셀 배경 선택지(블러/흰배경/없음)와 같은 말로 맞춤. 이후 줄임말이 오히려 헷갈린다는 지적을 받아 "그라데" 표기는 다시 "그라데이션"으로 풀어씀
- **결과** ▶ 슬라이더 개수·범위·동작은 그대로고 문구만 바뀜

### 목업과 실물이 다르다는 지적 — 세 군데가 실제로 안 맞춰져 있었음
- **문제** ▶ 목업을 만들어 합의까지 했는데 실물 생김새가 다르다는 지적
- **확인** ▶ 스크린샷을 나란히 놓고 대조하니 구조만 옮기고 생김새를 안 맞춘 곳이 있었음
  - 그룹 탭이 선택 시 파랗게 채워진 상자였음 (목업은 회색 레일 위 파란 글씨 + 파란 밑줄)
  - 페이지 탭이 "사진 업로드" 섹션 안에서 한 줄을 다 먹는 큰 파란 박스였고, 디자인·도움말 탭에 있으면 지금 몇 페이지를 편집 중인지 아예 안 보였음
  - 목업에 있던 저장 상태 표시가 실물에 없었음
  - 사진 행 펼친 모습도 옛날 한 줄 구조 그대로였음 — 행을 만드는 함수가 요소를 코드로 하나하나 만들고 이벤트를 직접 붙이는 구조라(`makeItemRow()`) 손대면 체크박스·순서 드래그·페이지 이동이 깨질 위험이 있어 안전한 쪽을 택했던 것
- **처리** ▶ 탭을 밑줄형으로, 페이지 탭을 그룹 탭 위 고정 자리에 내용 폭만 쓰는 알약형(103px)으로, 저장 상태 표시 추가, 펼친 행은 기존 요소와 이벤트를 그대로 옮겨 담아 라벨 붙은 줄로 재구성
- **결과** ▶ 목업과 같은 모양으로 맞춰짐. **위험해서 범위를 줄일 땐 그 사실을 완료 보고에 반드시 적어야 한다는 것을 놓친 사례** — 판단 자체보다 말하지 않은 것이 문제였음

### 사진 칸을 눌러도 세부 설정은 또 화살표를 눌러야 나오던 불편
- **문제** ▶ 포스터 칸을 누르면 파란 표시만 들어오고 세부 설정은 화살표를 한 번 더 눌러야 나와서 같은 상품에 두 번 손이 감. 펼친 줄의 "표시", "옮기기" 같은 라벨은 옆을 보면 아는 것들이라 자리만 먹음
- **처리** ▶ 포스터 칸을 누르면 그 상품 행이 바로 펼쳐지고, 다른 칸을 누르면 이전 것은 접히게 함(한 번에 하나). 페이지 이동 버튼은 삭제 ✕ 아래 세로로 수납해 3페이지 중 2페이지를 보고 있으면 ✕ / ←1 / →3 세 칸이 되게 함. 펼친 줄은 라벨을 전부 없애고 체크 3개 · 구분선 · 배경 스테퍼 한 줄로 정리
- **같이 고친 것** ▶ 펼친 상태가 목록을 다시 그릴 때마다 사라져 체크 하나만 눌러도 행이 접혔음 (`refresh()`가 목록을 다시 만들면서 클래스가 날아감) → 펼친 상품을 따로 기억해두고 복원하게 함 (`_openItemId`, `setOpenItem()`)
- **결과** ▶ 3페이지 상태에서 세로칸이 ✕ / ←1 / →3으로 나오고, 칸을 바꿔 누르면 하나만 펼쳐지며, 목록을 다시 그려도 펼침이 유지되는 것 확인

### 신규: 작업가이드 · 셀 배경 색상 직접 고르기
- **처리** ▶ 배치할 때 지킬 원칙(인기 상품 위로, 같은 IP 인접, 사진 크게, 글자가 피규어 가리지 않게, 흰 배경 사진 지양)을 제목 오른쪽 버튼에서 바로 펼쳐볼 수 있게 함 — 사진을 옮기는 도중에 확인하는 것이라 탭 안에 넣으면 그때마다 탭을 갈아타야 함
- **처리** ▶ 셀 배경 선택을 세로 ▲▼에서 가로 ◀ 이름 ▶으로 바꾸고 "색상"을 추가 — 고르면 옆에 색 선택기가 나타나고 그 색이 칸 배경으로 들어감 (블러 / 흰배경 / 색상 / 없음 순환)
- **결과** ▶ 배경이 네 단계로 순환하고 색상일 때만 색 선택기가 나타나는 것, 고른 색이 칸에 반영되는 것 확인

### 자잘한 수정
- 개선사항 메모장 팝업이 감싸는 버튼 칸(112px) 폭을 물려받아 입력창이 82px밖에 안 됐던 것을 350px로 넓힘 — 좌우를 모두 고정해뒀던 것을 오른쪽만 고정하고 폭을 따로 지정
- 하단 버튼 세 개 중 개선사항만 10px 좁았던 것을 칸 크기가 내용과 무관하게 정해지는 방식으로 바꿔 정확히 삼등분 (`grid-auto-columns:1fr`)
- 저장 슬롯을 라벨·드롭다운·＋ 버튼 한 줄로 합치고, 한 번 읽으면 끝나는 안내 문구를 13px→12px로 줄임
- 셀 배경 블러·하단 그라데이션 묶음을 제품명 일괄입력 아래로 옮김 — 사진을 올리고 이름을 채운 뒤에 손대는 값이라 실제 작업 순서에 맞춤
- 펼침 화살표가 24px·11px 글자라 점처럼 보였던 것을 30px·15px로 키움

### 아직 반영하지 않고 대기 중인 요청 (사용자가 모아서 한 번에 하기로 함)
- 작업가이드에 "첫 줄 2칸 권장", "비인기·남성향 상품은 뒷페이지에 그룹핑" 추가
- 저장 상태 표시를 실제 저장 완료 기준으로 바꾸기 — 지금은 저장 함수가 불린 뒤 0.5초 타이머라 실패해도 "저장됨"이 뜸
- 펼친 줄 중앙정렬, 가로 구분선 제거, 배경 화살표 크기 복원(28×20px)
- 작업가이드 버튼 색을 노란 계열로 (포스터 오픈일 노랑과 어울리는 톤)
- 펼침 화살표 버튼 삭제 — 포스터 칸 클릭만으로 펼침·접힘이 되므로 접을 일이 사실상 없다는 판단
- 스토어명 위치 기본값을 중앙으로 — 지금은 로고와 오픈일 폭에 밀려 "남는 공간의 가운데"에 놓임
- 타이틀 뒤에 -2, -3을 붙이는 드롭다운 추가

</details>

<details>
<summary>이전 기록 (2026-09-02 이전)</summary>

## 2026-08-07

### 사진이 종횡비 안 맞을 때 잘리고 위아래 드래그가 안 되던 문제 (근본 수정까지 3차 시도)
- **무슨 문제/요청이 있었나**: 그리드 사진 여백(빈 배경) 방지 코드를 넣은 뒤, 종횡비가 칸이랑 안 맞는 사진(예: 세로로 긴 인물 사진)에서 머리·글씨가 잘리고, 잘린 부분을 드래그로 되찾으려 해도 위아래로 안 움직인다는 반복 신고
- **확인해보니(1차, 실패)**: 처음엔 CSS `object-fit:cover`로 여백을 없앴는데, 이게 **칸의 고정 크기 기준으로 이미지를 이미 한 번 크롭해서 렌더링**해버려서, 그 위에 얹은 확대·이동은 이미 잘려나간 결과물만 움직일 뿐 크롭된 부분은 애초에 화면에 없어 드래그로도 못 돌아옴 (`object-fit:contain·cover`류 공통 함정)
- **확인해보니(2차, 근본 원인 확정)**: `object-fit`을 아예 버리고 이미지 원본 크기(`naturalWidth/Height`) 기준으로 직접 `width/height/left/top`을 계산하는 진짜 cover-fit 수식(`baseScale = max(칸가로/사진가로, 칸세로/사진세로)`)으로 전면 재작성. 이러면 "잘리는" 부분도 실제 이미지 데이터로 남아있어 드래그로 끝까지 훑어볼 수 있음
- **어떻게 처리했나**: `applyImgTransform()`을 `object-fit` 없이 직접 좌표 계산하는 함수로 교체, 드래그/터치 이동량 계산에서 배율 나누기 제거(화면 그대로의 픽셀 단위로 변경), 로드 완료 후(`img.onload`)·행 높이 슬라이더 변경 후 재계산되도록 보강. 추가로 Shift+휠로 "사진 전체가 다 보이는" 지점까지는 축소 허용(`containMinScale`)
- **결과**: Playwright로 세로로 긴 사진의 잘렸던 머리 부분이 드래그로 실제 노출되는 것, 어떤 경우에도 칸을 완전히 덮는 것, 행 높이를 극단적으로 바꿔도 정확히 커버되는 것, Shift 뗀 후 안전 배율로 스냅되는 것까지 모두 검증. **"버그가 재신고되면 이전 수정을 의심하고 근본부터 재검토할 것"을 다시 한번 상기한 사례**

### 블러 배경·흰배경 연출이 위 수정 이후 안 보이던 버그
- **무슨 문제/요청이 있었나**: 위 cover-fit 수정 이후 "블러 배경 켜도 블러가 안 보인다"는 신고, 이어서 "블러↔흰배경 오갈 때 사진 크기(줌)가 확 바뀌어 보인다"는 추가 신고
- **확인해보니**: 블러/흰배경은 원래 "사진이 칸을 다 못 채우고 남는 자리에 배경이 비쳐 보이는" 연출인데, 새 cover-fit 기준(cover=항상 꽉 채움)으로는 사진이 항상 칸을 덮어버려 배경이 보일 자리가 아예 없어짐. 흰배경은 그 중에서도 기준을 안 고쳐서 블러(contain 기준)와 오갈 때마다 크기가 확 달라 보였음
- **어떻게 처리했나**: `it.blur`나 `it.whiteBg`가 켜진 사진은 기준을 cover가 아니라 contain(전체가 다 보이는 크기)으로 분기. 그리고 "1줄 높이" 슬라이더 재계산 로직이 셀 안의 블러 배경 이미지(`.blur-bg`)를 메인 사진으로 착각해 크기를 잘못 건드리던 별도 버그도 같이 발견해 수정(`:not(.blur-bg)`로 명시)
- **결과**: 체크무늬 테스트 이미지로 스크린샷 찍어 블러가 다시 칸 전체를 감싸는 것 확인, 블러→흰배경 전환 시 사진 크기가 완전히 동일하게 유지되는 것도 검증

### 자잘한 마무리: 배지 정렬, 사진 화질, 오픈헤더 그라데이션·로고, 이름표 가림
- **무슨 문제/요청이 있었나**: (1) 이름 옆 배지가 10px쯤 가라앉아 보임 (2) 세로로 긴 사진을 올리면 화질이 눈에 띄게 나쁨 (3) 오픈용 헤더 그라데이션 조절 슬라이더가 2개뿐이라 부족함 (4) 이름표(제품명)에 사진 아랫부분이 가려질 때 위로 당겨서 조정하려 해도 cover-fit 제한 때문에 안 움직임 (5) 오픈용 헤더 켠 상태에서 로고 파일을 바꿔도 화면에 반영이 안 됨
- **확인해보니**: (1) 배지·이름을 담은 줄이 `align-items:flex-end`(바닥 맞춤)라, 배지 글자가 이름보다 작아서 배지 윗부분이 비어 보였음 — 실제로 center로 바꿨을 때 어떻게 보이는지 스크린샷 2장 찍어 비교 후 사용자 확인받고 반영 (2) 사진 압축 품질이 0.75였는데, git 이력을 뒤져보니 원래 최초 버전은 0.9였다가 2026-07-03에 `localStorage` 용량 초과 회피용 임시조치로 0.75까지 낮춘 뒤, 그 직후 저장소를 IndexedDB로 옮겨 용량 문제 자체가 해소됐는데도 품질은 다시 안 올려놓은 채 방치돼있던 것 (3) 오픈헤더 그라데이션이 농도·퍼짐 2개뿐이고 일반 그라데이션(제품명 아래 그라데이션)은 이미 시작점·퍼짐·농도·압축 4개였음 (4) 이름표가 있는 칸은 그 영역이 이름표 자체의 짙은(88% 검정) 그라데이션으로 덮여 있어서, 위쪽으로만 이름표 높이만큼 더 당겨도 실제로는 안 보임 (5) 로고 교체 코드가 기본 헤더 로고(`.p-logo`)만 갱신하고 오픈헤더 로고(`.oh-logo`)를 빼먹고 있었음
- **어떻게 처리했나**: (1) `align-items:center`로 변경 (2) JPEG 압축 품질 0.75→0.9로 복원 — 100%까지 올리는 것도 검토했으나 실제 테스트 결과 90%보다 파일 용량이 3배 가까이 커지는데 눈에 보이는 차이는 거의 없어 90%로 확정 (3) 오픈헤더 그라데이션도 시작점·퍼짐·농도·압축 4개로 확장 (4) 이름표가 있는 칸에 한해 위쪽 이동 한계만 이름표 실제 높이(`clientHeight`)만큼 추가 확보, 좌우·아래쪽과 이름표 없는 칸은 기존 그대로 유지 (5) 로고 교체 시 `.oh-logo`도 같이 갱신하도록 수정
- **결과**: 각각 Playwright로 배지 위치 수치·화질 용량 비교표·그라데이션 반영값·동일 크기 셀 3개 비교(이름 있음/없음/뱃지 포함)·오픈헤더 로고 갱신 여부까지 모두 검증 후 반영. 사용자가 직접 만든 스크린샷 비교로 결정한 것(배지 정렬)과 git 히스토리 발굴로 근거를 찾은 것(화질)까지 과정을 투명하게 남김


## 2026-07-31

### 미리보기 줌 팝업 그랩 이동 + 보드 순서 교체 기능 추가
- **무슨 문제/요청이 있었나**: 스테이지 상단에 항상 고정돼있던 미리보기 줌 팝업이 거슬리니 온오프하거나 옮길 수 있게 해달라는 요청, 그리고 보드(페이지) 간 순서를 바꾸는 기능을 새로 추가해달라는 요청
- **확인해보니**: 팝업은 `position:sticky`로 항상 상단에 고정된 구조였고, 페이지 탭은 클릭으로 전환만 가능할 뿐 순서를 바꾸는 기능 자체가 없었음
- **어떻게 처리했나**: 팝업을 `position:fixed`로 바꾸고 손잡이(⠿) 아이콘을 드래그하면 원하는 위치로 옮기고 마지막 위치를 기억하도록 만듦(`localStorage`에 좌표 저장). 페이지 탭에 HTML5 드래그앤드롭(`draggable`, `dragstart/dragover/drop`)을 붙여 탭끼리 순서를 바꿀 수 있게 함(`reorderPages()`)
- **결과**: Playwright로 팝업 이동 후 위치 저장, 페이지 순서 교체 후 각 페이지 내용물이 올바르게 따라 이동하는지 확인 완료 후 반영

### 팝업 손잡이가 너무 작아서 드래그가 안 걸리던 문제
- **무슨 문제/요청이 있었나**: 위에서 옮길 수 있게 만든 팝업을 실제로 써보니, 그랩해도 안 움직인다며 "왜 lock했냐"는 지적. 베이지 배경(스테이지 전체)이 잠긴 것 같다고도 함
- **확인해보니**: 코드 자체는 정상 동작했지만, 드래그 가능한 영역이 손잡이 아이콘(16px 크기) 하나뿐이라 실제로 정확히 그 지점을 클릭하지 못하면 전혀 안 움직였던 것. 베이지 배경은 원래 스테이지 전체를 스크롤하는 별개 기능이라 거길 잡아도 팝업은 안 움직이는 게 정상 동작이었음
- **어떻게 처리했나**: 드래그 가능 영역을 손잡이 아이콘만이 아니라 팝업 전체(슬라이더 등 조작 요소는 제외)로 넓힘
- **결과**: Playwright로 팝업의 빈 공간·라벨 텍스트 아무 곳이나 클릭해도 정상 이동하고, 슬라이더 조작은 그대로 되는 것 확인 후 반영

### 페이지 삭제 기능 부재 + 파일 드래그로 사진 교체 기능 추가
- **무슨 문제/요청이 있었나**: 페이지(보드)를 추가만 할 수 있고 삭제하는 기능이 없어서 추가해달라는 요청. 그리고 파일 탐색기에서 사진을 셀 위로 직접 끌어다 놓으면 그 칸 사진이 바로 교체되는 기능도 추가해달라는 요청(다른 프로젝트 코드 참고해 정리한 스니펫 제공받음)
- **어떻게 처리했나**: 페이지 탭마다 × 버튼을 추가해 `deletePage()`로 삭제하되, 마지막 남은 1페이지는 삭제를 막고 사진이 있으면 확인창을 띄우도록 함(실행취소로 복구 가능). 셀의 `drop` 이벤트에서 `e.dataTransfer.files`로 파일 드래그인지 먼저 확인해, 파일이면 그 칸 사진만 바꾸고 기존 셀↔셀 순서교환 드래그 로직은 그대로 유지
- **결과**: Playwright로 페이지 3개 중 가운데 삭제 후 나머지 페이지 데이터 유지, 실제 drop 이벤트로 파일 이미지 교체 모두 정상 동작 확인 후 반영

### 캔버스가 화면 상단에 붙어 보이고 세로로 드래그가 안 되던 문제
- **무슨 문제/요청이 있었나**: 브라우저를 거의 최대화하고 줌 50%, 페이지 3개에 사진을 실제로 올린 상태에서, 베이지 배경을 세로로 드래그해도 전혀 안 움직이고 가로만 움직인다는 신고. 화면 중앙이 아니라 계속 위에 붙어 보인다고도 함
- **확인해보니**: 실제로 그 조건(1920×1080, 줌 50%, 3페이지, 사진 있음)을 그대로 재현해보니 캔버스 세로 길이가 화면 높이에 정확히 딱 맞아서 스크롤할 여백 자체가 0이었음 — 코드가 막은 게 아니라 진짜 남는 공간이 없었던 것. 여백을 아래쪽에만 줬던 것도 원인 중 하나(화면 로드 시 항상 맨 위 스크롤 위치에서 시작)
- **어떻게 처리했나**: 스테이지 위아래 여백을 대칭으로(화면 높이의 45%씩) 바꿔서 항상 세로 드래그 여지가 남도록 하고, 처음 열었을 때 스크롤 위치를 그 여백 한가운데로 자동 이동시켜 캔버스가 화면 중앙에서 시작하도록 함
- **결과**: Playwright로 동일 조건(1920×1080, 줌50%, 3페이지+사진) 재현해 세로 여백이 0에서 확보된 상태로 바뀐 것, 초기 로드 시 스크롤이 중앙으로 맞춰지는 것, 위/아래 양방향 드래그 모두 정상 동작하는 것 확인 후 반영

## 2026-07-16

### 메인 허브 도구명 변경 + 깃헙 계정명 변경 대응
- **무슨 문제/요청이 있었나**: 허브 페이지의 도구 이름을 "메가하우스 신제품 예고 메이커"에서 "메가하우스 마토메 메이커"로 바꿔달라는 요청. 그리고 깃헙 계정명을 `znddidk2-ctrl`에서 바꿀 예정인데 지금 만든 페이지에 영향이 있는지 물어봄
- **확인해보니**: `megahouse-matome/index.html`을 포함해 도구 파일들에 계정명·도메인이 하드코딩된 곳이 없어(전부 상대경로), 계정명이 바뀌어도 페이지는 그대로 작동함을 확인. 도구명 표기는 루트 `index.html`(허브)에 있었음
- **어떻게 처리했나**: 허브의 카드 이름을 "메가하우스 마토메 메이커"로 변경. 사용자가 실제로 계정명을 `pw-cpteam-ctrl`로 바꾼 뒤, 로컬 git 원격 주소를 새 주소로 업데이트하도록 안내(`git remote set-url`)
- **결과**: `main` 반영 완료, 새 주소(`pw-cpteam-ctrl.github.io/ccode`)에서 정상 확인

### 로컬 git pull이 계속 실패하던 문제 (잘못된 파일명)
- **무슨 문제/요청이 있었나**: 수정을 push했는데도 사용자 PC에 반영이 안 됨. 로컬 index 파일 날짜가 옛날 그대로였음
- **확인해보니**: 이전에 실수로 파일명 자체에 윈도우 경로(`C:\Users\Brain\ccode\CHANGELOG-작성-규칙.md`)가 통째로 들어간 파일이 커밋돼 있었고, 윈도우는 파일명에 `\`·`:`를 못 써서 `git pull`이 `invalid path`로 매번 중단되고 있었음. 그래서 사용자 로컬이 한 번도 최신화되지 못한 것
- **어떻게 처리했나**: 그 잘못된 파일을 저장소에서 삭제(`git rm`)하고 push. 이후 `git fetch` + `git reset --hard origin/main`으로 로컬을 강제로 최신과 일치시키도록 안내
- **결과**: pull 정상화, 로컬 파일 최신화 확인

### 다운로드 이미지에서만 글자가 잘리는 버그 (미리보기는 정상)
- **무슨 문제/요청이 있었나**: 다운로드한 PNG에서 제품명(리로드·코난 등)과 오픈헤더 스토어명 끝 글자가 잘리고 뱃지와 겹친다는 신고. "미리보기랑 실제 다운로드 결과가 다르다"는 지적이 반복됨
- **확인해보니**: 다운로드 엔진(html-to-image)이 텍스트를 화면보다 미세하게 넓게 그리는데, 글자 상자가 내용에 딱 맞게 `overflow:hidden`으로 잘려 있어 끝 글자가 잘려나가는 것이 원인. 처음 다섯 번은 테스트 환경에 실제 폰트가 없어 재현이 안 돼 헛다리를 짚었고, 사용자 실제 데이터 + 프리텐다드/페이퍼로지 폰트를 강제로 심어 재현한 뒤에야 원인을 눈으로 확정함(상세 시도 기록은 CHANGELOG 참고)
- **어떻게 처리했나**: 강제 잘림 제거 + 긴 이름은 뱃지를 윗줄로 이동 + 글자 크기 자동 축소에 여유 마진 부여 + 폰트 로드 후 재조정. 실제 폰트 환경에서 다운로드 결과물 자체를 미리보기와 나란히 비교해 검증
- **결과**: 미리보기·다운로드 결과가 일치하고 모두 안 잘리는 것을 브라우저로 확인 후 `main` 반영. **재발 방지 교훈을 CHANGELOG에 교과서 형태로 상세 정리**

### 뱃지 수정 과정에서 원래 있던 반응형 동작을 훼손 → 사용자 지적으로 재점검
- **무슨 문제/요청이 있었나**: 위 잘림 버그를 고치면서 "이름이 길면 뱃지를 무조건 윗줄로 올리는" 규칙을 넣었는데, 이게 원래 있던 사양(글자 크기에 따라 옆에 들어가면 옆, 안 되면 위로 반응형)을 멋대로 바꿔버린 것이었음. 사용자가 "왜 또 거지같이 바꿔놨냐, 원래 그런 설정이었다"고 강하게 지적
- **확인해보니**: 지적이 맞았음 — 고정 임계값(칸의 80% 넘으면 무조건 위로)으로 기존 반응형 로직을 덮어써서, 옆에 있어도 됐을 페르소나3 리로드 같은 이름까지 위로 올라가 버렸던 것
- **어떻게 처리했나**: 원래 동작으로 복원 — 뱃지는 이름 옆 유지가 기본, 이름이 길면 글자 크기를 반응형으로 줄여서 옆에 맞추고, 그래도 안 될 만큼 극단적으로 길 때만 위로. 다운로드 시 겹침은 뱃지를 옆에 두는 대신 이름 상자에 여유 폭을 확보하는 방식으로 대응
- **결과**: 사용자 데이터로 재검증 후 반영. **"버그 고치다 근처의 잘 작동하던 로직까지 덮어쓰지 말 것"을 CHANGELOG 교훈에 추가**

### 뱃지 글자 크기가 이름 대신 슬라이더 값을 그대로 따라가던 버그
- **무슨 문제/요청이 있었나**: 헌터x헌터·페르소나3 리로드가 한 줄에 있는 상태에서 줄 글자크기 슬라이더를 올리면, 헌터는 이름이 커지는데 페르소나는 이름은 그대로고 "초회특전" 뱃지만 커지는 이상한 현상 신고
- **확인해보니**: 뱃지 글자 크기가 CSS `em` 단위로 "칸 기준 크기(슬라이더 값)"에 그대로 묶여 있었음. 페르소나는 이름이 칸에 맞춰 자동으로 작아지는데 뱃지는 슬라이더 값 그대로 커지니 균형이 깨진 것
- **어떻게 처리했나**: 뱃지 크기를 슬라이더 값이 아니라 "실제로 화면에 표시되는 이름 크기"에 비례하도록 계산해서 직접 지정
- **결과**: 슬라이더를 올려도 뱃지가 이름 크기를 따라 같이 커지고 작아짐을 확인 후 반영

### 다운로드-미리보기 미묘한 차이(뱃지 밀림·스토어 정렬)의 진짜 근본 원인 해결
- **무슨 문제/요청이 있었나**: 뱃지 텍스트 위치가 미묘하게 밀리고, 오픈헤더 스토어명이 중앙정렬 안 되고 밀려 보인다는 신고. "익스포트랑 미리보기가 왜 자꾸 미묘하게 차이나냐"는 반복 지적과 함께, 페르소나처럼 이름이 길어 확대하면 뱃지가 커지기만 하고 위로 안 올라간다는 지적
- **확인해보니**: 지금까지 여러 차례(1~8차) 여유 마진으로 증상만 땜질해왔는데, 진짜 근본 원인은 따로 있었음 — 다운로드 엔진(html-to-image)이 CORS 때문에 CDN 폰트(프리텐다드·페이퍼로지)를 못 읽어서, 다운로드 시엔 화면과 다른 폴백폰트로 그려지고 있었던 것. 그래서 화면과 다운로드가 항상 미세하게 다른 폭으로 렌더됨
- **어떻게 처리했나**: 다운로드 직전 웹폰트를 CDN에서 받아 data URL로 만들어 `htmlToImage`의 `fontEmbedCSS` 옵션으로 직접 박아넣음 → 화면과 다운로드가 100% 동일한 폰트로 렌더. 이후 그동안 넣어뒀던 여유 마진들을 전부 제거하고, 배지 배치 로직도 "이름이 슬라이더 크기 그대로 옆에 들어가면 옆 유지, 안 들어가면 위로"로 정확하게 재정리. 배지 위치를 조정할 때마다 처음부터 다시 판단하도록 해서, 한 번 위로 올라간 배지가 조건이 바뀌어도 안 내려오던 부수 버그도 같이 잡음
- **결과**: 사용자 실제 데이터로 화면·다운로드 결과물을 나란히 비교 검증, 뱃지 밀림·스토어 정렬·잘림 모두 해소되고 페르소나 확대 시 배지가 정확히 위로 올라가는 것까지 확인 후 `main` 반영. 사용자가 "이제야 되네"로 확인. **CHANGELOG에 9차로 근본원인·해결법·교훈("여유 마진으로 덮기 전에 근본을 없애라") 상세 기록**

</details>
