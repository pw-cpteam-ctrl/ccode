# UI 리뉴얼 시 신경 쓸 것

> "UI 전반을 점검하고 개선해달라" 또는 "이 화면 리뉴얼해달라"는 요청을 받았을 때 쓰는
> 순서와 기준. 이 레포의 모든 프로젝트에 공통으로 적용되는 범용 지침이며, 특정 프로젝트
> 이야기는 넣지 않는다.
> 작업 방식 전반은 `handoff-behavior-guide.md`, 새 프로젝트 구조·배포는 `CLAUDE.md` 참고.

## 이 문서의 전제

- **요청은 대개 증상으로 온다** — "난잡하다", "쓰기 불편하다", "뭔가 답답하다", "정리해줘",
  또는 증상 없이 "전반적으로 점검해줘". 어느 쪽이든 **증상은 원인이 아니다.**
  같은 "난잡하다"가 과밀 때문일 수도, 배치가 작업 순서를 안 따라서일 수도 있다.
- **그래서 감으로 고치지 않는다.** 먼저 숫자로 재고, 그 다음에 처방을 고른다.
  순서를 반대로 하면 멀쩡한 걸 깎고 정작 원인은 그대로 남는다.
- **사용자는 코딩 입문자다** — 설명은 전문용어 없이, 사용자가 빠진 단계를 스스로 채워야 하는
  상황을 만들지 않는다.

---

## 1단계 · 진단 먼저 (고치지 말고 재기만)

실제로 쓰는 분량의 더미 데이터를 넣고 브라우저로 렌더해서 잰다. 빈 화면으로 보면 문제가
안 보인다 — 목록은 실제로 쌓이는 개수만큼, 텍스트는 실제 길이만큼 채워야 한다.

아래 7가지 축으로 훑고, **해당 없는 축은 "해당 없음"으로 넘긴다**(억지로 찾아내지 않는다).

### A. 어긋남 — 배치가 작업 순서를 안 따름
- **단계 간 이동거리**: 번호가 매겨진 단계(1·2·3…)를 순서대로 훑으며 다음 단계까지의 가로·세로
  이동 픽셀을 기록하고, 가로로 크게(200px 이상) 튀는 지점을 센다.
  *"왕복이 있다"만 알면 못 고친다. 어느 단계 사이에서 몇 px 튀는지 알아야 원인이 특정된다.*
  흔한 원인: 안내 문구와 배치가 어긋남(예: "버튼을 누르면 3번이 채워집니다"라고 써 있는데
  3번이 화면 반대편 열에 있음).
- **입력 ↔ 그 결과의 거리**: 값을 고치는 곳과 결과가 보이는 곳이 얼마나 떨어져 있는지.

### B. 과밀 — 시선을 똑같이 나눠 먹음
- **설명문 비중**: 읽기만 하는 안내 텍스트의 면적 대비 조작 가능한 요소의 면적. 가장 큰 설명
  블록 하나가 전체 설명 면적의 몇 %인지도 뽑는다(보통 이 하나가 범인이다).
- **항목마다 상시로 떠 있는 버튼 개수**: 목록·카드가 n개일 때 총 몇 개가 늘 보이는지, 화면
  전체 버튼 중 몇 %인지.
- **빈 공간과 불균형**: 영역(열·섹션)별 `실제 쓰는 높이` vs `전체 높이`.
  *설명문·버튼 개수만 재면 이건 절대 안 잡힌다. 그런데 한쪽은 꽉 찼고 다른 쪽은 절반이 비어
  있는 식으로, 화면 최대 낭비가 여기일 때가 많다.*
- **혼자 한 줄을 차지하는 요소 개수**: 같은 수평 띠를 공유하는 요소가 하나도 없는 것을
  세고, 그중 폭이 영역 절반 미만인 것을 따로 표시한다.
  *설명문 면적·버튼 개수·열 단위 빈 공간을 다 재도 이건 안 잡힌다. 열은 꽉 차 있는데
  그 안에서 줄마다 하나씩만 앉아 있는 경우가 있다 — 입력칸이 폭 95%를 쓰고 그 아래
  버튼이 폭 10%로 또 한 줄을 먹으면, 나머지 90%가 빈 채로 한 줄이 사라진다.
  줄여지는 세로 픽셀은 크지 않지만, 한 줄에 하나씩 훑느라 생기는 시선 이동이 실제 비용이다.*

### C. 불일치 — 같은 것이 다르게 동작함
- **같은 성격의 동작(삭제·편집·열기 등)이 영역마다 다르게 동작하는 지점.**
  *새로 설계하지 말고 **이미 잘 된 쪽을 기준으로 통일**한다 — 검증된 패턴이 이미 화면 안에
  있다는 뜻이라 가장 싸고 안전하다.*
- **화면 라벨과 실제 결과가 다른 곳** (→ 이건 버그다. 2단계로.)

### D. 발견 불가 — 있는데 못 찾음
- **화면 밖으로 밀리거나 접혀서 존재 자체를 모르게 되는 기능.**
- **방어 문구의 위치**: "여기는 ○○하는 화면이 아닙니다", "위 작업과는 영향이 없습니다"처럼
  오해를 막으려 넣은 문구. 이런 문구가 필요하다는 건 배치가 그 사실을 못 보여준다는 신호라,
  문구를 다듬기보다 배치로 해결되는지 먼저 본다.
- **위치는 멀쩡한데 색 때문에 묻히는 경우.** 화면에 이미 의미가 정해진 강조색(선택색·위험색
  등)이 있으면, 새 요소가 그 색과 겹쳐 오해를 주거나, 반대로 무채색이라 배경과 구별이 안 될
  수 있다. 배치를 고치기 전에 "이 요소만의 색이 있는지, 대비가 충분한지"부터 본다.

### E. 막힘·헤맴 — 다음에 뭘 해야 할지 모름
- **비활성(disabled) 버튼이 왜 눌리지 않는지 그 자리에서 알 수 있는지.**
- **시간이 걸리는 동작에 진행 표시가 있는지**, 끝났을 때 끝난 걸 알 수 있는지.
- **결과가 옛날 값인지 최신인지 사용자가 알 수 있는지.**

### F. 실수·유실 위험
- **되돌리기 어려운 동작(삭제·초기화·덮어쓰기)이 얼마나 쉽게 눌리는 자리에 있는지.**
- **새로고침·탭 닫기 후에도 작업이 남아 있는지** (이 레포 최우선 항목).
- 모든 입력 요소가 **값 변경 → 화면 반영 → 저장** 3단계를 빠짐없이 갖췄는지.

### G. 입력 수단·접근성
- **hover에만 의존하는 동작이 있는지** (터치 기기엔 hover가 없다).
- **키보드만으로 조작·이동이 되는지**, 포커스가 눈에 보이는지.
- **터치 타깃이 손가락으로 누를 만한 크기인지.**

---

## 2단계 · 버그는 따로, 먼저

**화면 라벨이 실제 결과와 다르다**, **표시된 값이 저장된 값과 다르다** 같은 불일치는 배치
변경과 섞지 말고 먼저 고쳐서 배포한다. 섞으면 나중에 문제가 생겼을 때 배치 때문인지 버그
수정 때문인지 못 가린다.

---

## 3단계 · 만들기 전에 합의부터

진단 결과를 보고하면서 **아래를 한 번에 묶어서 묻는다.** 하나씩 물어보면 사용자가 심문받는
느낌이 들고, 답이 모일 때까지 착수도 못 한다.

물어보는 방식:
- **선택지마다 대가를 같이 적는다** — 시간, 학습비용, 레포에 남는 흔적, 나중에 낡는지 여부.
  대가를 안 적으면 사용자가 "다 좋아 보이는데?" 상태가 돼서 고를 수가 없다.
- **추천안을 먼저 쓰고 왜 추천인지 붙인다.** 사용자는 결정하려고 묻는 게 아니라 판단 재료를
  받으려고 묻는 경우가 많다.
- **답이 작업을 바꾸지 않는 질문은 하지 않는다.** 버튼 하나 옮기는 수준이면 이 절차를 건너뛴다.

### 물어볼 것

**Q1. 비교본을 어떤 형태로 받고 싶은가 (전달 방식)**
- **Artifact 같은 호스팅 링크** — 링크라 팀원·상사에게 그대로 공유 가능, 레포를 안 건드림.
  대개 이게 낫다.
- **레포에 임시 폴더 + 배포 URL 하위 경로** — 팀 워크플로 안에 남지만 임시 폴더가 생기고
  나중에 지워야 함.
- **자체 완결 HTML 파일 전송** — 가장 간단. 공유하려면 파일을 직접 전달해야 함.

**Q2. 프로토타입을 어느 수준까지 만들까 (충실도)**
- **클릭되는 더미 목업** — 더미 데이터로 화면을 다 채우고 UI 상호작용(단계 이동·탭·접힘)은
  실제로 동작. 실제 업로드·연산·AI 호출은 안 함. **레이아웃·흐름 평가엔 이게 충분하고 빠름.**
- **실제 동작까지 복제** — 실사용 감각까지 확인되지만 작업량이 훨씬 크고, **원본이 바뀌면
  사본이 낡는다.**
- **정적 시안 이미지** — 가장 빠르지만 '흐름'을 평가할 수 없음.

**Q3. 재설계 강도는 어디까지 (개편 범위)**
- **정보 정리 위주** — 단계·구조는 그대로 두고 설명글을 접기/툴팁으로 정리, 컨트롤 그룹핑,
  시각 위계 세우기. **기존 사용자가 안 헤매면서 체감 개선이 큼.**
- **구조까지 재편** — 단계 통합·분리처럼 흐름 자체를 다시 짬. 개선폭은 크지만 **익숙한
  사용자에게 학습비용이 생김.**
- **둘 다 만들어 비교** — 보수적 안 / 과감한 안을 나란히 놓고 현재까지 3개를 비교.
  시간이 더 걸리지만 선택지가 명확해짐.

**Q4. 어느 화면·기기에서 완벽해야 하나 (기준 폭)**
쓰는 모니터 해상도와 기기를 확정한다. 이게 안 정해지면 유동/고정 폭, 세로 배치로 넘기는
기준점, 가로 스크롤 허용 여부를 전부 감으로 정하게 된다. "모바일은 볼 필요 없다"도
**명확한 답이므로 먼저 받아둔다** — 안 받으면 안 쓰는 화면에 시간을 쓴다.

**Q5. 건드리면 안 되는 영역이나 이미 정해둔 결정이 있나**
사용자가 앞서 직접 고른 것(폭 고정, 스크롤 방식, 항목 개수, 특정 폴더 제외 등)을 확인한다.
*이걸 안 묻고 "더 깔끔해서" 되돌리면 사용자가 이미 판단해서 정한 결정을 뒤집는 셈이 된다.*

---

## 4단계 · 배치는 시안 먼저

기존 코드를 건드리지 말고 **클릭되는 목업**을 별도 HTML로 만들어 보여주고, 확인받은 뒤
적용한다. 목업은 색·폰트·토큰을 실제 도구와 같게 맞춘다 — 그래야 "실물에서도 이럴까"를
판단할 수 있다.

### 증상 → 처방

진단에서 나온 축에만 해당 처방을 쓴다. 전부 다 적용하려 들지 않는다.

| 진단 | 처방 |
|---|---|
| A 어긋남 | **번호 붙은 단계는 한 방향으로만 흐르게.** 열의 역할을 나누면(입력 / 결과 / 출력) 대부분 저절로 풀린다 |
| A 입력↔결과 멀다 | **결과 영역을 스크롤 고정(sticky).** "결과 보러 올라갔다 내려오기"는 클릭 수로는 안 잡히지만 실제로 가장 피곤한 동작이다. 단 **화면에 붙는 열은 화면보다 커질 수 없다** — 옆 열이 화면 두세 배로 길면 두 상자 높이는 절대 안 맞고, 이건 either/or다(붙여서 항상 보이게 두거나, 높이를 맞추고 따라다니기를 포기하거나). 붙이기로 했으면 높이를 고정값으로 박지 말고 화면 높이 기준으로 잡는다 — 고정 상한을 쓰면 화면이 남는데도 거기서 잘린다 |
| B 한 줄 낭비 | **라벨·입력칸·버튼을 같은 줄에 앉힌다.** 짧은 컨트롤 여러 개는 한 줄에 나란히 두고, 입력칸과 그 값을 조절하는 슬라이더처럼 짝인 것은 반드시 같은 줄에 둔다. 이미 한 줄에 잘 붙어 있는 곳이 화면 안에 있으면 그걸 기준으로 맞춘다 |
| B 설명문 과다 | **지우지 말고 접히는 패널로. 기본은 펼침.** 단 **기본 상태와 "접은 걸 기억할지"를 같이 정한다** — 기본 펼침만 정하면 면적이 그대로라 정리 효과가 없다. 처음 한 번만 읽으면 되는 설명은 접은 상태를 기억시켜야 실제로 줄어든다(첫 사용자는 다 보이고, 아는 사람은 계속 접힌 채) |
| B 상시 버튼 과다 | **항목별 버튼은 마우스 올릴 때만.** 나타날 자리를 미리 잡아둬서(높이 고정) 카드가 흔들리지 않게. **터치 기기는 예외로 항상 보이게** |
| B 빈 공간 | 비어 있는 영역에 **원래 그 흐름에 속하는 것**을 옮긴다. 관계없는 걸 채워 넣으면 오히려 나빠진다. 열 폭을 유동으로 바꿔 남는 폭을 흡수하는 방법도 있다 |
| C 불일치 | 이미 잘 된 쪽으로 통일 |
| D 발견 불가 | 배치 순서를 당기거나, 열을 나눠 항상 보이게. 방어 문구로 때우지 않는다 |
| D 색 충돌/대비 부족 | 이미 의미가 있는 색(선택색·위험색 등)은 피하고, 새 요소만의 색을 고르되 배경·글씨 대비가 충분한지 확인한다 |
| E 막힘 | **비활성 이유를 버튼 옆에 같이 표시.** 결과물(다운로드·복사)과 현재 상태는 **하단 고정 바**에 둬서 항상 손 닿게(사이드 패널보다 낫다) |
| E 결과가 옛날 값 | **"확정하고 다음 →" 관문 대신 변경 감지** — 입력이 바뀌면 화면이 스스로 "결과가 옛날 값"이라고 알리게. 사용자에게 순서를 강제하지 않는다 |
| F 실수 위험 | **위험한 동작은 선택했을 때만 나타나게.** 상시 노출하지 않는다 |
| G hover 의존 | `@media (hover: none)`으로 터치 기기 예외 처리 |

---

## 5단계 · 정리는 덜어내는 게 아니다

지금 잘 쓰이고 있는 건 깔끔함을 이유로 깎지 않는다 — **한눈에 보이던 정보량**, **적은 클릭으로
되던 동작** 같은 것. 줄일 건 *시선을 똑같이 나눠 먹는 요소*지 실제로 쓰는 기능이 아니다.
뭘 줄이려면 **그게 지금 방해가 되고 있는지 먼저 확인**한다.

사용자가 이미 명시적으로 정한 것(폭 고정, 특정 스크롤 방식, 항목 개수 등)은 "더 깔끔해서"를
이유로 되돌리지 않는다. 바꾸는 게 낫다고 판단되면 먼저 물어본다.

---

## 6단계 · 완료 조건

- **브라우저로 직접 눌러보고** 확인한다. 코드 리뷰만으로 "됐다"고 하지 않는다.
  Playwright로 스크립트 불가능한 부분(네이티브 파일창 등)은 그 한계를 명확히 밝힌다.
- **기존 토큰(pill·배지 등)에 새 요소를 맞추는 작업이면 스크린샷만 믿지 않는다.**
  글자 크기 몇 px 차이는 스크린샷으로 잘 안 보인다 — `getComputedStyle`로 폰트 크기·색·패딩을
  직접 재서 기준 요소와 같은 값인지 확인한다. "붙여줘/맞춰줘" 요청은 리뉴얼에서 자주 나오는데,
  스타일을 재사용하다 값 하나가 슬쩍 어긋나도 화면으로는 못 잡는 경우가 실제로 있었다.
- **실제 쓰는 화면 폭 여러 개에서** 가로 넘침이 0인지 확인한다(예: 1920 / 1440 / 1366 / 폰).
  *한 폭에서만 보면 못 잡는 버그가 있다. 세로 배치에서 줄바꿈 설정이 잘못돼 카드가 옆 칸으로
  넘어가며 화면이 가로로 늘어나는 식의 문제는 여러 폭을 돌려야만 드러난다.*
- **세로로 쌓이기 시작하는 기준점 바로 위아래도 같이 잰다**(기준점 −50px / +50px 정도).
  *가로 스크롤은 대개 여기서만 생긴다. 안쪽에 더는 줄어들 수 없는 요소가 있으면, 아직
  가로 배치인 상태로 폭이 모자라 밀려나기 때문이다. 자주 쓰는 폭(1920·1440·1366·폰)만
  재면 이 좁은 띠는 어디에도 안 걸린다. 넘침이 나면 기준점을 그 요소가 버티는 폭까지
  올려서 그보다 좁으면 쌓이게 한다.*
- **예전에 저장된 데이터로도 열어본다** — 새 필드가 없던 구버전 저장본이 그대로 로드되는지.
- **라이브(배포된 URL)에서 보이는지** 확인한다.
- **화면이 크게 바뀌면 접속만으로 알 수 있게 안내한다.** 안내는 이렇게 만든다:
  - **만료일을 박는다** — 기간이 지나면 저절로 안 뜨게. 안 그러면 나중에 지우러 다시 들어가야 한다.
  - **"봤음"이 아니라 '숨김 만료 시각'을 저장한다** — 그러면 "하루 그만보기 / 3일 그만보기" 같은
    스누즈가 값 하나로 해결된다.
  - **버튼을 눌러야만 저장한다** — 그냥 닫으면(ESC) 다음 접속 때 다시 보이게.
- **스코프 밖이라 안 고친 것을 완료 보고에 명시한다** — 고칠 수는 있지만 요청 범위를 넘어서
  손대지 않은 부분을 적어둬야 사용자가 할지 말지 판단할 수 있다.

---

## 진행 방식

- **시키지 않은 문서·기능은 만들지 않는다.** 필요하면 먼저 물어본다.
- **갈리는 선택은 시작 전에 비교해서 제시한다.** 추천안을 먼저 쓰고 이유를 붙인다.
- **보고서를 쓰지 않는다.** 결과만 짧게.

---

## 한 줄 요약

> **UI 문제는 증상으로 신고되지만 원인은 따로 있다 — 배치가 작업 순서와 어긋났는지, 시선을
> 나눠 먹는 요소가 많은지, 같은 동작의 규칙이 화면마다 다른지, 다음에 뭘 할지 알 수 있는지.
> 그래서 먼저 숫자로 재서 원인을 특정하고, 지우는 대신 접고 숨기고 붙여서 고친다.**
