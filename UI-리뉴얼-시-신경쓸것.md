# UI 리뉴얼 시 신경 쓸 것

> "화면이 난잡하다"를 고칠 때 쓰는 순서와 기준. 이 레포의 모든 프로젝트에 공통으로
> 적용되는 범용 지침이며, 특정 프로젝트 이야기는 넣지 않는다.
> 작업 방식 전반은 `handoff-behavior-guide.md`, 새 프로젝트 구조·배포는 `CLAUDE.md` 참고.
>
> **핵심 전제**: 난잡함은 "요소가 많아서"가 아니라 **시선과 손이 왔다 갔다 하기 때문**에
> 생긴다. 그래서 이 문서는 "무엇을 지울까"가 아니라 "무엇이 어긋나 있나"를 먼저 재게 한다.

---

## 1단계 · 진단 먼저 (고치지 말고 재기만)

실제로 쓰는 분량의 더미 데이터를 넣고 브라우저로 렌더해서 **숫자로** 잰다. 감으로 고치면
멀쩡한 걸 깎고 정작 원인은 그대로 남는다. 아래 6가지를 잰다.

1. **설명문 비중** — 읽기만 하는 안내 텍스트가 차지하는 면적 대비 조작 가능한 요소의 면적.
   가장 큰 설명 블록 하나가 전체 설명 면적의 몇 %인지도 같이 뽑는다(보통 이 하나가 범인이다).

2. **항목마다 상시로 떠 있는 버튼 개수** — 목록·카드가 n개일 때 총 몇 개가 늘 보이는지.
   화면 전체 버튼 중 몇 %인지까지 계산한다.

3. **단계 간 이동거리** — 번호가 매겨진 단계(1·2·3…)를 순서대로 훑으면서 다음 단계까지의
   가로·세로 이동 픽셀을 전부 기록하고, 가로로 크게(200px 이상) 튀는 지점을 센다.
   *"왕복이 있다"만 알면 못 고친다. 어느 단계 사이에서 몇 px 튀는지 알아야 원인이 특정된다.*
   흔한 원인: **번호 매긴 순서와 화면 배치 순서가 다름** (예: "버튼을 누르면 3번이 채워집니다"
   라고 안내하는데 3번이 화면 반대편 열에 있는 경우).

4. **빈 공간** — 영역(열·섹션)별로 `실제 쓰는 높이` vs `전체 높이`를 재서 빈 면적을 구한다.
   *설명문·버튼 개수만 재면 이건 절대 안 잡힌다. 그런데 화면에서 가장 큰 낭비가 여기일 때가
   많다 — 한쪽 열은 꽉 찼는데 다른 열은 절반이 비어 있는 식.*

5. **같은 화면 안에서 규칙이 두 개인 곳** — 같은 성격의 동작(삭제, 편집, 열기 등)이 영역마다
   다르게 동작하는 지점을 찾는다. 난잡함의 큰 원인이 "일관성 없음"이다.
   *이때 새로 설계하지 말고 **이미 잘 된 쪽을 기준으로 통일**한다 — 이미 검증된 패턴이 화면
   안에 있다는 뜻이므로 가장 싸고 안전하다.*

6. **방어 문구 자리** — "여기는 ○○하는 화면이 아닙니다", "위 작업과는 영향이 없습니다"처럼
   오해를 막으려고 넣은 문구의 위치. 이런 문구가 필요하다는 건 배치가 그 사실을 못 보여주고
   있다는 신호라, 문구를 다듬기보다 배치로 해결되는지 먼저 본다.

---

## 2단계 · 버그는 따로, 먼저

**화면 라벨이 실제 결과와 다르다** 같은 불일치는 배치 변경과 섞지 말고 먼저 고쳐서 배포한다.
섞으면 나중에 문제가 생겼을 때 배치 때문인지 버그 수정 때문인지 못 가린다.

---

## 3단계 · 배치는 시안 먼저

기존 코드를 건드리지 말고 **클릭되는 목업**을 별도 HTML로 만들어 보여주고, 확인받은 뒤 적용한다.
목업은 색·폰트·토큰을 실제 도구와 같게 맞춘다 — 그래야 "실물에서도 이럴까"를 판단할 수 있다.

### 배치 방향

- **번호 붙은 단계는 한 방향으로만 흐르게** — 좌우로 왕복이 생기면 배치가 순서를 안 따른다는
  신호다. 열의 역할을 나누면(입력 / 결과 / 출력) 대부분 저절로 풀린다.
- **입력과 그 결과가 동시에 보이게** — 결과 영역을 스크롤 고정(sticky)하는 것만으로 왕복이
  사라진다. "결과 보러 올라갔다 내려오기"는 클릭 수로는 안 잡히지만 실제로 가장 피곤한 동작이다.
- **설명은 지우지 말고 접히는 패널로. 기본은 펼침** — 단, **기본 상태와 "접은 걸 기억할지"를
  같이 정한다.** 기본 펼침만 정하면 면적이 그대로라 정리 효과가 없다. 처음 한 번만 읽으면 되는
  설명은 접은 상태를 기억시켜야 실제로 줄어든다(첫 사용자는 다 보이고, 아는 사람은 계속 접힌 채).
- **삭제 같은 위험한 동작은 선택했을 때만 나타나게.**
- **항목별 버튼은 마우스 올릴 때만** — 단 터치 기기는 hover가 없으므로 예외로 항상 보이게 한다.
  나타날 자리를 미리 잡아둬서(높이 고정) 카드가 흔들리지 않게 한다.
- **"확정하고 다음 →" 관문 대신 변경 감지** — 입력이 바뀌면 화면이 스스로 "결과가 옛날 값"이라고
  알리게 한다. 사용자에게 순서를 강제하지 않는다.
- **사이드 패널보다 하단 고정 바** — 결과물(다운로드·복사)과 현재 상태는 항상 손 닿는 곳에.

---

## 4단계 · 정리는 덜어내는 게 아니다

지금 잘 쓰이고 있는 건 깔끔함을 이유로 깎지 않는다 — **한눈에 보이던 정보량**, **적은 클릭으로
되던 동작** 같은 것. 줄일 건 *시선을 똑같이 나눠 먹는 요소*지 실제로 쓰는 기능이 아니다.
뭘 줄이려면 **그게 지금 방해가 되고 있는지 먼저 확인**한다.

---

## 5단계 · 완료 조건

- **브라우저로 직접 눌러보고** 확인한다. 코드 리뷰만으로 "됐다"고 하지 않는다.
- **실제 쓰는 화면 폭 여러 개에서** 가로 넘침이 0인지 확인한다(예: 1920 / 1440 / 1366 / 폰).
  *한 폭에서만 보면 못 잡는 버그가 있다. 세로 배치에서 줄바꿈 설정이 잘못돼 카드가 옆 칸으로
  넘어가며 화면이 가로로 늘어나는 식의 문제는 여러 폭을 돌려야만 드러난다.*
- **예전에 저장된 데이터로도 열어본다** — 새 필드가 없던 구버전 저장본이 그대로 로드되는지.
- **라이브(배포된 URL)에서 보이는지** 확인한다.
- **화면이 크게 바뀌면 접속만으로 알 수 있게 안내한다.** 안내는 이렇게 만든다:
  - **만료일을 박는다** — 기간이 지나면 저절로 안 뜨게. 안 그러면 나중에 지우러 다시 들어가야 한다.
  - **"봤음"이 아니라 '숨김 만료 시각'을 저장한다** — 그러면 "하루 그만보기 / 3일 그만보기" 같은
    스누즈가 값 하나로 해결된다.
  - **버튼을 눌러야만 저장한다** — 그냥 닫으면(ESC) 다음 접속 때 다시 보이게.
- **스코프 밖이라 안 고친 것을 완료 보고에 명시한다** — 고칠 수는 있지만 요청 범위를 넘어서
  손대지 않은 부분을 적어둬야 사용자가 할지 말지 판단할 수 있다.

---

## 진행 방식

- **시키지 않은 문서·기능은 만들지 않는다.** 필요하면 먼저 물어본다.
- **갈리는 선택은 시작 전에 비교해서 제시한다.** 추천안을 먼저 쓰고 이유를 붙인다.
- **보고서를 쓰지 않는다.** 결과만 짧게.

---

## 한 줄 요약

> **난잡함의 원인은 "많음"이 아니라 "어긋남"이다 — 번호와 배치가 어긋나고, 입력과 결과가
> 떨어져 있고, 같은 동작의 규칙이 화면마다 다른 것. 그래서 먼저 숫자로 재고(이동거리·빈
> 공간·규칙 불일치), 지우는 대신 접고 숨기고 붙여서 어긋남을 없앤다.**
