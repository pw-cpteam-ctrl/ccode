<details open>
<summary>2026-07-31 (오늘)</summary>

## 2026-07-31

### 실행취소(Ctrl+Z) 추가

- **무슨 문제/요청이 있었나**: 실수로 사진을 지우거나 순서를 바꿨을 때 되돌릴 방법이 없었음. 실행취소 스니펫 공유 후 v2에 반영 요청
- **어떻게 처리했나**: `undoStack` 배열 + `pushUndo()` / `undo()` 함수 추가. 보드 추가·삭제·복제, 파일 추가, 셀 삭제·교체, 캔버스 전환, 탭 드래그 순서변경 등 데이터를 바꾸는 모든 지점 직전에 `pushUndo()` 호출. Ctrl+Z (Mac: Cmd+Z) 키보드 단축키 등록 — 텍스트 입력칸 포커스 중일 때는 동작 안 함. 최대 30단계
- **결과**: Playwright로 이벤트 등록 확인

---

### 휠 줌 + 줌바 드래그 이동

- **무슨 문제/요청이 있었나**: 미리보기 배율을 슬라이더로만 바꿀 수 있었음. 스니펫 3종(휠 줌 / 툴바 그랩 이동 / 보드 탭 순서변경) 공유 후 반영 요청
- **어떻게 처리했나**:
  - 휠 줌: `#stage`에 `wheel` 이벤트 리스너 추가 — 줌바(`.zoom-bar`) 위에서는 무시, 그 외 스테이지 어디서든 휠로 확대/축소. 처음에 `.sc-wrap` 위도 제외하도록 짰다가 "빈 셀도 sc-wrap 안에 있어서 사실상 배경이 없음" 확인 후 `.zoom-bar`만 제외로 수정
  - 줌바 드래그: `.zoom-bar`를 `position:sticky → position:fixed`로 전환, `⠿` 핸들 추가. 핸들 `mousedown` → `window mousemove/mouseup`으로 이동 처리, 화면 밖 이탈 clamp 포함. 위치는 `localStorage('mhv2_toolbar_pos')`에 저장해 새로고침 후에도 유지
- **결과**: Playwright로 휠 후 `#previewZoomVal` 텍스트 50%→60% 확인, 줌바 드래그 후 X좌표 120px 이상 이동·localStorage 저장 확인

---

### 보드 탭 드래그 순서변경 재구현

- **무슨 문제/요청이 있었나**: 보드 탭 드래그 순서변경이 실제로 동작하지 않았음. 스니펫 3번이 새 기능 요청이었는데 "이미 구현됨"으로 임의 판단해 넘어간 것도 잘못
- **확인해보니**: 기존 코드가 `dragstart`에서 `e.dataTransfer.setData()`를 호출하지 않아 `drop` 이벤트가 제대로 발화하지 않는 구조였음 (HTML5 DnD는 `setData` 없으면 drop이 안 fires). Playwright 이벤트 로그로 직접 확인
- **어떻게 처리했나**: `setData('text/plain', String(인덱스))` → drop에서 `getData`로 읽는 방식으로 교체. `reorderBoards(from, to)` 함수 분리 — 현재 선택 보드(`curBoardId`)를 참조로 유지한 채 `splice` 후 `curBoardId` 재계산
- **결과**: Playwright에서 boards 배열 canvasIdx 기준으로 [0,1,2] → [1,2,0] 순서변경 확인

---

</details>

<details>
<summary>2026-07-30</summary>

## 2026-07-30

### 기능 6종 추가 (보드 복제·탭 드래그·빈칸 파일드래그·교체버튼·보드클릭전환·다운로드분리)

- **무슨 문제/요청이 있었나**: 아래 6가지 기능을 순서대로 추가해달라는 요청
  1. 보드 탭을 드래그해서 순서 바꾸기
  2. 보드 복제
  3. 빈 칸(회색 칸)에 파일을 드래그해서 사진 즉시 추가
  4. 이미지 교체 버튼
  5. 미리보기 캔버스 클릭 시 해당 보드로 전환
  6. 현재 보드만 저장 / 전체 보드 저장 버튼 분리
- **어떻게 처리했나**:
  - 보드 탭 드래그 순서변경: 보드 탭 목록을 그리는 함수 (`renderBoardTabs`)에 HTML5 네이티브 드래그 이벤트 추가 — 출발 탭과 도착 탭의 인덱스를 구해서 배열 안에서 위치를 바꿈 (`boards.splice(di, 0, boards.splice(si, 1)[0])`)
  - 보드 복제: 선택한 보드의 데이터를 그대로 복사해 새 보드를 바로 뒤에 끼워 넣는 함수 (`duplicateBoard`) 추가, 보드 헤더에 ⧉ 버튼 추가
  - 빈 칸 파일 드래그: 셀의 드롭 이벤트 핸들러에 "드래그된 데이터가 파일이면 그 슬롯에 직접 이미지 넣기" 처리 추가 (`e.dataTransfer.files` 감지 → `FileReader`로 읽어 해당 슬롯 교체). 드래그 중 파란 점선으로 강조되는 시각 피드백 포함 (`file-drag-over` 클래스)
  - 이미지 교체 버튼: 각 셀 우측에 ↺ 버튼 (`cell-replace-btn`) 추가 — 클릭 시 숨겨진 파일 입력창을 띄워서 사진 교체. 사진 없는 칸에도 동일하게 "추가" 용도로 표시됨. 캔버스 축소 배율에 역보정을 적용해서 항상 실제 화면에서 44px 크기로 보이게 처리 (`applyPreviewZoom`에 `.cell-replace-btn` 역보정 추가)
  - 보드 클릭 전환: 미리보기 캔버스 래퍼(`sc-wrap`) 클릭 시 `switchBoard`를 호출하도록 연결 — 단, 드래그 핸들·삭제·교체 버튼·제품명 텍스트 클릭 시에는 전환 안 되게 제외 처리
  - 다운로드 분리: 기존 "PNG 다운로드"를 현재 선택 보드만 저장하는 "이 보드 저장"으로 변경, 전체 보드를 순서대로 저장하는 "전체 저장" 버튼 별도 추가 (`downloadAllPNG` 함수 신규)
- **결과**: Playwright로 각 항목 확인 — 복제 버튼 클릭 시 보드 2개로 증가, sc-wrap 클릭 시 해당 탭이 `on` 상태로 바뀜, 교체 버튼 12개(셀 수만큼) 정상 생성, "이 보드 저장"/"전체 저장" 버튼 텍스트 확인

---



### 허브 페이지에 v2 링크가 없던 문제

- **무슨 문제/요청이 있었나**: 메가하우스 v2 도구를 배포했는데 메인 허브 페이지(`index.html`)에 카드가 없어서 URL을 직접 알아야만 접근 가능한 상태였음
- **어떻게 처리했나**: 허브 페이지에 메가하우스 v2 링크 카드 추가 (`<a class="card" href="megahouse-v2.html">`)
- **결과**: 허브에서 바로 진입 가능해짐

---

### 사진 여러 장 추가해도 보드가 안 늘어나는 문제

- **무슨 문제/요청이 있었나**: 사진을 여러 장 한꺼번에 올려도 현재 보드 슬롯이 꽉 차면 나머지 사진이 그냥 무시되고 있었음. 보드를 자동으로 추가해달라는 요청
- **어떻게 처리했나**: 사진을 받아서 슬롯에 배정하는 함수 (`addFiles`)를 수정 — 현재 보드가 꽉 차면 자동으로 새 보드를 만들고 이어서 채우도록 구현. 최대 6보드까지 자동 확장되고, 그 이상이면 안내 메시지 표시
- **결과**: 사진 6장을 한 번에 올리면 2보드(3장×2)로 자동 분배됨. Playwright로 확인

---

### 보드 2개 이상일 때 미리보기에서 보이지 않는 문제

- **무슨 문제/요청이 있었나**: 보드를 2개 이상 추가하면 사이드바 탭에는 "보드1 / 보드2"가 뜨는데, 실제 미리보기 캔버스에는 보드1만 보이고 보드2는 아예 안 보임
- **확인해보니**: CSS 화면 크기 조절 방식(`transform: scale()`)은 시각적으로만 줄여줄 뿐 레이아웃상 실제 공간은 원본 크기(1080px)를 그대로 유지함. 그래서 보드들이 flex 행에서 각각 1080px씩 차지하다 보니 화면 밖으로 밀려나 안 보였던 것 (`applyPreviewZoom`이 보드 크기를 스테이지 높이 기준으로 계산하고, 래퍼 div 크기를 아예 설정하지 않았던 문제)
- **어떻게 처리했나**: 스케일된 크기만큼만 레이아웃 공간을 잡는 래퍼 div(`sc-wrap`)를 추가하고, 그 안에 실제 캔버스를 절대 위치로 배치. 크기 계산은 스테이지 높이 대신 너비 기준으로 바꾸고, 보드 수와 간격(32px)을 고려해 슬롯 폭을 구하도록 수정 (`applyPreviewZoom`에서 `slotW = (stageW - gap * (n-1)) / n` 방식으로 변경)
- **결과**: 보드 2개가 나란히 정상 표시됨. Playwright로 x 좌표 확인 (보드1: 408px, 보드2: 906px)

---

### 셀 드래그로 다른 보드 위치 교환이 안 되는 문제

- **무슨 문제/요청이 있었나**: 셀을 드래그해서 같은 보드 안에서는 순서 교환이 되는데, 다른 보드로 옮기면 아무 일도 안 일어남
- **확인해보니**: 셀에 뭔가를 떨어뜨렸을 때 처리하는 코드(`drop` 이벤트 핸들러)에 "출발 보드와 도착 보드가 같을 때만 처리"하는 조건(`srcBoardId === destBoardId`)이 있어서 보드 간 이동이 원천 차단되어 있었음
- **어떻게 처리했나**: 해당 조건을 제거해 "같은 자리가 아니면" 무조건 swap 처리하도록 수정
- **결과**: 보드1 셀 → 보드2 셀 드래그 교환 정상 작동. Playwright로 아이템 위치 교환 확인

---

### 드래그 핸들이 너무 작게 보이는 문제 + ✕ 삭제 버튼 없는 문제

- **무슨 문제/요청이 있었나**: 드래그 핸들(⠿)이 캔버스가 커서 상대적으로 너무 작게 보이고, 셀에서 사진을 지우는 버튼도 없음
- **확인해보니**: 핸들이 캔버스 내부에 있어서 미리보기 축소 배율(`scale`)에 같이 줄어들고 있었음. 예를 들어 핸들이 44px이어도 캔버스를 40% 크기로 보여주면 실제론 17px 수준으로 표시됨. 삭제 버튼은 CSS 클래스만 정의됐고 버튼 자체가 아예 없었음
- **어떻게 처리했나**:
  - 드래그 핸들과 삭제 버튼에 축소 배율의 역수만큼 반대로 키우는 처리(`scale(1/scale)`) 추가 — 캔버스가 얼마나 줄어들든 항상 화면에서 44×44px로 보이게 됨 (`applyPreviewZoom`에서 `poster.querySelectorAll('.cell-drag-handle, .cell-del-btn')`에 역보정 적용)
  - 셀 우상단에 빨간 ✕ 버튼 실제 구현 — 클릭 시 해당 슬롯 사진 삭제 후 화면 갱신
  - 핸들 위치를 오른쪽 위 → 왼쪽 위로 변경해 삭제 버튼(오른쪽 위)과 겹치지 않게 배치
- **결과**: Playwright로 핸들·삭제 버튼 시각적 크기 44×44px 확인. 셀에 마우스 올리면 두 버튼 나타남

</details>

<details>
<summary>이전 기록 (2026-07-29 이전)</summary>

## 2026-07-29

</details>
