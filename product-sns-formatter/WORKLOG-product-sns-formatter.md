# WORKLOG — 상품 SNS 포맷터 (product-sns-formatter)

> 작성 규칙은 레포 루트의 `WORKLOG-작성-규칙.md` 참고. 하루 단위로, 그날 있었던 요청과
> 처리 과정을 비개발자도 읽고 이해할 수 있게 풀어서 적는 문서다.

---

<details open>
<summary>2026-07-29 (오늘)</summary>

## 2026-07-29

### 여러 상품 원고를 한 번에 만드는 "일괄 원고 생성" 기능 추가
- **무슨 문제/요청이 있었나**: B2B로 상품을 한꺼번에 가져온 뒤에도 원고는 상품마다 하나씩
  버튼을 눌러야 해서, 상품이 많은 날(수십 건)은 그것만으로도 오래 걸린다는 요청. 시작 전에
  버튼을 어디에 둘지, 어떤 상품을 대상으로 할지, 오래 걸리면 중간에 다른 작업을 병행해도
  되는지까지 먼저 상의하고 진행함.
- **확인해보니**: 상품을 순서대로 하나씩 처리하면(순차 처리) 상품 수만큼 그대로 대기 시간이
  늘어나서, 상품이 많을수록 체감 대기시간이 너무 길어짐.
- **어떻게 처리했나**: 좌측 목록 상단(B2B 가져오기 버튼 바로 아래)에 "일괄 원고 생성" 버튼을
  새로 추가함. 원고가 아직 없는 상품만 대상으로 하고, 한 번에 5개씩 동시에 처리(동시 처리
  개수 제한)해서 대기 시간을 줄임. 하나가 실패해도 나머지는 계속 진행하고 끝나면 성공/실패
  건수를 알려줌. 처리 중에도 다른 상품 화면으로 넘어가거나 사진 작업을 병행할 수 있게, 진행
  상태를 특정 상품 화면이 아니라 버튼 옆 공용 영역에 표시함 (`runBatchGenerate`,
  `generateDraftCore`로 단일/일괄 생성 로직 공용화).
- **결과**: 가짜 API 응답으로 상품 12개(정상 10개/정보 없는 1개/강제 실패 1개)를 만들어
  실제로 돌려봐서, 대상 필터링·동시 5개 처리·배치 도중 개별 생성 버튼 잠금·성공 10건/실패
  1건 요약까지 전부 의도대로 동작하는 것을 확인함.

### B2B 스크래퍼가 완전히 실패했을 때 뜨는 안내문이 팀원이 못 알아보는 영어 에러 그대로였던 문제
- **무슨 문제/요청이 있었나**: 팀원은 코딩 지식이 거의 없는데, 스크래퍼가 통째로 실패하면
  화면에 Playwright 내부 에러 문구가 그대로 뜨는 상태였음. 매일 쓸 도구인 만큼, 오류가 나도
  팀원 스스로 어느 정도는 감을 잡을 수 있게 해달라는 요청.
- **확인해보니**: 인터넷 끊김, 로그인 시간초과(5분), 크롬 미설치, 이미 실행 중인 창 있음,
  파일 저장 권한 문제, 디스크 용량 부족처럼 흔히 겪을 만한 원인 몇 가지는 미리 구분해서
  안내할 수 있었음.
- **어떻게 처리했나**: 흔한 실패 패턴 몇 가지는 사람 말로 먼저 안내하고, 여기 안 걸리는
  나머지 전부는 "원인 특정 불가 — 화면 캡처해서 담당자에게 보내주세요"로 통일함
  (`friendly-error.js` 신설). 어느 경우든 원본 에러 메시지도 같이 찍히게 해서, 캡처만
  받아도 진단할 수 있게 함.
- **결과**: 인터넷 끊김/로그인 타임아웃/크롬 미설치/브라우저 강제종료/파일 권한/디스크
  용량/전혀 모르는 에러까지 8가지 케이스를 직접 넣어 실행해보고 각각 의도한 문구로 정확히
  매칭되는 것을 확인함.

### B2B 가져오기 과정에서 팀원이 매일 겪을 수고를 줄이는 방법 상의 후 적용
- **무슨 문제/요청이 있었나**: 스크랩이 끝난 뒤 그 결과 폴더를 웹페이지에서 다시 찾아 들어가
  선택하는 과정이 매일 반복되는데, 이걸 조금이라도 줄일 방법이 있는지 물어봄. 시작 전에
  클립보드 복사/탐색기 자동 열기/브라우저 자동 실행/드래그앤드롭 네 가지 후보를 놓고 뭘
  넣을지 상의함.
- **확인해보니**: 웹페이지 버튼이 팀원 PC의 프로그램을 직접 실행하는 것(예: 스크래퍼를
  웹에서 바로 실행)은 브라우저 보안 구조상 원천적으로 불가능하다는 것부터 짚고 넘어감.
  대신 로컬 스크립트(`scrape.js`)가 결과를 다 만든 "직후"에 할 수 있는 일은 여러 가지
  있었음.
- **어떻게 처리했나**: 스크랩 완료 시 결과 폴더 경로를 자동으로 클립보드에 복사하고, 그
  폴더를 탐색기 창으로 자동으로 띄워주는 기능을 추가함 (`os-helpers.js`의
  `copyToClipboard`/`openInExplorer`, 둘 다 실패해도 스크랩 자체엔 영향 없게 처리). 웹페이지의
  "B2B 가져오기" 영역에는 드래그앤드롭을 새로 지원해서, 방금 자동으로 뜬 탐색기 창에서 그
  폴더를 그대로 끌어다 놓으면 폴더선택창 없이 바로 가져와지게 함(재귀적으로 하위 폴더까지
  훑어서 기존 가져오기 로직에 그대로 전달). 브라우저 자동 실행은 이번엔 넣지 않기로 함.
- **결과**: 가짜 폴더 구조(output.json + photos 하위폴더)를 흉내 낸 드래그 이벤트로 실제
  재귀 탐색과 가져오기까지 이어지는 것을 확인함.

### 원고 생성이 규칙 위반으로 막혔을 때 "로그에 남았다"고 안내해놓고 실제로는 저장이 안 되던 문제
- **무슨 문제였나**: AI가 입력에 없던 일본어를 만들어내서 원고 생성이 막혔는데, 안내문에는
  "이상감지 로그에 남았다"고 나왔지만 실제로 로그 화면을 새로고침해보면 0건으로 나옴.
- **확인해보니**: 로그 저장 함수가 깃허브에 커밋하는 데 실패해도 서버 콘솔에만 에러를
  출력하고 조용히 넘어가도록 되어 있었음 (`logAnomaly`). 그리고 그 위에서 안내문을 만드는
  코드는 저장 성공/실패 여부를 아예 확인하지도 않고 무조건 "로그에 남았다"고 찍고 있었음.
- **어떻게 처리했나**: 로그 저장 함수가 성공/실패 여부를 실제로 돌려주게 고치고, 안내문도
  그 결과에 따라 "로그에 남았다" 또는 "로그 저장은 실패했다 — 캡처해서 담당자에게
  보내달라"로 갈리게 고침.
- **결과**: 코드상으로 성공/실패 분기가 정확히 갈리는 것을 확인함. 다만 애초에 왜 저장이
  실패했는지(권한 문제 등 구체적 원인)는 Vercel 서버 로그를 직접 봐야 확정되는 부분이라
  아직 확인 못 함 — 다음에 확인 필요.

### 원고 제목(【】) 안 영문 고유명사가 한글로 안 바뀌던 문제 (규칙끼리 충돌) + 유명 시리즈명 오표기
- **무슨 문제였나**: "Komissa Taro & Komissa Hanako Scale Figure"라는 입력이 제목에서
  "Komissa Taro & Komissa Hanako 스케일 피규어"로, 상품 종류(Scale Figure)만 한글로 바뀌고
  이름은 영문 그대로 남음. 별개로 "Umamusume: Pretty Derby" 시리즈명도 정확한 표기(우마무스메)
  에서 "스" 음절이 빠진 "우마무메"로 나옴.
- **확인해보니**: 굿스마일 브랜드 규칙에 "제목에 이미 내용이 있으면 단어를 더 붙이거나
  바꾸지 마라(이모지·기호만 보존)"는 별도 규칙이 있었는데, 이 문구가 "제목 안 이름도
  건드리지 마라"는 뜻으로 AI에게 확대해석될 여지가 있었음 — 원래 목적은 "굿스마일 같은
  단어를 중복해서 붙이지 마라"는 것뿐이었음. Umamusume 쪽은 사전(`proper-nouns.js`)에 이
  시리즈명이 아예 없어서, AI가 직접 발음을 추측하다가 생긴 오표기였음.
- **어떻게 처리했나**: 제목 규칙에 "이건 단어 추가/변경 금지이지 고유명사를 영문 그대로
  둬도 된다는 뜻은 아니다, 고유명사 한글표기 규칙은 제목 안에서도 그대로 적용된다"는 예외
  조항을 명시적으로 추가함. 고유명사 규칙 자체에도 "낯설거나 확신 안 서는 이름에도 예외
  없이 적용된다"는 예시를 보강함. 사전에 "umamusume → 우마무스메" 항목을 추가함.
- **결과**: 이건 프롬프트 문구/사전만 고친 거라, 실제로 다시 원고를 생성해봐야 확실히
  확인되는 부분 — 아직 실사용 재확인은 못 함.

### 캐러셀 이미지 미리보기 확대 + 부제(작은 글자) 내용을 "작품명"에서 "제품 라인"으로 변경 + 글자크기 조절
- **무슨 문제/요청이 있었나**: 캐러셀 편집 미리보기(주황틀 프리뷰)가 작아서 답답하다는
  요청으로 2배 확대를 요청. 그리고 부제(작은 글자)에 지금은 원고에 있는 작품명(예:
  "우마무스메: 프리티 더비")이 들어가는데, 실제로는 "넨도로이드/피그마/아크릴 스탠드"처럼
  제품 라인이 들어가야 한다는 요청. 처음엔 그림자 크기 슬라이더를 넣어달라고 했다가, 다시
  확인해보니 그림자는 기본값으로 고정하고 대신 글자크기 슬라이더를 원한 것으로 정정됨.
- **확인해보니**: 부제가 지금 나오는 방식은 원고 텍스트의 『작품명』을 그대로 뽑아 쓰는
  구조였는데, 이 『작품명』은 실제로 게시되는 원고 본문 그 자체라서 이 소스를 제품 라인으로
  바꿔치기하면 실제 게시글에서도 작품명이 사라지는 부작용이 생김.
- **어떻게 처리했나**: 미리보기(`#composerCanvas`)의 화면 표시 크기를 210×262.5 → 420×525로
  2배 키우고, 그만큼 우측 편집 영역은 grid 구조상 자동으로 줄어들게 함(저장용 실제 해상도
  1080×1350은 그대로 유지). 부제는 원고 텍스트가 아니라 상품정보(officialText) 안에서 제품
  라인 이름(넨도로이드/피그마/봉제인형 등, 목록에 없으면 스케일 표기가 있으면 그대로,
  없으면 "스케일 피규어")을 직접 매칭해서 채우도록 소스를 분리함 (`rules/product-lines.js`
  신설). B2B 가져오기 직후·상품정보 직접 입력 시·원고 생성/검수 시 전부 자동으로 다시
  채워지게 함. 제목/부제 글자크기를 각각 조절하는 슬라이더 2개를 추가함(전체 상품 공통
  적용, 그림자는 조절 옵션 없이 기존 고정값 유지).
- **결과**: "Hatsune Miku AMP+ Figure"→"AMP+ 피규어", "Chocopuni Plushie"→"쵸코푸니 봉제인형"
  처럼 실제 예시로 매칭이 맞는지 확인함. 원고를 생성해도 부제가 계속 제품 라인으로
  유지되고, 실제 원고 본문의 작품명은 그대로 남아있는 것도 확인함. 글자크기 슬라이더도
  캔버스에 실제로 반영되는 값까지 확인함.

### B2B 가져오기 안내문구를 쉬운 말로 수정 + 버튼 사이 어색한 간격 + 상품 초기화 버튼 추가
- **무슨 문제/요청이 있었나**: "output/<날짜> 폴더를 통째로 선택"이라는 안내문이 비개발자
  팀원 입장에서 이해하기 어렵다는 지적. "일괄 원고 생성"과 "상품 추가" 버튼 사이 간격이
  이상해 보인다는 지적. 그리고 오늘 담아둔 상품을 통째로 지우는 "상품 초기화" 버튼을 새로
  요청 — 레이아웃은 좌측에 두 버튼(일괄 원고 생성/상품 추가)이 위아래로 붙고, 우측에
  초기화 버튼이 그 둘 높이만큼 세로로 길게 자리잡는 모양으로 여러 차례 아스키 그림을 주고
  받으며 확정함.
- **확인해보니**: 안내문의 "output/<날짜>"라는 경로 표기 자체가 비개발자에게는 낯선
  문법이었음. 버튼 사이 간격은 그 사이에 있는 빈 상태 표시줄(`#batchGenStatus`)이 평소엔
  텅 비어있어도 CSS 여백(`margin-top:8px`) 때문에 불필요한 틈을 만들고 있었음.
- **어떻게 처리했나**: 안내문을 "스크래퍼 프로그램을 실행하면 자동으로 만들어지는 오늘
  날짜 이름의 폴더"로 풀어씀. 상태 표시줄을 버튼 사이가 아니라 버튼 블록 전체 밑으로 옮김.
  확정된 레이아웃대로 CSS grid로 좌측 2단/우측 세로로 긴 버튼 구조를 만들고
  (`.action-row`/`.action-col-stack`), 상품 초기화 버튼은 눌렀을 때 오늘 담아둔 상품이
  없으면 알림만, 있으면 개수를 알려주는 확인창을 거친 뒤 상품 목록·사진(IndexedDB
  캐싱)·메모리 캐시까지 전부 지우도록 함 (`resetAllProducts`).
- **결과**: 실제로 좌측 버튼 스택과 우측 초기화 버튼의 화면 높이가 픽셀 단위로 정확히
  일치하는 것, 상품 0개/2개 각각에서 알림·확인창 문구가 의도대로 뜨는 것, 확인 후
  IndexedDB·메모리 캐시·localStorage까지 실제로 비워지는 것을 전부 직접 확인함.

### (보류) 재료가 부족한 원고에서 AI가 없는 감성적 수식어를 지어내던 문제
- **무슨 문제였나**: "TENITOL TALL shiro Swimsuit ver."처럼 입력에 "Swimsuit ver." 태그만
  있고 별다른 설명이 없는 경우, AI가 "수영복으로 깜찍하게 변신한"처럼 입력에 없는 감성적
  묘사를 지어내서 붙임 — 이미 있는 "사실에 없는 걸 지어내지 마라"는 규칙을 어긴 사례.
- **확인해보니**: 굿스마일 규칙에 "~로 등장!"으로 끝나는 문장이 반드시 있어야 한다는 강제
  조항이 있는데, 재료가 너무 없으면 AI가 그 문장을 자연스럽게 만들려고 없는 수식어로
  메꾸는 것으로 추정됨. 예시 문구를 프롬프트에 추가하는 방향으로 상의하다가, 예시가 너무
  구체적(수영복)이면 AI가 그 사례에만 쏠릴(앵커링) 위험이 있다는 우려가 나와서 "특정 상품
  종류에 국한된 규칙이 아니다"를 명시하는 절충안까지 합의함.
- **어떻게 처리했나**: 아직 코드에 반영 안 함 — 다른 요청(캐러셀 흰텍스트 규칙 확인 등)으로
  넘어가면서 보류된 상태.
- **결과**: 미해결. 다음에 이어서 반영 필요.

</details>

<details>
<summary>이전 기록 (2026-07-28 이전)</summary>

## 2026-07-28

### 캐러셀 사진 기본 위치를 "가운데" 기준에서 "위쪽" 기준으로 변경
- **무슨 문제/요청이 있었나**: 사진을 올리면 기본으로 중앙 기준 크롭이 되는데, 캐릭터
  얼굴이 사진 위쪽에 몰려있는 경우가 많아서 정수리 위쪽이 잘려버림.
- **확인해보니**: 사진을 처음 올릴 때 남는 여유 공간을 위아래로 반씩 나눠서 잘라내고
  있었음.
- **어떻게 처리했나**: 남는 여유 공간을 전부 아래쪽에서만 잘라내도록(위쪽 기준) 기본값을
  바꿈 (`addPhotoFileToProduct`의 기본 `oy` 계산).
- **결과**: 실제 사진으로 확인함 — 얼굴이 잘리지 않고 위쪽 기준으로 채워짐.

### 캐러셀 이미지에 남아있던 미세한 렌더링 흠집들 (1px 선/틈, 뱃지 위치, 그라데이션 끊김)
- **무슨 문제였나**: 확대해보면 캔버스 오른쪽 끝에 미세한 선이 보이거나, 그라데이션과 틀
  하단 사이에 1px 틈이 보이거나, 좌상단 뱃지 글자가 위아래 가운데가 아니라 아래쪽으로
  치우쳐 보이는 등 자잘한 시각적 흠이 여러 개 있었음.
- **확인해보니**: 사진을 꽉 채워 자르는 계산이 부동소수점 반올림 오차로 아주 살짝 모자라서
  오른쪽에 빈 픽셀이 남았고, 그라데이션을 칠하는 영역이 틀 안쪽 경계에서 끝나서 틀과 만나는
  지점에 미세한 틈이 생겼고, 뱃지 글자는 기준선이 아래쪽(alphabetic)으로 잡혀 있어서 글자가
  커질수록 아래로 쏠렸음.
- **어떻게 처리했나**: 사진을 자르는 배율에 아주 약간의 여유(`COVER_FIT_SLOP`)를 더하고 그
  밑에 어두운 배경을 깔아 이중 안전장치를 둠. 그라데이션 칠하는 영역을 캔버스 맨 아래까지
  늘림(색상 계산 좌표는 그대로 두고 칠하는 영역만 확장). 뱃지 글자 기준선을 가운데(middle)
  정렬로 바꿈.
- **결과**: 픽셀 단위로 이어지는 지점을 직접 스캔해서 이전엔 있던 흰 틈/선이 없어진 것을
  확인함.

### 뱃지·그라데이션 조절값을 상품별 개별 설정에서 "전체 공통" 설정으로 변경
- **무슨 문제/요청이 있었나**: 그라데이션(시작점/퍼짐/농도/압축)과 뱃지 자간을 상품마다
  따로 저장하고 있었는데, 상품이 여러 개일 때 매번 똑같이 맞추기 번거로우니 한 번
  설정하면 모든 상품에 공통으로 적용되게 해달라는 요청.
- **어떻게 처리했나**: 이 값들을 상품 데이터가 아니라 전역 설정(`GLOBAL_GRAD`) 하나로
  옮기고, 슬라이더를 움직이면 그 전역 값 하나만 바뀌어서 모든 상품 미리보기에 즉시
  반영되게 함. 브라우저 저장(localStorage)도 상품과 별도로 이 설정 하나만 따로 저장함.
- **결과**: 여러 상품을 오가며 슬라이더를 움직여봐서, 한 번 조절하면 다른 상품에도 그대로
  적용되는 것을 확인함.

### 캐러셀 제목 입력칸에서 줄바꿈(Enter)이 무시되던 문제
- **무슨 문제였나**: 제목 칸에서 Enter로 줄을 나눠 입력해도, 실제 캐러셀 이미지에는 한
  줄로 합쳐져서 그려짐.
- **확인해보니**: 긴 텍스트를 줄바꿈하는 로직이 공백 기준으로만 잘라서, 사용자가 입력한
  실제 줄바꿈을 무시하고 있었음.
- **어떻게 처리했나**: 줄바꿈 문자를 먼저 기준으로 문단을 나누고, 그 안에서만 자동
  줄바꿈이 되도록 순서를 바꿈 (`wrapCanvasText`).
- **결과**: 두 줄로 입력한 제목이 실제로 두 줄로 나뉘어 그려지는 것을 확인함.

### AI가 없는 이름을 지어내 "하츠네 미쿠"를 "초음속 미쿠"로 바꿔 쓰던 문제
- **무슨 문제였나**: 실제 원고 생성(AI 호출)에서 "Hatsune Miku"가 "초음속 미쿠"처럼 원문에
  없는 창작된 별명으로 바뀌어 나옴.
- **확인해보니**: 고유명사를 한글로 옮기라는 규칙은 있었지만 "뜻을 창작해서 별명으로
  바꾸지 마라"는 규칙이 따로 없어서, AI가 그럴듯한 별명을 지어내는 걸 막지 못했음.
- **어떻게 처리했나**: "고유명사는 오직 소리 나는 대로만 옮기고 뜻을 지어내 바꾸지 마라"는
  규칙과, 자주 나오는 고유명사 사전을 프롬프트에 같이 넣도록 함 (`api/format.js`,
  `rules/proper-nouns.js`와 연동).
- **결과**: 이후 사전에 있는 이름들은 정해진 표기 그대로 나오는 것을 확인함.

### "대표만 남기고 나머지 틀 제거" 버튼의 버그 두 가지
- **무슨 문제/요청이 있었나**: 대표 사진 빼고 나머지 틀을 제거하는 버튼이, 대표를 다른
  사진으로 바꾸면 예전 대표의 틀이 안 켜져서 계속 빈 채로 남는 문제. 그리고 원래 이 버튼은
  껐다 켰다 할 수 있는 토글이어야 하는데 한 번 끄면 다시 켤 방법이 없었음.
- **확인해보니**: 대표를 바꾸는 코드가 "누가 대표인지"만 바꾸고 각 사진의 "틀 있음/없음"
  값은 그대로 내버려두고 있었음. 그리고 버튼 자체가 껐다 켜는 로직 없이 "끄기"만 하도록
  짜여 있었음.
- **어떻게 처리했나**: 대표를 바꿀 때 이전 대표는 다시 틀이 없는 상태로, 새 대표는 틀이
  켜진 상태로 맞춰주도록 고침. 버튼도 지금 상태를 보고 "나머지 틀 제거"와 "나머지 틀 다시
  켜기" 사이를 오가는 진짜 토글로 바꿈 (`toggleFrameOnNonMain`).
- **결과**: 대표를 여러 번 바꿔가며 틀 상태가 항상 올바르게 따라가는 것, 버튼을 눌러 껐다
  켰다 반복해도 정상 동작하는 것을 확인함.

### "틀 off" 했을 때 사진 주위로 하얀 여백이 남던 문제
- **무슨 문제/요청이 있었나**: 캐러셀 틀을 꺼서 사진만 보이게 하면, 사진이 캔버스 전체를
  못 채우고 테두리에 하얀 여백이 남아 어색해 보임.
- **확인해보니**: 사진을 꽉 채워 자르는 계산이 항상 "틀 안쪽의 좁은 영역" 기준으로만 되어
  있어서, 틀이 꺼진 상태에서도 그 좁은 기준 그대로 계산되다 보니 캔버스 전체를 못 채우고
  있었음.
- **어떻게 처리했나**: 틀이 켜져 있으면 틀 안쪽 영역, 꺼져 있으면 캔버스 전체를 기준으로
  계산하도록 분기함 (`fitRectFor(slide)`).
- **결과**: 틀을 껐을 때 네 귀퉁이까지 사진이 꽉 차는 것을 확인함.

### 좌측 상품 목록에 대표 사진 썸네일 추가 + 레이아웃 개편 (여러 차례 수치 조정)
- **무슨 문제/요청이 있었나**: 좌측 상품 목록이 텍스트만 있어서 어떤 상품인지 한눈에 안
  들어옴 — 대표 사진을 작게라도 같이 보여달라는 요청. 처음엔 중앙 기준 200% 확대로
  만들었더니 얼굴이 잘려 보였고, 이후 패딩/목록 폭/글자 크기/썸네일 확대 비율/줌 기준점을
  여러 차례 실측하며 조정함.
- **확인해보니**: 실제 화면에서 텍스트 영역 폭을 재보니 113px밖에 안 돼서 제목이 억지로
  줄바꿈되고 있었음.
- **어떻게 처리했나**: 상품의 대표(★) 사진을 작게 잘라 목록에 같이 보여주도록 함. 여러
  차례에 걸쳐 좌우 패딩 축소(11→6px), 상태 표시(●/✕)를 한 줄로 합침, 목록 폭 확장
  (250→300px, 이후 요청에 따라 다른 영역은 안 줄이고 폭 자체를 50px 늘림), 영어 글자 크기
  축소(10px), 썸네일을 200% 확대하되 기준점을 중앙→상단→위쪽 25%로 조정함(줌 기준이
  중앙이면 얼굴이 잘리고, 완전 상단이면 정수리만 보이는 문제가 있어서 그 중간으로 맞춤).
- **결과**: 실측 결과 텍스트 영역 폭이 113px에서 174px로 늘어난 것, 썸네일이 잘리지 않고
  얼굴이 보이는 위치로 나오는 것을 확인함.

### 새로고침하면 좌측 목록 썸네일이 안 보이던 문제
- **무슨 문제였나**: 페이지를 새로고침하면 좌측 목록에 썸네일이 하나도 안 보이다가,
  상품을 한 번씩 클릭해야만 나타남.
- **확인해보니**: 썸네일에 쓰이는 사진은 상품을 클릭해서 열어야 메모리에 불러와지는
  구조였는데, 목록 자체는 페이지가 열리자마자 먼저 그려지다 보니 아직 사진이 준비 안 된
  상태로 그려지고 있었음.
- **어떻게 처리했나**: 페이지가 열릴 때 상품마다 대표 사진 1장만 먼저 불러와두는 과정을
  추가함 (`preloadMainThumbnails`, 전체 사진을 다 불러오면 무거우니 대표 1장으로 제한).
- **결과**: 새로고침 후에도 클릭 없이 바로 썸네일이 보이는 것을 확인함.

### 영어→한글 발음 변환기(좌측 목록 미리보기용, AI 아님)의 오표기들을 계속 잡아나간 일 (여러 건)
- **무슨 문제/요청이 있었나**: 좌측 목록에 보이는 한글 발음은 AI 없이 규칙으로만 돌아가는
  자체 변환기인데, 실사용해보니 여러 단어에서 이상하게 나옴 — "Helm"이 "헤름"으로(정확힌
  헬름), "Post-Shower"가 "포슷사우어"로(하이픈을 단어 경계로 인식 못 함), "Fate/Grand Order
  Moon Cancer"가 "팟/그라느드... 모온"처럼 깨짐.
- **확인해보니**: L 받침을 겹쳐 써야 하는 규칙이 "모음 사이의 L"만 처리하고 "단어 끝에
  자음이 붙는 L"은 처리 못 하고 있었음(Helm/Film 사례). 하이픈이 공백처럼 단어를 나누는
  경계로 인식되지 않아서 "Post"와 "Shower"가 하나의 단어로 합쳐져 엉뚱하게 변환됐음.
  "Moon"은 일본어 로마자 표기법 대조표에서 잘못 먼저 걸려버린 경우였고, "Fate"/"Grand"는
  영어 발음 규칙 자체에 빈 구멍(예: 묵음 e 처리 없음)이 있는, 서로 다른 두 가지 원인이었음.
- **어떻게 처리했나**: L 겹받침 규칙에 "단어 끝 자음군 앞의 L"도 포함하도록 보강함.
  하이픈도 공백처럼 단어 경계로 인식하도록 정규식을 고침. "Life"처럼 사전에 없어서 깨지는
  단어는 사전에 직접 추가함. Moon/Fate/Grand 같은 근본적인 엔진 한계는 사전 우회 외에
  완전히 고칠 방법이 마땅치 않다는 점도 같이 설명함(로마자→한글 자동변환 기존 라이브러리
  자체가 마땅한 게 없다는 것도 확인함).
- **결과**: Helm/Film이 헬름/필름으로, Post-Shower가 포스트-샤워로 정상적으로 바뀌는 것과
  girl/melon/gorilla 등 기존 케이스가 회귀되지 않는 것을 확인함. 다만 이 변환기는 AI가
  아니라 규칙 기반이라 정확도의 근본적 한계는 여전히 남아있음(실제 원고의 정확한 한글
  표기는 AI가 만드는 값이 항상 우선 적용됨).

### 고유명사(시리즈명·상품 라인명 등) 표기를 사전 하나로 통합
- **무슨 문제/요청이 있었나**: 위 변환기와 AI 원고 양쪽에서 같은 고유명사가 계속
  다르게/틀리게 나오는 사례가 반복되자, 고유명사 표기를 한 곳에 모아 양쪽이 같이 참고하게
  해달라는 요청.
- **어떻게 처리했나**: `rules/proper-nouns.js` 파일을 새로 만들어 시리즈명·상품 라인명·업계
  용어부터, 실사용에서 자주 깨졌던 일반 영어 단어(collection, surprise, exercise 등)까지
  100개 넘는 표기를 모아둠. 좌측 목록 변환기와 AI 원고 프롬프트 양쪽이 이 파일 하나만 같이
  읽도록 연결함.
- **결과**: 이후 발견되는 오표기는 이 파일 한 곳에만 추가하면 양쪽에 다 반영되는 구조로
  정리됨.

### 매일 회사 B2B 사이트에서 상품을 긁어오는 별도 도구(b2b-scraper) 수집 항목 정리
- **무슨 문제/요청이 있었나**: 수집 항목 중 도매가/소매가/카톤당 수량은 실사용에 필요
  없어서 빼달라는 요청. 그리고 성인 전용 상품은 회사가 취급하지 않으니 수집 단계에서부터
  아예 제외해달라는 요청.
- **어떻게 처리했나**: 가격/카톤수량 관련 추출 코드를 통째로 제거함. 상품 상세 페이지의
  "성인 전용 안내 문구"를 감지해서, 성인 상품이면 사진·텍스트 수집을 아예 건너뛰도록 함.
- **결과**: 성인 상품 안내 문구가 있는 가짜 페이지로 실제로 걸러지는 것을 확인함.

### b2b-scraper를 비개발자 팀원 PC에 안전하게 배포하는 방법 정리
- **무슨 문제/요청이 있었나**: 이 도구를 여러 팀원 PC에 나눠주고 최신 상태로 유지해야
  하는데, 로그인 정보가 도구 폴더 안에 같이 저장돼 있으면 폴더를 주고받을 때 유출 위험이
  있다는 점, 그리고 업데이트를 어떻게 배포/적용할지, 어느 저장소(비공개/공개)로 배포할지
  까지 정리가 필요했음.
- **확인해보니**: 로그인 세션이 도구 폴더 안(`chrome-profile`)에 저장되고 있어서, 폴더를
  그대로 압축해서 전달하면 로그인 정보까지 같이 넘어갈 위험이 있었음.
- **어떻게 처리했나**: 로그인 프로필 저장 위치를 도구 폴더 밖(팀원 PC의 `%LOCALAPPDATA%`)
  으로 옮김 (`profile-dir.js`). 로그인을 초기화하는 버튼(`reset-login.bat`)도 추가함. 새
  버전이 있는지 조용히 확인만 하고 강제로 적용하지 않는 기능(`check-update.js`)과, 팀원이
  원할 때 직접 눌러서 최신 코드로 덮어쓰는 업데이트 실행 파일(`update.bat`)을 추가함. 내부
  작업 기록이 섞인 메인 저장소 대신, 팀에서 이미 쓰던 별도 공개 저장소(`share`)로 배포
  경로를 정함.
- **결과**: 실제 배포된 저장소 파일 구조와, 업데이트 실행 파일이 압축 해제 후 예상한 폴더
  구조를 그대로 찾아 덮어쓰는지까지 직접 내려받아 확인함. 다음 출근(7/29) 후 실제 팀원
  배포 테스트를 이어서 진행하기로 함.

## 2026-07-27

### B2B 스크래퍼 더블클릭 실행 파일이 한글 깨짐으로 아예 안 돌아가는 문제 (두 차례 수정)
- **무슨 문제였나**: 팀원이 새 컴퓨터에서 더블클릭용 실행 파일(`run.bat`)을 돌렸더니
  `'nodejs.org'은(는) 내부 또는 외부 명령...` 같은 에러가 줄줄이 뜨면서 아예 실행이 안 됨.
- **확인해보니**: `run.bat` 안에 한글 안내문이 섞여 있었는데, 이 파일이 저장된 방식(UTF-8)과
  한국어 윈도우 CMD가 배치파일을 읽는 방식(CP949)이 어긋나서 한글이 깨짐. 깨진 바이트가
  명령줄 파싱까지 망가뜨려서, 깨진 글자 조각(`nodejs.org`, `ho` 등)을 진짜 명령어로 착각해
  실행하려다 전부 에러가 난 것.
- **어떻게 처리했나**: 처음엔 파일 자체를 한글 윈도우 기본 인코딩(CP949)으로 다시 저장해서
  우회했는데, 더 근본적인 방법을 알려주셔서 그걸로 다시 정리함 — **`run.bat`에는 아예 한글을
  한 글자도 안 쓰고 전부 영어(ASCII)로만 작성**, 팀원에게 보여줄 한글 진행 메시지는 그
  실행파일이 불러오는 실제 스크립트(`scrape.js`, Node.js)에서 출력하도록 역할을 나눔. ASCII는
  어떤 코드페이지로 읽어도 안 깨지기 때문에 이 문제 자체가 원천적으로 재발 안 함.
- **결과**: 새로 받아서 다시 실행하니 정상적으로 로그인 창이 뜨고 끝까지 진행됨, 팀원 확인 완료.

### B2B 스크래퍼가 실제 사이트에서 상품 정보를 하나도 못 긁어오던 문제
- **무슨 문제/요청이 있었나**: 로그인은 잘 되는데, 실제 오늘 상품 목록/상세 페이지의 화면
  구조를 몰라서 자리표시자로만 짜여있던 상태 (`TODO(recon)` 표시). 실제 사이트를 정찰해서
  알려달라고 요청.
- **확인해보니**: 정찰 결과를 직접 받아서 살펴본 결과 — 이 사이트는 데이터를 화면 뒤에
  따로 심어두는 방식(JSON)이 전혀 없는, 그냥 완성된 화면 그대로 내려주는 방식이었음. 그리고
  상품 상세 페이지에서 제목·발매월·가격·사이즈·제조사·저작권·사진이 각각 어떤 화면 요소
  안에 들어있는지도 실제 페이지로 확인함.
- **어떻게 처리했나**: 데이터 추출 방식을 통째로 화면 요소를 직접 읽는 방식(DOM 파싱,
  `page.locator`)으로 바꾸고, 실측한 화면 구조에 맞춰 각 항목을 뽑아내도록 정리함. "사이즈"
  항목에 재질·생산방식 설명("Ready-to-assemble" 등)까지 그대로 섞여 있는 것도 이때 같이
  확인함(아래 항목에서 별도 처리).
- **결과**: 팀원이 실제로 로그인해서 돌려본 결과, 상품 정보·사진이 실제로 채워지는 것까지
  확인됨.

### 오늘 발표된 상품만 가져와야 하는데, 최근 몇 주치가 다 섞여서 117건이나 잡히던 문제
- **무슨 문제였나**: 정찰 결과를 반영해 처음 돌려봤더니 상품이 0건, 그다음엔 목록 페이지
  구조를 실제로 반영했더니 이번엔 117건이나 잡힘 — 오늘 발표된 것만 필요한데 최근 것까지
  다 긁어온 것.
- **확인해보니**: 목록 화면의 "신상품" 구역이 하루치가 아니라 최근 며칠~몇 주치를 한꺼번에
  보여주고 있었음. 근데 상품 카드 하나하나에 발표일이 이미 눈에 안 보이는 값으로 박혀있는
  것을 발견함 (`data-guidance_date="20260728"` 형태).
- **어떻게 처리했나**: 상세 페이지를 일일이 열어보지 않고도, 목록 단계에서 이 값이 오늘
  날짜와 정확히 일치하는 상품만 걸러내도록 고침.
- **결과**: 실제로 다시 돌려서 오늘 발표분만 정확히 잡히는 것 확인함 (117건 → 실제 발표
  건수로 줄어듦).

### 원고 만드는 방식을 두 번 갈아엎음 — "AI가 AI를 교정"하던 구조에서 "사람이 직접 고치고 AI가 검수"하는 구조로
- **무슨 문제/요청이 있었나**: 실사용 팀원이 맞춤법에 약해서 원문/결과 비교(diff)가 반드시
  있어야 한다는 요청이 있었는데, 처음엔 "스펙 정보를 자연스러운 초안으로 한 번 가공한 뒤
  그 초안을 다시 AI가 교정하는" 2단계 구조로 만듦. 근데 실제로 써보니 이미 깔끔한 AI
  결과끼리 비교하다 보니 "교정할 내용이 없다"며 교정 내역 칸이 아예 사라져버리고, diff도
  의미가 없어짐. 진짜 필요한 건 "AI 결과 vs 팀원이 손으로 직접 고친 내용"을 비교해서 팀원의
  오탈자를 잡아주는 것이었음.
- **어떻게 처리했나**: 2단계 가공 구조를 걷어내고, 원고 생성은 다시 한 번의 변환(스펙→
  1차 텍스트)으로 되돌림. 대신 화면을 "재료 입력/1차 텍스트(읽기전용)"와 "2차 입력(팀원이
  직접 수정)/검수하기 버튼/수정본" 이렇게 네 칸으로 나눠서, 팀원이 2차 입력에서 직접 고친
  뒤 "검수하기"를 누르면 그제서야 AI가 그 수정본의 실제 오탈자만 짚어서 고쳐주고 "뭐가
  달라졌는지" 교정 내역을 보여주도록 다시 짬. 실시간으로 색깔(빨강/초록) 비교를 보여주는
  기능은 필요 없다고 해서 뺌.
- **결과**: 실제 화면을 자동으로 조작해서(입력→변환→직접 수정→검수) 4칸 레이아웃과
  교정 내역이 의도대로 뜨는 것 확인함.

### 사이즈 정보에 "Ready-to-assemble" 같은 영어 설명이 그대로 섞여 나오던 문제
- **무슨 문제였나**: B2B에서 가져온 "사이즈" 항목에 재질·생산방식 설명("Ready-to-assemble
  non-scale plastic model kit" 등)이 통째로 들어있었는데, 이게 그대로 결과 문장에 섞여 나와서
  "Ready-to-assemble"이라는 영어가 어색하게 남음.
- **어떻게 처리했나**: 사이즈 정보에서 실제로 필요한 건 높이/크기 수치뿐이라는 규칙을 추가해서,
  재질·생산방식 설명은 결과에서 빼고 크기 수치만 쓰도록 정리함.
- **결과**: 재변환해보니 해당 영어 설명이 빠지고 크기 수치만 남는 것 확인함.

### 시리즈명·캐릭터명이 번역 안 되고 영어 그대로 나오던 문제
- **무슨 문제였나**: "Blue Archive Saori"처럼 시리즈명·캐릭터명이 결과에서도 영어 그대로
  나오는 경우가 있었음. 처음 한 번 고쳤을 때는 "공식 표기를 확신 못 하면 영어 그대로 둬라"로
  짜놨더니, "Purple Lollipop"처럼 덜 알려진 이름은 여전히 영어로 남는 재발이 있었음.
- **어떻게 처리했나**: 규칙을 "모르면 영어 그대로"에서 "모르면 발음 나는 대로 한글로 옮겨
  적어라"로 바꿈 — 잘 알려진 시리즈명은 공식 한국어 표기(예: 블루아카이브)를, 안 알려진
  이름은 소리 나는 대로(예: 퍼플 롤리팝) 무조건 한글로 쓰게 함.
- **결과**: 재변환해보니 두 사례 다 한글로 바뀌는 것 확인함.

### 원고 내용이 캐러셀 이미지 칸에 자동으로 안 이어지던 문제 + 사진 없이는 미리보기가 안 뜨던 문제
- **무슨 문제/요청이 있었나**: 원고를 만들어도 캐러셀 이미지의 흰 텍스트(제목/부제)를 매번
  손으로 다시 입력해야 했음. 또 사진을 최소 1장 올리기 전에는 틀·글자가 어떻게 나올지
  미리 볼 방법이 없어서, 사진 없이도 틀·문구부터 먼저 보여달라는 요청도 있었음.
- **어떻게 처리했나**: 원고(1차 텍스트·수정본)에서 상품명/시리즈명 형태(【】/『』로 감싼
  부분)를 자동으로 뽑아서 캐러셀의 제목/부제 칸에 그대로 채워주도록 함 — 원고를 생성하거나
  검수할 때마다 최신 내용으로 자동 갱신됨. 그리고 사진이 0장이어도 캐러셀 미리보기 칸이
  항상 보이게 바꿔서, 회색 배경 위에 틀과 문구만 먼저 확인할 수 있게 함.
- **결과**: 원고만 만들어도 캐러셀 제목/부제가 자동으로 채워지고, 사진 없이도 틀+문구
  미리보기가 뜨는 것을 실제 화면으로 확인함.

### 주황 틀 좌상단에 발표 종류를 표시하는 드롭다운 추가
- **무슨 문제/요청이 있었나**: 주황 틀 왼쪽 위(로고 반대편)에 "이게 신제품 발표인지, 상품화
  결정 발표인지, 원형 공개인지" 같은 짧은 표시를 넣고 싶다는 요청.
- **어떻게 처리했나**: 실제 틀 이미지 파일을 열어서 로고가 없는 빈 자리(왼쪽 위)를 확인한 뒤,
  그 자리에 표시될 문구를 고르는 드롭다운을 추가함 (오늘 날짜가 자동으로 들어가는 "mm.dd
  신제품" / "상품화 결정" / "원형 공개" / 직접 입력하는 "기타" 중 선택).
- **결과**: 드롭다운으로 고른 문구가 실제 캐러셀 이미지 왼쪽 위에 얹혀 나오는 것, 사진을
  내보낼 때도 같이 찍히는 것까지 확인함.

### 캐러셀 제목이 길면 캔버스 밖으로 잘려서 안 보이던 버그
- **무슨 문제였나**: 자동으로 채워진 상품명이 길면("메지로 맥퀸 [Bring 'Em Home, Please!]
  Ver. 1/7 스케일 피규어" 등) 글자가 화면 양옆으로 잘려나가 일부만 보임.
- **확인해보니**: 제목 글자를 무조건 한 줄로만 그리고 있어서, 길이에 상관없이 화면 폭을
  그냥 넘어가 버린 것.
- **어떻게 처리했나**: 제목이 길면 2줄까지 자동으로 줄바꿈하고, 그래도 안 들어가면 글자
  크기를 점점 줄여가며 맞추도록 고침.
- **결과**: 긴 제목으로 다시 만들어보니 잘리지 않고 2줄로 깔끔하게 들어가는 것을 이미지로
  직접 확인함.

### 좌측 상품 목록이 안 읽히던 문제 (제목 잘림 / 상태 표시가 안 보임)
- **무슨 문제/요청이 있었나**: 상품명이 22자에서 강제로 잘려서(...) 뭔지 알아보기 힘들었고,
  "브랜드·원고·이미지" 상태 표시도 전부 같은 색 글씨라서 뭐가 끝났는지 한눈에 안 들어옴.
  거기에 영어 상품명만 있으니 한글로도 같이 보고 싶다는 요청 (단, AI를 매번 부르면 비용이
  드니 AI 없이 대략적인 한글 표기를 보여주는 자체 기능으로).
- **어떻게 처리했나**: 22자 강제 절삭을 없애고 줄바꿈으로 전체 제목이 보이게 함. 원고/이미지/
  검수 상태를 각각 초록(완료)·주황(대기) 색깔 알약 모양 표시로 바꿔서 한눈에 구분되게 하고,
  "검수" 상태도 새로 추가함. 그리고 AI 호출 없이 영어를 대략적인 한글 발음으로 바꿔주는
  규칙 기반 변환기를 만들어서, 원고를 아직 안 만든 상품도 영어 밑에 한글 발음이 같이
  보이게 함 (원고를 만들고 나면 그때는 AI가 정확하게 뽑아낸 한글 이름이 우선으로 쓰임).
- **결과**: 실제 화면으로 확인함 — 제목이 안 잘리고 다 보이고, 상태 알약이 초록/주황으로
  뚜렷하게 구분되고, 원고 생성 전에도 대략적인 한글 발음이 같이 뜸. 다만 이 한글 발음
  변환기는 AI가 아니라 규칙으로만 돌아가서 정확도가 완벽하지 않다는 점은 그대로 안내함
  (예: "Purple Lollipop"이 "퍼프르 로르리포프"처럼 다소 어색하게 나오는 경우 있음).

## 2026-07-24

### 상품 1건씩만 처리하던 화면을, 하루치 여러 상품을 한 화면에서 다루는 구조로 통째로 개편
- **무슨 문제/요청이 있었나**: 실제로는 하루에 여러 상품을 처리하는데, 화면은 한 번에 상품
  1개만 붙여넣고 변환하는 구조였음. 페이지를 옮기지 않고 그날 상품 전체(정보 입력 → 원고
  생성 → 캐러셀 이미지 제작)를 한 화면에서 끝낼 수 있게 다시 짜달라는 요청. 실제 구현 전에
  레이아웃 후보 여러 개를 눈으로 먼저 비교하고 확정한 뒤에 진행함.
- **확인해보니**: 레이아웃 후보 3가지(세로로 쌓기/카드로 나열/좌측 목록+우측 작업창)를
  실제 화면처럼 만들어서 비교해본 결과, "왼쪽에 그날 상품 목록, 오른쪽에 고른 상품의 작업
  화면" 방식이 제일 낫다고 확정됨.
- **어떻게 처리했나**: 화면을 좌측 상품 목록 / 우측 작업 화면 구조로 새로 짬. 상품마다
  ①상품정보(공식정보+캐릭터 메모, 기존에 쌓아둔 실제 게시물 157건 자료에서 검색해서 바로
  가져다 쓸 수 있음) → ②원고(기존 비교 화면·교정 내역·글자수 표시 그대로 재사용) →
  ③캐러셀 이미지 순서로 배치. 원고는 실제 판매 링크가 들어간 것을 기본으로 하고, "인스타용
  복사" 버튼을 누르면 그 링크 부분만 안내 문구로 자동으로 바꿔서 복사되게 함 (`insVersion()`
  함수가 정규식으로 링크 줄만 치환). 상품 목록과 입력 내용은 브라우저에 자동 저장되게 함
  (`localStorage`), 사진 원본은 용량이 커서 별도 저장소에 둠 (`IndexedDB`).
- **결과**: 실제 화면을 자동으로 조작해보는 방식(상품 추가/전환/입력/검색)으로 검증함,
  정상 동작.

### 캐러셀 이미지, 실제로 만들어보니 주황 틀이 상품을 너무 많이 가리는 문제
- **무슨 문제/요청이 있었나**: 사진을 올리면 정해진 비율(1080x1350, 4:5)로 꽉 채워 자동으로
  잘라주고, 미리 만들어둔 주황색 틀 이미지를 맨 위에 얹어 완성하는 기능을 새로 만들었는데,
  실제로 써보니 틀 위쪽(로고 들어간 부분)이 생각보다 두꺼워서 상품 사진 윗부분이 많이
  가려짐.
- **확인해보니**: 사진을 자를 때 기준을 "캔버스 전체 크기"로 잡고 있어서, 어차피 안 보이는
  (틀에 가려지는) 부분까지 사진에 억지로 채워 넣느라 필요 이상으로 확대되고 있었음. 실제
  틀 이미지 파일을 픽셀 단위로 분석해서 진짜로 보이는 영역(투명한 부분)의 정확한 좌표를
  알아냄.
- **어떻게 처리했나**: 사진을 자르는 기준을 "캔버스 전체"가 아니라 "틀에서 실제로 사진이
  보이는 영역"으로 바꿈 (`FRAME_INNER` 좌표 기준으로 `cover-fit` 계산). 추가로 여러 장 중
  대표로 지정한 1장에만 어두운 그라데이션과 글자를 넣고, 나머지 장은 필요하면 틀 자체를
  꺼서 사진이 안 가려지게 하는 기능도 넣음.
- **결과**: 일부러 사진 맨 위에 표시가 되는 테스트 이미지를 만들어서, 가려지는 비율이
  기존 방식보다 실제로 줄어드는 것을 직접 재봐서 확인함 (기존 0% 노출 → 개선 후 2.66%
  노출). 완전히 안 가려지는 건 아니라서, 그래도 가리면 사진 위치를 손으로 옮겨서 조정
  가능.

### 사진 여러 장 순서를 못 바꾸는 문제 → 마우스로 직접 끌어서 옮기는 방식으로 추가
- **무슨 문제/요청이 있었나**: 캐러셀 사진을 여러 장 올렸을 때 순서를 바꿀 방법이 없었음.
  처음엔 화살표 버튼(◀▶)으로 만들었는데, 써보니 너무 불편하다며 마우스로 직접 끌어서
  순서를 바꾸는 방식으로 바꿔달라는 요청 (참고할 기존 코드도 같이 전달받음).
- **어떻게 처리했나**: 화살표 버튼을 없애고, 사진을 마우스로 누른 채 다른 사진 위로 끌어다
  놓으면 그 자리로 순서가 바뀌는 방식으로 새로 만듦. 순서를 실제 화면의 이동 칸 위치가
  아니라 사진마다 가진 고유 값으로 계산해서 (`slide.key` 기준 이동), 대표로 지정해둔 사진이나
  지금 편집 중인 사진이 순서를 바꿔도 엉뚱한 걸 안 가리키게 같이 맞춰줌.
- **결과**: 마우스로 직접 끌어서 옮기는 걸 자동으로 재현해서, 순서가 실제로 바뀌는 것까지
  확인함.

### "나머지 사진 틀 제거" 버튼이 사진을 통째로 지워버리는 것으로 오해해서 잘못 만듦
- **무슨 문제/요청이 있었나**: "대표 사진 빼고 나머지는 틀 제거" 기능을 요청받았는데, 처음엔
  "나머지 사진 자체를 지우는 것"으로 잘못 이해해서 그렇게 만들어버림. 실제 의도는 사진은
  그대로 두고 틀(액자 이미지)만 안 보이게 해달라는 것이었음.
- **어떻게 처리했나**: 사진을 지우는 대신, 사진마다 "틀 있음/없음" 표시만 따로 갖게 해서
  (`slide.noFrame` 값), 버튼을 누르면 대표 사진 빼고 나머지는 이 표시만 켜지도록 고침. 사진
  자체는 전혀 건드리지 않음.
- **결과**: 버튼을 눌러도 사진 개수는 그대로 유지되고, 대표가 아닌 사진만 틀이 빠지는 것을
  픽셀 단위로 직접 확인함 (틀이 있어야 할 자리의 색을 검사).

### 틀을 한 번 제거하면 다시 켜지지 않는 버그
- **무슨 문제였나**: 위 기능으로 틀을 제거한 뒤, 대표 사진을 다른 걸로 바꾸면 새로 대표가 된
  사진에 틀이 안 생기고 계속 빈 채로 남는 문제. 그리고 특정 사진 하나만 다시 틀을 켜고 싶어도
  그럴 방법이 없었음.
- **확인해보니**: 대표 사진을 바꾸는 버튼이 "대표"라는 값만 바꾸고, 그 사진의 "틀 있음/없음"
  표시는 그대로 내버려두고 있었음. 그래서 예전에 꺼져 있던 사진이 새로 대표가 돼도 꺼진
  채로 남아있었던 것.
- **어떻게 처리했나**: 대표로 지정할 때 그 사진의 틀을 무조건 다시 켜지도록 고치고, 추가로
  사진마다 개별적으로 틀을 껐다 켰다 할 수 있는 버튼을 새로 만듦.
- **결과**: 대표를 바꿔도 새 대표에 틀이 정상적으로 생기는 것, 개별 버튼으로 틀을 껐다가
  다시 켰을 때도 정상적으로 돌아오는 것까지 화면을 직접 조작해서 확인함.

### B2B 사이트에서 상품을 자동으로 가져오는 기능 착수
- **무슨 문제/요청이 있었나**: 상품정보를 사람이 일일이 손으로 옮겨 적는 게 아니라, 회사
  B2B 사이트에서 자동으로 긁어와달라는 요청. 화면엔 "B2B에서 가져오기" 버튼만 있고 실제
  연동은 안 돼 있던 상태였음.
- **확인해보니**: 이 사이트는 로그인이 필요하고, 로그인 자체는 사람이 직접 눈으로 보고
  하는 방식이 안전하다는 걸 이전에 만든 비슷한 도구(다른 사이트 자동 수집 도구)에서 이미
  확인해둔 상태였음. 그리고 이 로그인 과정은 화면이 있는 컴퓨터에서만 할 수 있어서, 지금
  작업하는 원격 서버에서는 대신 해줄 수 없다는 것도 확인함.
- **어떻게 처리했나**: 새 프로젝트(`b2b-scraper`)를 만들어서, 로그인 세션 저장, 사이트 구조
  확인용 정찰 스크립트, 사진 다운로드, 결과 정리까지 필요한 부분을 미리 짜둠. 그리고
  실제 사이트 연동이 되기 전에도 확인해볼 수 있게, 화면 쪽 "B2B 가져오기" 버튼을 안내
  문구만 뜨던 상태에서 실제로 파일(사이트에서 긁은 결과 + 사진)을 골라서 상품 목록에
  채워 넣는 기능으로 먼저 완성함.
- **결과**: 실제 사이트 로그인·정찰은 사람이 직접 컴퓨터에서 진행해야 하는 부분이라 아직
  못 함(다음 순서로 넘김). 그 앞단인 "가져온 결과를 화면에 채워 넣는 기능"은 가짜 결과
  파일로 직접 넣어보는 방식으로 확인함, 정상 동작 (상품 제목·정보·사진 개수까지 전부
  정확히 반영됨).

### B2B 스크래퍼, 실제 쓸 사람(비개발자 팀원) 기준으로 실행 방식 다시 정리
- **무슨 문제/요청이 있었나**: 위 B2B 스크래퍼는 처음엔 "터미널 열어서 로그인 스크립트 먼저
  실행 → 엔터 → 스크래핑 스크립트 실행" 방식으로 짜여 있었는데, 실제로 쓸 사람은 코딩을
  전혀 모르는 팀원이라 이 방식은 안 된다는 요청. 매번 검은 창 열어서 명령어 치는 방식(예전
  메가하우스 스크래퍼 방식)은 금지, 목표는 "바탕화면 아이콘 더블클릭 → 크롬에서 로그인만 →
  끝."
- **어떻게 처리했나**:
  - 로그인 스크립트와 스크래핑 스크립트를 하나로 합침 — 이제 스크래핑 스크립트 하나가
    "로그인 세션이 남아있나 확인 → 없거나 만료됐으면 크롬 로그인 창을 띄우고 기다림 → 있으면
    바로 스크래핑"까지 한 흐름으로 처리함. 로그인 완료도 터미널에서 엔터 치는 방식 대신,
    로그인 후 페이지 주소가 바뀌는 걸로 자동 감지하게 바꿈.
  - 아이콘 더블클릭 하나로 "Node 설치 확인 → (최초 1회만) 필요한 파일 자동 설치 →
    스크래퍼 실행"까지 되는 실행 파일(`run.bat`, 맥은 `run.command`)을 추가함. 팀원은 이
    파일을 더블클릭만 하면 됨 — 타이핑 없음.
  - 최초 설치를 가볍게 하기 위해, 브라우저까지 통째로 내려받는 방식(`playwright`) 대신
    팀원 컴퓨터에 이미 깔려 있는 진짜 크롬을 그대로 쓰는 방식(`playwright-core`)으로
    바꿈 — 처음 설치할 때 수백MB를 추가로 받을 필요가 없어짐.
- **결과**: 팀원이 겪을 과정이 "더블클릭 → (필요할 때만) 크롬에서 로그인 → 완료 문구 확인"
  세 단계로 줄어듦. 다만 진행상황을 보여주는 검은 콘솔 창 자체는 뜨는데(입력은 필요 없음),
  이걸 완전히 없애려면 별도 프로그램 포장이 필요해서 이번 규모엔 과하다고 판단해 지금
  방식으로 마무리함. 실제 사이트 정찰·연동은 여전히 로컬 컴퓨터에서 사람이 진행해야 하는
  부분이라 그대로 대기 중.

## 2026-07-13

### 변환 실패했을 때 안내문이 실제 원인과 안 맞는 문제
- **무슨 문제였나**: 변환이 실패하면 항상 "키를 등록해달라"는 안내문이 뜨는데, 이미 등록도
  끝나고 배포도 정상인 지금은 이 안내가 오히려 헷갈리게 만듦.
- **확인해보니**: 실패 이유가 뭐든 상관없이 무조건 같은 안내문을 덧붙여서 보여주고 있었음.
- **어떻게 처리했나**: 실제 실패 사유에 "키 관련" 내용이 있을 때만 그 안내를 붙이고, 아니면
  실제 실패 내용만 그대로 보여주도록 고침.
- **결과**: 우리 작업 공간에는 반영해뒀고, 정식 반영은 나중에 다른 수정 건들과 같이 하기로
  미뤄둠.

### 상품 설명에 섞인 잡담을 "오타"라고 잘못 설명하던 문제, 알고 보니 대사였던 경우
- **무슨 문제/요청이 있었나**: "어때, 예쁘게 나와?" 같은 상품 설명이 아닌 말이 입력에 섞여
  있으면 결과에서는 잘 빠지는데, 그 이유를 "의미 없는 오타라서 지웠다"고 잘못 설명함. 근데
  알고 보니 이 문장은 잡담이 아니라 피규어 캐릭터가 실제로 하는 대사였고, 이런 대사를 홍보
  글 맨 앞에 넣는 게 원래 흔한 방식이었음.
- **확인해보니**: AI에게 "오타 교정"과 "상품과 무관한 문장 제외"를 구분해서 알려주는 내용
  자체가 없어서 아무 이유나 갖다 붙인 것으로 보였음. 그리고 실제 계정에 올라간 과거 게시물
  157건을 받아서 살펴보니, 캐릭터 대사를 큰따옴표로 시작하는 게 드물지 않은 관행(약 17%
  정도)이라는 것도 확인됨.
- **어떻게 처리했나**: 오타 교정과 무관한 문장 제외를 구분해서, 무관한 문장을 뺄 땐 정확한
  이유를 적도록 정리함. 그리고 맨 앞에 큰따옴표로 감싼 문장이 있으면 잡담이 아니라 대사이니
  그대로 살리도록 규칙에 새로 추가함. 대사가 문장부호 없이 어정쩡하게 끝나면 마침표를 붙여
  마무리하도록도 같이 정리함.
- **결과**: 이후 테스트에서 이유 설명이 정확해졌고, 대사도 잘 살아남는 것 확인함.

### 브랜드를 미리 고르게 해달라는 요청
- **무슨 문제/요청이 있었나**: 원래는 세 브랜드(부시로드/메가하우스/굿스마일) 규칙을 한꺼번에
  넣어주고 AI가 알아서 맞는 걸 고르게 했는데, 정확도가 떨어지니 사용자가 입력 전에 브랜드를
  직접 고르게 해달라는 요청.
- **확인해보니**: 규칙 세 개를 한 번에 다 넣어주다 보니 서로 헷갈릴 여지가 있었음.
- **어떻게 처리했나**: 화면에 브랜드 선택 버튼 3개를 추가하고, 고른 브랜드의 규칙만 골라서
  넘기도록 바꿈. 버튼 모양도 처음엔 작고 어색해서, 화면 폭에 맞춘 큼직한 버튼으로 바꿨다가
  나중엔 가운데 정렬 + 각각 독립된 박스 모양으로 다시 다듬음. 이 중 건수가 제일 많은
  굿스마일을 기본값으로 미리 선택해둠.
- **결과**: 실제로 화면에서 버튼 눌러가며 확인함, 정상 동작.

### 실제 계정 게시물 데이터로 굿스마일 규칙을 다시 만든 일
- **무슨 문제/요청이 있었나**: 굿스마일 브랜드 규칙이 예전에 추측으로 만들어둔 거라 실제
  게시물이랑 많이 다르다는 지적이 있었고, 실제 계정에서 뽑은 과거 게시물 목록(엑셀 표)을
  받아서 그 기준으로 다시 정리해달라는 요청.
- **확인해보니**: 실제 게시물을 살펴보니 여러 차이가 드러남 — 글자수 제한이 실제보다 훨씬
  빡빡하게 잡혀있었고, 해시태그 개수/조합도 실제와 다르게 정해져 있었고, "아직 판매는 안
  하고 공개 발표만 하는 글" 양식이 아예 빠져있었음. 이모지는 게시물 표 자체에서 대부분
  빠져있어서, 이건 표를 뽑아준 도구가 놓친 것뿐이고 실제로는 담당자가 알아서 넣는 것으로
  확인됨.
- **어떻게 처리했나**: 실제 게시물 패턴에 맞춰 규칙을 통째로 다시 작성함 (글자수 제한 완화,
  해시태그 개수 실제에 맞춤, 발표 전용 양식 추가, 이모지는 AI가 손대지 않고 입력에 있는
  그대로 두도록 정리).
- **결과**: 이후 테스트에서 실제 게시물과 비슷한 형태로 나오는 것 확인함. 다만 세부적으로
  안 맞는 부분은 이후에도 계속 발견돼서 아래 "반복 테스트" 항목에서 계속 다듬어나감.

### 예전 광고 초안들로 반복 테스트하며 잡은 문제들 (여러 건)
- **무슨 문제/요청이 있었나**: 오타도 많고 지금 양식과도 다른, 1년 전쯤 실제로 썼던 초안을
  여러 개 넣어보면서 결과가 이상한 부분을 하나씩 짚어주는 방식으로 여러 차례에 걸쳐 진행됨.
- **확인해보니 + 어떻게 처리했나** (건별로):
  - 장식용 기호(이모지)가 스펙 줄 말고 다른 자리(작품 소개 줄, 설명 문장 안)에서는 자꾸
    사라짐 → 결과 전체에 기호 보존 원칙이 적용되도록 다시 정리
  - 제목 칸이 비어있을 때 캐릭터 이름만 겨우 채워 넣거나, 반대로 이미 제대로 된 제목인데도
    단어를 하나 더 붙여서 이상하게 중복시킴 → 제목이 진짜로 비어있을 때만 보강하고, 이미
    내용이 있으면 절대 손대지 않도록 정리
  - 문장이 이미 자연스러운데도 억지로 "이름 (버전)" 줄과 "~로 등장!" 줄, 이렇게 두 줄로
    어색하게 쪼개던 문제 → 문장을 억지로 안 쪼개고 자연스럽게 이어쓰도록 정리하되, 모바일
    에서 읽기 좋게 줄바꿈은 하도록 함
  - 마무리 문장이 "~만나보세요", "~등장합니다" 등 제각각으로 끝나던 문제 → 실제 계정은
    예외 없이 "~등장!"으로 끝난다는 걸 확인하고 그렇게 고정
  - 구매 링크나 스펙(높이/발매일) 앞에 붙는 기호가 한 줄로 합칠 때 사라지거나 다른 항목
    기호로 바뀌던 문제, 링크 앞 콜론 표시가 통째로 사라지는 경우도 있었음 → 항목마다 원래
    기호를 각자 그대로 유지하고 콜론도 항상 남기도록 정리
  - 입력에 있지도 않은 대사를 "더 생생해 보이려고" 마음대로 지어내서 넣은 심각한 사례
    발견 → 대사는 입력에 있는 것만 옮기고 절대 새로 짓지 않도록 강하게 금지
  - 원래 한국어로 된 대사를 갑자기 일본어로 바꿔서 써버린 사례 발견 (정확한 원인은 알 수
    없으나, 이 분야 콘텐츠 자체가 일본 원작이 많다 보니 AI가 "원래 일본어였을 것"이라고
    착각한 것으로 추정) → 지침으로 강하게 금지하는 것과 별개로, 없던 일본어 글자가 결과에
    나오면 아예 화면에 내보내지 않고 자동으로 막아버리는 이중 안전장치를 추가함
  - 원문에 마침표로 문장이 나뉘어 있으면 무시하고 억지로 한 문장으로 붙이거나, 반대로
    마침표가 하나도 없는데 새로 마침표를 만들어서 쪼개버리는 문제가 둘 다 있었음 → 원문에
    있는 문장 경계는 존중하되, 없는 경계는 새로 만들지 않도록 양쪽 다 정리
  - "정보를 지우지 말고 문법만 고쳐라"고 너무 좁게 정해놔서, 정보 손실 없이 표현만
    다듬는 것까지 막혀버림 → 정보는 그대로 유지하되 표현이나 문장 구조는 자유롭게 다듬어도
    되도록 완화
  - 같은 조사가 겹쳐서 어색한 문장(예: "~모습으로 ~시리즈로 등장")이 계속 안 고쳐짐 →
    "시리즈" 뒤에서만 다른 조사를 쓰도록 구체적으로 못박음
  - 복잡한 설명에서 같은 내용을 두 번 반복해서 쓰고, 두 캐릭터의 역할까지 뒤바꿔 쓴 심각한
    사례 발견 → 실패했던 사례를 그대로 예시로 넣어서 최대한 방지하도록 정리했지만, 문장이
    복잡할 때 AI가 헷갈려서 생기는 문제라 완전히 막힌다고 장담은 못 함
- **결과**: 매번 실제로 재테스트해가며 하나씩 확인함. 이 과정에서 나온 문제들은 한 번에 다
  안 잡히고 여러 차례 되풀이해서 조금씩 잡아나갔음. 특히 마지막 "중복 서술" 문제는 규칙
  문구만으로 100% 방지된다고 보장하기 어려운 유형이라, 앞으로도 비슷한 사례가 보이면
  계속 알려주기로 함.

### 원문이랑 결과를 나란히 비교해서 보여달라는 요청
- **무슨 문제/요청이 있었나**: 뭐가 어떻게 바뀌었는지 한눈에 보기 힘드니, 위키나 코드
  비교하듯 원문과 결과를 좌우로 놓고 빠진 건 빨간색, 추가된 건 초록색으로 표시해달라는
  요청. 교정 내역도 항목이 많으면 안 읽히니 핵심만 굵게 표시해달라는 것도 같이 요청.
- **어떻게 처리했나**: 변환하면 원문/결과를 좌우로 놓고 색으로 비교해서 보여주게 만들고,
  필요하면 색을 꺼서 볼 수 있는 버튼도 추가함. 교정 내역은 "무엇을 무엇으로 고쳤는지" 핵심
  부분만 자동으로 굵게 표시되게 함.
- **결과**: 실제 화면에서 확인함, 정상 동작.

### AI가 왜 그런 판단을 했는지 기록으로 남겨달라는 요청
- **무슨 문제/요청이 있었나**: 일본어를 마음대로 지어내는 것처럼 AI가 규칙을 어긴 경우,
  그냥 막기만 하지 말고 왜 그랬는지 기록을 남기고 화면에서도 바로 확인할 수 있게
  해달라는 요청.
- **어떻게 처리했나**: 규칙 위반이 감지되면 그 순간의 원문, 막힌 결과, AI가 스스로 설명한
  이유까지 전부 별도 기록으로 남기도록 만들고, 화면 맨 아래에 접었다 펼 수 있는 "이상 감지
  로그" 칸을 추가해서 그 기록을 바로 볼 수 있게 함.
- **결과**: 화면에 칸은 정상적으로 뜸. 다만 기록이 실제로 잘 쌓이는지는 아래 항목 문제로
  아직 확인 못 함.

### 변환 기록이 실제로 저장 안 되는 문제 (미해결)
- **무슨 문제였나**: 오늘 수십 번 변환 테스트를 했는데도, 그 기록이 쌓여야 할 저장소를
  확인해보니 단 한 건도 남아있지 않음. 접속에 필요한 값들을 다시 등록했는데도 여전히
  그대로임.
- **확인해보니**: 접속에 필요한 값 세 가지 중 최소 하나가 여전히 비어있거나 틀렸을
  가능성, 또는 값은 맞는데 "읽기"만 되고 "쓰기"는 안 되는 권한으로 등록돼 있을 가능성,
  이렇게 두 가지로 좁혀둔 상태. 정확히 어느 쪽인지는 아직 확정 못 함.
- **어떻게 처리했나**: 원인이 확정되지 않아 별도 조치는 하지 않음. 확인에 필요한 정보를
  요청해둔 상태.
- **결과**: 미해결. 다음에 이어서 확인 필요.

</details>
