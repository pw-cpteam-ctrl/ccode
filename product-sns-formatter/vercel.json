{
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1 || git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD ./",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "claude/sns-formatter-verified-commits-test": true,
      "*": false
    }
  }
}
