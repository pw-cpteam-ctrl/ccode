## 2026-09-23

### 메가하우스 업로드 과정을 한 화면으로 모으는 도구 새로 만듦
- **문제** ▶ 매달 메가하우스 업로드가 D-7 프리뷰부터 D+30 마감임박까지 이어지는데, 원고(공지 1건 + 상품별 인스타·트위터 + 리마인드)와 잡무(톡톡, 인플루언서 체크 등)가 여러 도구와 노션에 흩어져 있었음
  - D-7 본사 리스트는 뒤죽박죽이고 확정본도 아니라서, 인기도로 다시 정렬하고 D-1 발주서로 다시 맞춰야 함
  - 상품마다 스마트스토어 링크를 따서 원고에 넣는 작업도 따로 있음
- **처리** ▶ 클릭되는 시안을 먼저 보여드린 뒤, 실제로 쓰는 도구로 옮김 (`mh-upload-board/`)
  - 노션 연동은 예전에 호환이 안 좋았고 메가하우스는 데이터베이스로 만들 규모가 아니라서, 우선 브라우저 저장 + 백업 파일로 감
  - 인스타 원고엔 링크 대신 "구매는 프로필 링크를 참고해주세요", D+14·D+30도 인스타·트위터를 따로 만든다는 답변 반영
- **확인** ▶ 브라우저 자동 조작(Playwright)으로 붙여넣기 → 등급·순위 → 링크 → 원고 → 발주서 엑셀 → 새로고침 유지 → 백업/복원 → 옛 저장본까지 45개 항목 점검
  - 점검 중 발견해서 고친 것 ①: 발주서에서 상품명 열을 잘못 잡으면 목록 전체가 "빠짐"으로 반영될 수 있었음 → 한 줄도 못 읽으면 반영 버튼 잠금, 절반 이상 빠지면 경고
  - 점검 중 발견해서 고친 것 ②: 창을 닫을 때 무조건 저장하게 돼 있어서, 탭을 두 개 열면 한쪽이 다른 쪽 작업을 덮어쓸 수 있었음 (저장 대기 중일 때만 저장하도록 바꾸고, 다른 탭 변경을 바로 따라가게 함)
  - 점검 중 발견해서 고친 것 ③: 등급 끝에서 ▼를 누르면 비어 있는 등급을 건너뛰었음 → 바로 옆 등급으로 한 칸씩 넘어가게 함
- **결과** ▶ 허브 페이지에 카드 추가. D-7 공지·리마인드 문구와 번역기·캐러셀 바로가기는 아직 임시/미연결
