# SNS 성과 리포트 자동화 — 기획 정리 (v1, 진행 중 킵)

> 매달 수동으로 작성하던 SNS 성과 리포트(자사·경쟁사 비교 + 판매데이터 연동)를
> 자동화하는 프로젝트. 컴퓨터 필요한 작업이라 잠시 킵 — 재개 시 이 문서부터 확인.
> `llm-product-formatter-plan.md`(언어모델 웹 구현), `text-gradient/`(그라데이션 도구)와는
> 완전히 별개 프로젝트.

## 목적
- 자사/경쟁사 SNS 게시물의 좋아요·댓글·팔로워 등 공개 지표를 링크만 붙여넣으면 자동 수집
- 자사 vs 경쟁사 비교표 자동 생성 (몇 % 우세인지 비율 계산)
- 판매데이터와 연결해서 SNS 성과가 판매에 얼마나 기여했는지까지 (2단계, 후속)
- AI가 글 써주는 리포트는 불필요 — 순수 수집·집계 자동화만

## 결정된 사항

### 수집 지표
- 좋아요/댓글/팔로워수 등 **공개 지표만** (로그인 없이도 보이는 값)
- 경쟁사 계정은 로그인 불가하니 애초에 공개 지표 기준으로 설계

### 기간 필터
- 수집 범위 지정 가능해야 함 (예: 7/1~7/2, 또는 6/30 하루만)
- SNS 수집 + 판매데이터 양쪽 다 같은 기간 기준으로 맞춰서 비교

### 로그인 방식 (인스타/X)
- 본인 인스타그램/X 계정 로그인 세션으로 브라우징하며 수집 (예전에도 이 방식으로 해옴)
- Playwright `storageState` 파일(쿠키 기반)로 세션 전달 — 비밀번호는 절대 코드에 안 남음
- 세션 파일 생성법 (기본, `playwright codegen` 방식):
  ```
  npx playwright install chromium   # 최초 1회
  npx playwright codegen --save-storage=instagram-session.json https://www.instagram.com
  npx playwright codegen --save-storage=x-session.json https://x.com
  ```
  브라우저 창 뜨면 직접 로그인 → 창 닫으면 세션 파일 자동 생성
  - ⚠️ **실제로 시도해보니 X/인스타 양쪽 다 "일시적으로 로그인 제한됨"으로 막힘.** playwright가
    새로 내려받은 낯선 크로미움 + 자동화 흔적을 봇으로 의심해서 차단하는 것으로 추정
    (계정 2개 다 동일하게 막혀서 계정 문제가 아니라 브라우저 지문 문제로 판단).
    `--channel=chrome`(설치된 크롬 그대로 사용)로도 시도했으나 마찬가지로 막힘.
- **대안 경로 A-1 (개발자도구 cURL 복사) — ⚠️ 최신 크롬은 보안상 Cookie 헤더를 빼고 복사함,
  실제로 시도했으나 쿠키가 아예 없이 복사돼서 이 방식은 사실상 못 씀 (도구는 만들어뒀지만
  크롬 버전에 따라 동작 안 할 수 있음, 참고용으로만 남김):**
  1. F12 → Network 탭 → 새로고침 → 맨 위 문서 요청 우클릭 → Copy → Copy as cURL
  2. `node curl-cookies-to-storage-state.js <파일> x-session.json https://x.com`
- **대안 경로 A-2 (개발자도구 Application 탭 쿠키 표 복사) — 확장 없이 가능, 채택된 방식:**
  1. F12 → **Application** 탭 → Storage → Cookies → `https://x.com` 클릭
  2. 쿠키 표 안 클릭 → **Ctrl+A**(전체선택) → **Ctrl+C**(복사)
     — 실제로 해보니 **헤더 줄(Name, Value...)은 안 딸려오고 표 본문만 복사됨** (헤더는 고정
     영역이라 스크롤 영역 선택에 안 포함되는 듯). `table-cookies-to-storage-state.js`가 헤더 유무를
     자동 판별해서 헤더 없으면 크롬 고정 열 순서(Name/Value/Domain/Path/Expires/Size/HttpOnly/
     Secure/SameSite/PartitionKey/CrossSite/Priority)로 처리하도록 대응함
     — 만료일이 "세션"(한국어 로케일)으로 표시되는 것도 처리함
  3. `node table-cookies-to-storage-state.js <붙여넣은 텍스트 파일> x-session.json https://x.com` 실행
     → 표에 있는 실제 httpOnly/secure/SameSite/만료일 속성을 그대로 반영 (curl 방식보다 정확).
     Playwright 컨텍스트에 실제 로드되는 것까지 검증 완료 (모킹 값 + 실제 계정 쿠키 둘 다 확인)
  4. 인스타그램도 동일하게 origin만 `https://www.instagram.com`으로
  - ⚠️ **가상 스크롤 때문에 Ctrl+A가 화면에 보이는 근처 행만 선택할 수 있음.** 필요한 쿠키가 안
    보이면 패널 상단 필터(돋보기) 입력창에 쿠키 이름(`auth_token`, `ct0` 등)을 검색해서 그 행만
    복사하는 걸 추천
  - ⚠️ **실제 로그인 쿠키 값(`auth_token` 등)은 비밀번호와 동급으로 민감함 — 채팅에 붙여넣지 말고
    본인 컴퓨터에서 직접 `table-cookies-to-storage-state.js`를 돌려서 로컬에만 `x-session.json`을
    만들어 쓸 것.** (Claude Code 웹 실행 환경은 격리된 원격 샌드박스라 브라우저의 실제 네트워크
    접속 자체가 막혀있어서 — `example.com`조차 `ERR_CONNECTION_RESET` — 이 환경에서 실제
    x.com/인스타그램 접속 테스트도 못 함. `twitter.js`/`instagram.js` 실전 검증은 어차피
    PLAN대로 로컬 컴퓨터에서만 가능)
- **대안 경로 B (확장 설치, Cookie-Editor)** — A가 안 되면 시도:
  1. "Cookie-Editor" 확장 설치 → 정상 로그인 → "Export as JSON"으로 내보내기
  2. `node cookies-to-storage-state.js cookies-x-raw.json x-session.json https://x.com` 실행
  - 둘 다 로그인 자체를 평소 쓰던 정상 브라우저에서 하므로 자동화 탐지에 안 걸림
- ⚠️ 세션 파일(그리고 원본 쿠키 export 파일)은 로그인된 상태 그 자체. 유출 주의, 만료되면 재생성 필요.
  둘 다 `.gitignore`에 포함(`*-session.json`, `cookies-*.json`)돼서 실수로 git에 안 올라감.

### 판매데이터 (네이버 스마트스토어)
- 지금은 개인 개발 크롬 확장프로그램으로 "실시간 재고"만 조회 가능 (기간별 소급 조회 불가)
- 우선순위 추천:
  1. **네이버 커머스API센터 공식 API** 신청 가능한지 먼저 확인 (제일 안정적, 장기적으로 더 간단함)
  2. 안 되면 확장프로그램 내부 뜯기보다 **판매자센터 화면 자체를 로그인 세션으로 자동 열람** (원리는 SNS 스크래핑과 동일, 더 안전하고 유지보수 쉬움)
  3. 최후 수단으로만 확장프로그램 구조 분석
- 재고가 "현시점 조회만" 가능하다는 제약 있음 → 기간별 판매량 추정하려면 앞으로 **직접 스냅샷을 주기적으로 남겨둬야** 함 (과거 소급 불가)

### 산출물
- 웹으로 결과 확인 + **매번 자동으로 엑셀 저장** (히스토리 누적, 과거 데이터 안 날아가게 보존)

### 실행 방식
- 스케줄 자동화 아님 — **매달 본인이 직접 실행 요청**하는 방식

### 실행 환경 (중요)
- 수집에 몇 분씩 걸리는 작업이라 **버셀(Vercel) 서버리스 함수는 부적합** (실행시간 제한 짧음)
- 로컬 컴퓨터에서 스크립트로 직접 실행하는 방식으로 진행 (매달 수동 실행 패턴과도 잘 맞음)
- 언어모델 웹 구현 프로젝트(버셀 배포)와는 실행 환경이 다르므로 별도로 관리

## 진행 순서
1. **SNS 스크래퍼 먼저 완성 + 실제 계정으로 테스트/안정화**
2. 네이버 판매데이터 연동 방식 확정되면 후속으로 붙이기
   (불확실한 두 가지를 동시에 엮지 않기 위해 순차 진행하기로 결정)

## 스크래퍼 (이 폴더에 포함, 실제 계정으로 검증 완료 — 아래 "다음 단계" 참고)
- `twitter.js` — X(트위터) 프로필 게시물 수집. 예전 수동 방식(브라우저 JS 주입 + 대화창 누적)을
  Playwright 스크립트로 이식. 스크롤하며 파싱, 범위 시작일 이전 게시물 연속 3건 나오면 자동 중단.
- `instagram.js` — 인스타그램 프로필 게시물 수집. 링크 먼저 다 모으고(날짜 기반 조기 종료 포함)
  → 게시물별 개별 파싱. **좋아요/댓글수를 화면 좌표(x>700, y 400~580) 기준으로 찾는 부분,
  실제 계정으로 화면과 대조해서 정확히 일치함을 확인함.** 캡션 추출 정규식은 계정명 파라미터화 완료.
- 둘 다 계정명·기간·세션파일 경로를 파라미터로 받도록 일반화해둠 (원본은 자사 계정 한정 코드였음)

## 취합/저장 로직
- `aggregate.js` — `buildComparisonReport()`: 자사/경쟁사 수집 결과를 받아 플랫폼별(트위터/인스타)로
  게시물수·총/평균 좋아요·리트윗(트위터)·댓글(인스타)을 집계하고, 자사 대비 경쟁사 비율(%)과
  우세/열세 %p를 계산. 경쟁사가 여러 계정이면 계정별 비교 + 경쟁사 평균 비교를 함께 제공.
  - `parseCount()`: "1.2만", "3천", "1.2K", "3.4M", "3,412" 등 표기를 숫자로 변환. 파싱 실패(예:
    인스타 좌표 파싱이 잘못 잡은 값)는 죽지 않고 `null` 처리 후 계정별 `parseFailures` 건수로 노출.
  - **`buildProductComparison()` (상품별 비교):** run.js로 첫 실전 실행해봤더니 전체 합산
    요약만 있어서 "상품(피규어)별로 자사 vs 경쟁사 비교"를 보고 싶다는 요청 받음.
    - 1차 시도(해시태그 교집합 매칭)는 폐기함 — 해시태그 없이 상품명을 그냥 본문에 나열하는
      게시물(예: 고정 게시물의 "헌터x헌터 원피스 나루토...")을 다 놓쳐서 매칭률이 낮았음.
    - **최종: 본문 템플릿 안 고정된 위치에서 상품명을 직접 추출.** 자사/경쟁사 둘 다 게시물
      작성 템플릿이 일정하다는 걸 확인해서(사용자 확인) — **자사는 본문 첫 줄**, **경쟁사는
      "바로가기"/링크가 있는 줄 바로 위 줄(✔️ 표시)** 이 상품명이라는 규칙으로 `extractOwnProductName()`
      / `extractCompetitorProductName()` 구현. 추출한 상품명에서 키워드를 뽑아(브랜드/시리즈/판매
      상용구는 `GENERIC_KEYWORDS`로 제외) 하나라도 겹치면 같은 상품으로 그룹화(Union-Find).
      상품명을 못 뽑았거나 겹치는 키워드가 없는 게시물은 "매칭 안 됨" 목록으로 투명하게 노출.
    - **한계**: 순수 키워드 매칭이라 (1) 표현이 아예 다르면 매칭 실패, (2) 흔한 단어가 겹쳐서
      상관없는 상품이 잘못 묶일 가능성 있음 — `GENERIC_KEYWORDS` 목록은 지금까지 나온 실제
      캡션 기준으로 임시로 채운 것이라, 실제 결과 보고 이상한 매칭/과도한 미매칭 있으면
      계속 다듬어야 함.
    - **실제 계정으로 첫 실행 결과, 심각한 오매칭 발견+수정:** 자사 트위터 32건 중 21건이
      "원피스 액션 피규어" 하나로, 인스타 30건 중 29건이 "디지몬 어드벤처 GEM" 하나로
      통째로 뭉쳐버림. 원인 2가지 — (1) **"26년", "7월" 같은 날짜 토큰이 키워드로 잡혀서**,
      거의 모든 게시물에 공통으로 등장하는 날짜 하나 때문에 서로 다른 상품들이 사슬처럼
      전부 연결돼버림(Union-Find 특성상 A-B, B-C만 겹쳐도 A-B-C가 통째로 한 그룹이 됨).
      (2) **`GENERIC_KEYWORDS`가 정확히 일치(exact match)하는 단어만 걸러내서**, 한국어는
      띄어쓰기 없이 붙여쓰는 경우가 많아 "예약시작"처럼 상용구가 포함된 복합 단어는 못 걸러냄.
      → 숫자가 포함된 토큰은 전부 키워드에서 제외, `GENERIC_KEYWORDS` 필터링을 부분 포함
      (substring) 방식으로 변경, "세컨드"(재판/리뉴얼 버전을 뜻하는 상용구)도 목록에 추가.
      `verify-mock.js`에 날짜 공유 회귀 테스트 추가해서 재발 방지.
    - **표 구조를 사용자가 예전에 수동으로 쓰던 엑셀 형식에 맞춰 통째로 다시 짬.** 기존엔
      자사 요약 블록 따로, 경쟁사 요약 블록 따로 나열하는 방식이었는데, **한 행 = 한 상품**으로
      자사(PW)/경쟁사(BH) 값을 나란히 놓고 차이·배수·게시 시각·시각차이·결과(우세/약세/경합)까지
      한 번에 보는 방식으로 교체. 사용자가 준 예전 참고표를 역산해서 규칙 확정:
      - **차이/배수**: `PW-BH` 차이값 + `(배수)` — PW가 크면 `PW/BH`, BH가 크면 `-(BH/PW)`
        (예: 336/14=24배). `formatDiffWithMultiplier()`
      - **시각차이**: PW/BH 중 각자 제일 이른 게시 시각(KST) 차이를 분 단위로. **버그 수정:**
        표시가 "시:분"만 있어서 PW/BH가 하루 넘게 차이나는 날 각각 게시한 경우
        (예: "1496분") 왜 저렇게 크게 나오는지 헷갈렸음 — `formatKstTime()`이 날짜("M/D")도
        같이 표시하도록 수정 (`verify-mock.js`에 회귀 테스트 추가)
      - **결과**: 표시 지표(리트윗+좋아요 또는 좋아요+댓글) 둘 다 자사가 크면 우세, 둘 다
        작으면 약세, 엇갈리면 경합 — 참고표 12개 행 전부 이 규칙과 정확히 일치함을 확인
      - **IP/시리즈 분리**: `KNOWN_PRODUCT_LINES`(룩업/GEM/메가캣/테노히라/컬렉션 등 알려진
        상품 라인명) 목록에서 매칭되는 걸 "시리즈"로 빼고 나머지를 "IP"로. **새로운 라인명이
        나오면 이 목록에 계속 추가해야 함** — 목록에 없으면 시리즈 칸이 빈 채로 나감
      - 맨 아래 "계" 합계 행도 추가(시각/결과는 상품마다 달라서 합계 의미 없어 비워둠)
    - **버그: "원피스" 하나로 룩업/스케일/컬렉션/메가캣 등 완전히 다른 상품 라인이 다 뭉쳐서
      나옴.** 키워드만 보고 매칭해서 "원피스"라는 IP 공통점만으로 서로 다른 라인이 합쳐짐.
      → `detectProductLine()`으로 게시물마다 상품 라인을 감지해서, **감지된 라인이 정확히
      같아야만**(둘 다 미감지인 경우도 "같음") 매칭되도록 강화. 처음엔 "둘 중 하나만 미감지면
      통과"로 느슨하게 했다가 라인 미감지 게시물이 다리 역할을 해서 다른 라인끼리 다시
      전이적으로 합쳐지는 문제가 재발 — 완전히 엄격한 동일 비교로 교체.
      - **별칭 처리**: 자사는 "스케일"(예: "원피스 스케일 피규어"), 경쟁사는 "POP"/"P.O.P"라고
        부르는 같은 라인이 있어서 `LINE_ALIASES`로 정식 명칭(POP) 통일
      - **트레이드오프**: 라인 조건을 엄격하게 하니 매칭 안 됨 개수가 늘었음(트위터 기준
        7+13건 → 12+18건) — 한쪽만 라인명을 언급하면 매칭이 안 되기 때문. 잘못 합쳐지는
        것보다 투명하게 "매칭 안 됨"으로 보이는 쪽이 안전하다고 판단(수동 매칭 기능으로
        바로 보완 가능하니까)
      - `verify-mock.js`에 라인 분리 + 별칭 통일 회귀 테스트 추가
    - **실제 데이터로 재검증했더니 트위터 32건 중 21~23건, 인스타 30건 중 29~30건이 여전히
      하나로 뭉쳐짐 (사용자가 캐시 파일을 직접 공유해줘서 실제 원본 캡션으로 정밀 진단).**
      원인 3가지, 전부 "본문 전체"를 매칭 대상으로 넓히면서 새로 드러난 문제들:
      1. **`KNOWN_PRODUCT_LINES`(룩업/GEM/컬렉션 등)가 매칭 제외 목록엔 없었음** — 시리즈
         분리용으로만 쓰고 있어서 "컬렉션" 같은 라인명 하나로 완전히 다른 프랜차이즈 여러 개가
         엮였음 → `extractKeywords()`가 `GENERIC_KEYWORDS`와 `KNOWN_PRODUCT_LINES`를 같이 제외
      2. **naver.com 링크의 16진수 해시 조각이 우연히 겹침** — 숫자만 지우고 남은 "ca","ef","db"
         같은 2~3글자 조각이 알파벳 6개(a~f)뿐인 16진수라 무관한 게시물끼리도 우연히 겹쳐서
         전부 연결됨 → 낱말 단위 제외가 아니라 **naver.com/http/순수 16진수 줄을 통째로
         제거**하는 `stripUrlNoise()` 추가
      3. **"[예약시작]", "[채색원형 최초공개]" 같은 브라켓 태그 문구가 여러 게시물에 반복
         등장해서 그 문구 자체로 무관한 상품들이 연결됨** → 브라켓 통째로 제거. 인스타는
         "구매는 프로필 링크를 참고해 주세요!" 같은 반복 CTA 문구도 같은 문제라 제외 목록에 추가
      - **근본적으로 판단 기준도 강화**: 키워드 **1개만 겹쳐도 매칭** → 제외 목록에 없는 흔한
        단어 하나(예: "SET", "버전")가 우연히 겹치는 것만으로도 매칭 사고가 반복됨 →
        **2개 이상 겹쳐야 매칭**(`MIN_SHARED_KEYWORDS`)으로 변경
      - 재검증 결과: 트위터 17개 상품으로 정확히 분리(매칭 안 됨 자사 5/경쟁사 6, 원래
        11/25였음), 인스타 18개 상품으로 분리(매칭 안 됨 자사 1/경쟁사 4, 원래 4/6이었지만
        매칭된 것도 대부분 하나로 뭉쳐있었음) — 실제 캡션 데이터로 직접 검증 완료, 이상한
        오매칭 사례 안 보임
      - `verify-mock.js`에 이번에 찾은 3가지 오매칭 패턴(브라켓/URL해시/흔한단어 1개) 회귀
        테스트 추가
- `excel.js` — `saveReportToExcel()`: 취합 결과를 엑셀에 저장. **기존 시트는 절대 삭제/덮어쓰지
  않고 새 시트만 추가**(히스토리 누적), 같은 기간으로 재실행해도 시트 이름이 자동으로 분리됨.
  쓰기는 임시 파일 → 원자적 rename 방식이라 도중에 프로세스가 죽어도 기존 파일은 안전.
  파싱 실패 건수가 있으면 시트에 ⚠ 경고 문구로 표시. **저장 1회당 시트 2개 생성**: 요약 시트
  (`YYYYMMDD_YYYYMMDD`) + 상품별 비교 시트(`YYYYMMDD_YYYYMMDD-상품별`, 매칭 안 된 게시물 목록 포함).
- `run.js` — 위 두 로직 + `twitter.js`/`instagram.js`를 잇는 전체 실행 스크립트. `CONFIG`에
  원칙적으로 운용하는 자사/경쟁사 계정(X: megahousestore/megahouse_BH, 인스타:
  megahouse_store/megahouse_korea_dt_bh) 고정 반영됨. **실제로 한 번 실행해서 요약 비교표까지는
  정상 동작 확인됨.** 상품별 비교 기능은 이번에 추가된 것이라 아직 실제 데이터로 재실행 전.
  - 수집한 원본 게시물을 `reports/_last-collection.json`에 캐시로 저장함 (수집은 몇 분 걸리는데
    리포트 포맷만 고칠 때마다 매번 재수집시키는 게 비효율적이라는 피드백 받고 추가).
- `rebuild-report.js` — 위 캐시를 다시 읽어서 취합+엑셀+HTML 저장만 재실행 (브라우저/세션
  필요 없어서 몇 초 안에 끝남). `aggregate.js`/`excel.js`/`html-report.js` 포맷만 고쳤을 때
  이걸로 재생성, 계정/기간을 바꿔서 새로 수집해야 하면 `run.js`를 다시 돌려야 함.
- `html-report.js` — **엑셀은 보기 불편하다는 피드백으로 추가.** 사용자의 이사님이 쓰던
  판매 비교 리포트(완전히 다른 판매량/매출 도구, HTML) 스타일(상단 요약 카드 + 순위
  테이블 + 1~3위 강조 + 결과 배지)을 그대로 참고해서 우리 상품별 비교에 입힘.
  `saveHtmlReport()`가 `reports/sns-report.html`로 저장 — **히스토리 누적 안 하고 매번
  덮어씀**(과거 데이터 보존은 엑셀이 계속 담당, 이 HTML은 최신 결과를 보기 좋게 보는
  용도). `run.js`/`rebuild-report.js` 둘 다에서 엑셀과 함께 자동 생성됨. 실제 캐시 데이터로
  생성해서 Playwright로 스크린샷 찍어 직접 확인함(이모지가 IP명 앞에 지저분하게 남는 것도
  발견해서 `splitIpAndLine()`에 앞뒤 이모지/기호 제거 추가).
  - **게시물 임베드 토글 추가**: 각 상품 행의 "▶ 보기" 버튼을 누르면 그 상품에 속한 PW/BH
    게시물들을 2열(PW 왼쪽/BH 오른쪽)로 트위터/인스타 공식 oEmbed 마크업으로 렌더링,
    다시 누르면 접힘. 성능 때문에 각 위젯 스크립트(twitter widgets.js / instagram embed.js)
    호출은 **처음 열 때만** 1번 실행(`row.dataset.rendered`로 체크) — 모든 상품을 페이지
    로드 시점에 한꺼번에 처리하지 않음. **인터넷 연결된 브라우저에서 열어야 실제 카드로
    보임** — Claude Code 웹 실행 환경(원격 샌드박스)은 네트워크가 막혀있어 여기서는
    폴백(링크 텍스트)까지만 확인 가능, 실제 임베드 렌더링은 사용자가 직접 확인 필요.
    토글 열기/닫기 자체는 Playwright로 동작 확인함.
- `manual-matches.json` — **자동 매칭이 놓친 게시물을 수동으로 지정하는 목록 (사용자와
  상의해서 결정한 방식).** "자동 분류 → 미매칭만 사람이 지시" 흐름: 자동 매칭 후 남은
  "매칭 안 됨" 목록에 `PW #1`, `BH #1`처럼 번호가 붙어있는데(엑셀/HTML 둘 다), 사용자가
  채팅으로 "PW 3번 BH 1번 매칭해줘"라고만 말하면 Claude가 그 번호에 해당하는 실제
  링크를 찾아서 이 파일에 추가하고 `rebuild-report.js`로 재생성 — **사용자가 직접 JSON을
  안 만져도 됨.** 형식:
  ```json
  { "twitter": [{ "pw": ["링크1"], "bh": ["링크2"], "label": "표시할 상품명(선택)" }], "instagram": [] }
  ```
  `aggregate.js`의 `extractManualMatches()`가 자동 매칭보다 먼저 이 목록을 확정하고, 지정된
  게시물은 자동 매칭 대상 풀에서 빠짐. `label`을 주면 자동 정리(상용구 제거 등) 없이 그대로
  IP명으로 씀. 실제 캐시 데이터로 테스트해서 정상 동작(매칭 안 됨 개수 정확히 줄어듦) 확인함.
- `verify-mock.js` — 브라우저/로그인 세션 없이 `aggregate.js`+`excel.js` 내부 로직만 모킹
  데이터로 검증하는 스크립트 (`npm run verify`). 트위터.js/instagram.js가 실제로 반환할 형태를
  흉내낸 값으로 합계·비율 계산, 파싱 실패 집계, 엑셀 히스토리 누적(재실행 시 덮어쓰기 안 됨)까지 확인함.
- `test-collect.js` — `twitter.js`/`instagram.js`를 계정 하나만 빠르게 테스트하는 CLI
  (`node test-collect.js twitter <계정> <시작일> <종료일>`). `headless:false` 고정이라 브라우저
  창을 직접 보면서 확인 가능. `run.js` 전체 CONFIG 채우기 전 사전 점검용.

## 다음에 재개할 때 할 일

### 🔴 막혀있는 부분 (사용자 준비 필요 — 코드로 미리 처리 불가)
- [x] 인스타그램/X 로그인 세션 파일 생성 — `x-session.json`(auth_token/ct0/guest_id 등 5개),
  `instagram-session.json`(sessionid/csrftoken/ds_user_id 3개) 사용자 로컬에 생성 완료
  (`table-cookies-to-storage-state.js`로 변환, F12 Application 탭 쿠키 표 복사 방식 채택)
- [x] `twitter.js` — `megahousestore` 실제 계정 2026-07-01~07-02로 검증 완료. **원본 49건 →
  필터링 후 32건, 사용자가 기억하는 실제 게시 수(30건 이상)와 일치 확인.**
  - 버그 발견+수정 히스토리 (실제 계정 테스트 전엔 발견 불가능했던 것들):
    1. **리트윗/고정 게시물**: `<time>`이 리트윗 시점이 아니라 원본 게시물의 원래 작성 날짜를
       보여줘서, 최근 리트윗한 옛날 글이 섞이면 스크롤 시작 전에 "범위 벗어남"으로 착각해 즉시
       멈춤(3건에서 멈춤). → `[data-testid="socialContext"]` 라벨 붙은 건 수집 자체에서 제외
       (좋아요/RT수도 이 계정 성과가 아니라 원본 게시물 것이라 집계 관점에서도 제외가 맞음).
    2. 그래도 조기 중단 (원본 6건). 진단 로그로 원인 게시물 특정 → **"끌올"**(인용/멘션으로
       6/25 옛날 글을 오늘 다시 딸려오게 하는 방식)이 원인. 리트윗→인용→멘션처럼 패턴이 계속
       늘어날 걸로 보여서 패턴별 개별 예외처리 대신 **판단 기준 자체를 변경**: 오래된 게시물
       1건만으론 안 멈추고, 새로 발견된 순서(피드 순서) 기준 **연속 3건**이 전부 범위 시작일
       이전이어야 진짜로 과거로 넘어갔다고 판단 → 재검증해서 32건으로 정상 수집 확인
- [x] `instagram.js` — `megahouse_store` 실제 계정 2026-07-01~07-02로 검증 완료. 링크 36개 →
  파싱 성공 36건 → 필터링 후 30건. **좋아요/댓글 좌표 파싱(x>700, y 400~580)도 사용자가 실제
  화면과 직접 대조해서 정확히 일치 확인 — 원래 가장 못 믿었던 부분이 검증 통과함.**
  - 버그 발견+수정 (좌표 파싱과는 무관한 별개 문제): 7/1~7/2만 요청해도 프로필 그리드를 5월까지
    계속 스크롤하는 문제 발견. `twitter.js`와 달리 1단계(그리드에서 링크만 모으는 단계)엔 날짜
    판단이 전혀 없어서 "스크롤해도 새 링크 안 늘어남" 조건(=프로필 전체)까지 무조건 다 모으고,
    날짜 필터링은 2단계(게시물 개별 파싱)에서야 적용되는 구조였음 → 그리드 스크롤 중 새로
    발견한 링크 중 가장 나중 것(가장 오래됐을 가능성이 큰 것) 하나를 별도 탭으로 슬쩍 열어
    날짜만 확인(`peekPostDate()`, 메인 페이지 스크롤 위치는 안 건드림), 범위 시작일보다
    오래되면 스크롤 중단하도록 수정. 좋아요/댓글 좌표 파싱(2단계) 로직 자체는 안 건드리고
    그대로 뒀고, 재검증에서 정상 동작 확인됨.

**→ `twitter.js`, `instagram.js` 둘 다 실제 계정으로 검증 완료. 스크래퍼 초안 단계 마무리.**

## 원칙적으로 운용하는 계정 (고정 세팅, `run.js`의 `CONFIG`에 반영됨)
- X: 자사 `megahousestore` / 경쟁사 `megahouse_BH`
- 인스타그램: 자사 `megahouse_store` / 경쟁사 `megahouse_korea_dt_bh`
- 예외적인 계정을 수집해야 할 땐 이 CONFIG를 건드리지 말고 그때그때 별도 요청하기로 함

## 다음 단계 (스크래퍼 검증 완료 후)
- [ ] `run.js`를 실제로 실행해서 전체 파이프라인(수집 → 취합 → 엑셀 저장) 검증 — 지금까지는
  `test-collect.js`로 계정 하나씩만 테스트했고, `aggregate.js`/`excel.js`는 모킹 데이터로만
  검증됨. 실제 수집 결과로 비교표가 잘 나오는지, 리트윗/고정 게시물 제외 후 지표가 말이
  되는지 최종 확인 필요 (`node run.js`, 매번 `startDate`/`endDate`만 그 달 기간에 맞게 수정)
- [ ] 네이버 커머스API 신청 가능 여부 확인 (2단계, 후속)
- [x] 네이버 커머스API센터 개발업체계정 신청이 "개발업체계정 권한을 보유하지 않은 회원" 에러로
  막힘(통합매니저 계정으로 재시도해도 동일) — 리틀리처럼 커머스솔루션마켓 경유하는 방법도
  검토했으나, 그건 여러 판매자가 설치하는 공개 솔루션을 만들어 심사받는 절차라 오히려 더 무거워
  후보에서 제외. **API 승인 자체를 기다리지 않는 방향으로 전환**(아래 참고)

## 네이버 재고/가격 — API 없이 공개 페이지에서 읽기 (`naver-stock.js`)
- 배경: 커머스API센터 개발업체계정 신청이 막혀서 대안을 찾던 중, 사용자가 이미 쓰던 크롬 확장
  "Naver Store Stock Overlay"(재고를 상품 이미지 위에 뱃지로 표시해주는 확장) 소스 코드를
  직접 공유해줘서 분석함
- **핵심 발견**: 이 확장은 숨겨진/사설 API를 호출하는 게 아니라, `smartstore.naver.com` /
  `brand.naver.com` **공개 상품 페이지 자체에 이미 박혀있는 데이터**를 읽는 것뿐이었음:
  1. 최초 페이지 로드 시 `<script>`에 `window.__PRELOADED_STATE__ = {...}` 형태로 재고/가격/
     리뷰 등 상품 데이터 전체가 박혀있음(Next.js SSR 하이드레이션용, 원래 공개 정보)
  2. SPA 네비게이션(예: search.shopping.naver.com) 시엔 `self.__next_f.push([n,"..."])`로
     스트리밍되는 React Server Components flight 데이터 안에 같은 정보가 있음
  3. 상품 목록 스크롤 시 fetch/XHR 응답도 같은 데이터를 담고 있음(확장은 fetch/XHR을 패치해서
     가로챔)
  → **로그인/세션/API 승인 전혀 필요 없음.** PW든 BH든 공개 상품 페이지만 열면 아무나 받는
  데이터라 경쟁사 재고도 동일하게 조회 가능
- [x] `naver-stock.js` 작성 — 확장의 추출 로직(상품ID/재고/품절/가격 키 휴리스틱, JS 객체
  리터럴의 `undefined`/`NaN` 같은 비-JSON 토큰을 안전하게 치환하는 `sanitizeJsonLiteral`,
  균형잡힌 중괄호 블록을 잘라내는 `extractAssignedJson`)을 그대로 Node로 이식.
  `getProductStock(url)`: 상품/스토어 URL 하나로 재고·가격·이름을 반환.
- [x] `npm run verify`에 모킹 HTML(합성 `__PRELOADED_STATE__` + `__next_f` 조각, `undefined`
  토큰 섞은 케이스 포함)로 추출 로직 검증 추가, 통과 확인
- 1차 버전은 로그인 세션이 필요 없다는 점을 이용해 순수 `fetch()` GET으로 구현했었는데,
  샌드박스에서 `curl`로 접속 시 네이버 엣지(`server: nfront`)가 정적 429 에러 페이지를 반환.
  처음엔 "데이터센터 IP 차단"으로 추정했으나, **사용자가 본인 로컬(주거용 IP) 컴퓨터에서
  `node naver-stock.js <URL>` 실행해봐도 똑같이 HTTP 429**가 나서 그 가설이 틀렸음을 확인 →
  IP 평판이 아니라 **네이버 프론트가 TLS/브라우저 지문(fingerprint)으로 봇을 거르는 것**으로
  판단(Node의 fetch/TLS 스택은 실제 크로미움과 지문이 달라서 걸림). twitter.js/instagram.js가
  처음부터 Playwright로 간 것과 같은 이유 → **`naver-stock.js`도 순수 fetch 대신 Playwright
  (실제 크로미움)로 전환**, 로그인 세션은 여전히 불필요(공개 페이지라 새 컨텍스트로 그냥 열면 됨)
- **✅ 사용자 로컬 컴퓨터에서 실제 검증 성공** — `node naver-stock.js <상품 URL>` 1건 성공,
  `node naver-stock.js https://brand.naver.com/megahouse/` (메인 페이지)로 **81개 상품 재고/
  가격을 한 번에 수집** 성공. 로그인 세션 전혀 없이, 스토어 URL 하나로 전체 카탈로그가 다 나옴
- 실제 데이터 확인해보니 사용자가 짚어준 대로: 재고 숫자가 9000~10000대에 몰린 상품들은
  예약판매 중이라 판매자가 임의로 걸어둔 한도(예: 10000개)에서 줄어든 만큼이 실제 예약
  판매량이고, 82/24/15/6처럼 낮은 숫자는 이미 발매(입고)된 상품의 진짜 물리 재고임
  (메가하우스는 피규어 브랜드라 예약→발매까지 6개월~1년 걸려서 이 두 단계가 공존함)
- 한계: 재고/가격/판매상태만 나오고 실제 매출액·주문건수는 안 나옴 — 정확한 매출은 여전히
  커머스API/엑셀 내보내기가 필요함. 재고는 "현재 시점"만 조회되므로 기간별 판매량을 추정하려면
  주기적 스냅샷을 쌓아서 재고 감소량으로 역산해야 함(과거 소급 불가)

### 재고 스냅샷 저장/비교/리포트 반영
- 스냅샷 주기: 스케줄 자동화 아님, **사용자가 원할 때마다 수동 실행**(기존 "매달 직접 실행"
  방침과 동일선상) — 자동화가 필요해지면 나중에 별도로 요청하기로 함
- [x] `naver-stock-snapshot.js` 작성 — `naver-stock.js`로 지금 재고를 긁어서
  `reports/_stock-history.json`에 스냅샷 한 건 append(누적, 덮어쓰기 아님). PW는
  `https://brand.naver.com/megahouse`, BH는 `https://brand.naver.com/megahousemall`로 채워짐
  (URL 없는 스토어는 경고 로그만 찍고 건너뛰도록 짜뒀는데 이제 둘 다 채워져서 안 씀)
- [x] `stock-report.js` 작성 — 스냅샷 히스토리를 최신 vs 직전 스냅샷으로 비교해서
  `buildStockComparison()`이 store(PW/BH)별 상품 재고 변화량(`stockDelta`, 양수=판매 추정/
  음수=재입고 또는 한도 재설정)을 계산, `renderStockSectionHtml()`이 HTML 섹션으로 렌더링.
  스냅샷이 1개뿐이면 비교 없이 현재값만 보여줌(다음 수집부터 비교 시작)
- [x] SNS 비교 화면엔 욱여넣지 않고(방침 유지), `html-report.js`의 `buildHtmlReport(report,
  stockComparison)`가 기존 SNS 섹션들 다음(`.foot` 아래) **"📦 재고 스냅샷" 섹션을 문서 맨
  아래에 추가로 붙임** — 같은 페이지 안에서 스크롤해야 보이는 위치. 히스토리 파일이 없으면
  `renderStockSectionHtml(null)`이 빈 문자열을 반환해서 섹션 자체가 생략됨(기존 리포트에
  영향 없음)
- [x] `run.js`/`rebuild-report.js` 둘 다 `reports/_stock-history.json`이 있으면 자동으로
  읽어서 붙이도록 연결
- [x] `npm run verify`에 스냅샷 1개/2개 케이스(판매 추정·신규 상품·렌더링) 모킹 테스트 추가,
  통과 확인 + 가짜 히스토리로 실제 HTML 생성해서 Playwright 스크린샷으로 레이아웃 확인(정렬,
  색상, 판매추정/재입고 라벨 다 정상)

### SNS 순위 ↔ 매출 순위 대조 — 시간 단위 정밀 매칭 대신 순위표 병기로 단순화
- 사용자 아이디어: "시간까지 세세하게 할 필요 없다 — 어차피 SNS 실적표가 이미 실적 잘 나온
  순으로 줄 세워져 있으니(순위 컬럼), 재고 스냅샷 표에도 판매 추정치(10000에서 제일 많이
  깎인 순) 기준 순위를 매겨서 같이 두면, 두 순위를 사람이 눈으로 대조해서 SNS가 매출에
  얼마나 영향을 줬는지 대략 가늠할 수 있다" → 게시 시각 기준 정밀 매칭(더 복잡하고 정확도도
  애매함)보다 훨씬 단순하고 실용적인 접근이라 이 방향으로 확정
- [x] `stock-report.js`의 재고 스냅샷 표에 **"매출순위"** 컬럼 추가 — 판매 추정치(stockDelta)
  기준 내림차순, SNS 비교표와 같은 스타일(🥇🥈🥉 + 숫자)로 통일해서 두 표를 눈으로 대조하기
  쉽게 함
- [x] 비교 불가한(직전 스냅샷에 없던) 신규 상품은 판매 추정치 자체가 없어서 순위에 끼워넣지
  않고 "-"로 표시 — 근거 없는 순위 숫자를 보여주지 않기 위함
- [x] `npm run verify`에 매출순위 컬럼/1위 메달 검증 추가 + 가짜 히스토리로 스크린샷 재확인

### 정정: 매출순위를 별도 섹션이 아니라 SNS 표 우측 컬럼으로 이동
- 위에서 만든 걸 사용자가 확인해보니 의도한 것과 달랐음 — "표 같은 곳에서 보게 해라",
  "SNS 표 우측에 삽입해라"를 여러 번 얘기했는데 계속 스크롤해서 내려가야 하는 별도 섹션으로
  만들어서 반복 설명하게 만듦. 정확한 요구사항: **SNS 비교표 자체의 우측 끝에 매출 컬럼을
  추가**하고, 표 폭(페이지 레이아웃)은 고정값 그대로 유지한 채 **가로 스크롤로만** 그 우측
  컬럼이 보이게 할 것 — 위아래로 왔다갔다하며 대조하게 하지 말 것
- [x] 기존 `renderStockSectionHtml`/`STOCK_SECTION_STYLE`(별도 섹션 렌더러) 삭제, 대신
  `stock-report.js`에 `rankStockProducts()`(순위 계산만 분리)와 `findStockMatch(ip, line,
  rankedProducts)`(SNS 상품과 재고 상품을 이름으로 근사 매칭)를 export
- [x] `html-report.js`의 `renderPlatformSection`이 각 상품 행마다 PW/BH 재고를 매칭해서
  **"📦PW 매출"/"📦BH 매출" 컬럼을 기존 표의 맨 오른쪽에 추가**(별도 테이블 아님). 페이지/카드
  영역 폭은 그대로, `.table-wrap{overflow-x:auto}`가 이미 있어서 컬럼이 늘어난 만큼 자동으로
  가로 스크롤됨(추가 CSS 변경 불필요)
- 매칭 로직에서 실제 버그 하나 발견+수정: ip가 "은혼 카무이"처럼 여러 단어일 때 통짜
  substring으로 비교하면 재고 상품명이 "은혼 GEM 카무이"처럼 중간에 다른 말이 끼어 있는
  경우 매칭 실패함 → ip를 공백 기준으로 토큰화해서 모든 토큰이 상품명에 들어있는지(AND
  조건)로 바꿔서 해결. `npm run verify`에 실제 report 객체로 `buildHtmlReport`를 호출하는
  통합 테스트를 추가했다가 이 버그를 바로 잡아냄(모킹 테스트의 가치를 보여준 사례)
- [x] 매칭 안 되는 상품은 "-", 비교 스냅샷이 아직 없는 신규 상품은 "신규"로 표시. 마우스
  올리면 실제 매칭된 재고 상품명이 툴팁으로 보여서 매칭이 맞는지 바로 확인 가능
- [x] `npm run verify` 통과 + 실제 캐시 데이터 + 가짜 재고로 Playwright 스크린샷 찍어서
  가로 스크롤해야 우측 매출 컬럼이 보이는 것, 페이지 폭 안 변한 것 둘 다 확인

### 재정정: 하단 독립 재고 섹션은 지운 게 아니라 실수 — 복구함
- 위에서 "기존 renderStockSectionHtml/STOCK_SECTION_STYLE 삭제"라고 적은 건 사용자가 요청한
  적 없는데 내가 멋대로 판단해서 지운 것이었음. 사용자는 "SNS 표 우측에도 추가해달라"고 했지
  "기존 하단 독립 섹션을 없애라"고 한 적이 없는데, 요청을 오해해서 있던 기능을 제거해버림 —
  명백한 실수였고 사용자가 바로 지적함
- [x] `stock-report.js`에 `renderStoreTable`/`renderStockSectionHtml`/`STOCK_SECTION_STYLE`을
  그대로 복구(git 이전 커밋에서 가져옴), 내부 순위 계산만 새로 만든 `rankStockProducts()`를
  재사용하도록 정리(중복 로직 제거). `html-report.js`에 다시 연결해서 **SNS 표 우측 매출
  컬럼(새 기능)과 하단 독립 재고 섹션(원래 있던 기능) 둘 다 공존**하게 함
- [x] `npm run verify`에 "둘 다 있어야 함" 회귀 테스트 추가(하나라도 없으면 실패) — 이번 같은
  실수(요청 안 받은 삭제)가 재발해도 테스트가 바로 잡아내도록
- **교훈**: 새 요청을 반영할 때 "이미 있던 것도 같이 필요한지"를 먼저 확인하지 않고 임의로
  지우거나 바꾸지 말 것 — 명시적으로 지우라는 말이 없으면 있던 기능은 그대로 둔 채로 추가만
  할 것

### BH 스토어 URL 문제 3연타 — 접속 안 됨 → 로그인 게이트 → 파워링크 랜딩 링크로 해결
- 1차: `brand.naver.com/megahousemall`(BH) 접속 자체가 안 됨 → `smartstore.naver.com/megahousemall`로 교체
- 2차: 교체 후 PW는 로그인 없이 84건 수집되는데 BH만 수집 시점에 로그인창이 뜨고 0건 수집됨.
  처음엔 "연속 요청이라 봇 탐지로 걸리는 건가" 의심했는데, 사용자가 시크릿창에서 아무 요청
  없이 그냥 URL 직접 방문해도 로그인창이 뜨는 걸 확인해줌 → 우리 스크립트 문제가 아니라
  **BH 스토어 자체에 로그인 게이트가 걸려있는 것**(성인인증 필요한 피규어가 있어서로 추정)
  으로 원인 확정
- 3차: 사용자가 "파워링크(광고) 클릭 시 붙는 URL은 로그인 없이 뚫린다"고 알려줬는데, 처음엔
  안 해보고 "그건 그냥 광고 추적 파라미터라 안 될 것"이라고 넘겨짚었다가 사용자에게 지적당함
  → 실제로 테스트해보니:
  - `?NaPm=<진짜 광고클릭 해시>` 붙은 링크: 로그인 없이 통과
  - `?NaPm=1`(가짜 값): 로그인창 그대로 뜸 → 해시가 실제로 서버에서 검증되는 토큰이라
    확인됨(파라미터 존재 자체로 스킵되는 게 아님) → 이 방식은 광고클릭마다 발급되는 값이라
    고정해서 쓰면 나중에 만료될 수 있고, 매번 새로 받으려면 BH 광고를 자동으로 클릭해야
    해서 광고비를 축내는 셀이라 부적절
  - 사용자가 찾아낸 대안: 파워링크가 실제로 연결하는 랜딩 링크
    `https://mkt.shopping.naver.com/link/681dc55ab84505306af8828b` — 광고클릭 1회성 토큰이
    아니라 광고주가 등록한 고정 랜딩 URL이라 안정적. 실제로 로컬에서 로그인 없이 68건
    수집 확인됨
- [x] `naver-stock-snapshot.js`의 `STORES` BH 항목을 이 랜딩 링크로 교체
- 이 세 번의 시도 동안 이 샌드박스에서는 naver.com 계열 도메인 자체가 네트워크 레벨에서
  막혀있어서(`ERR_CONNECTION_RESET`) 직접 테스트가 전혀 안 됨 — 사용자의 로컬 실행 결과로만
  검증 가능했던 상황. 안 해보고 넘겨짚었던 부분은 명시적으로 인정하고 넘어감

### 버그: 직전 스냅샷 수집 실패(0건)했던 store가 다음 비교에서 전체 "신규"로 잘못 표시됨
- 위 URL 문제 때문에 BH가 한 번 0건으로 스냅샷 저장된 적이 있었음. 그 다음 정상 수집(68건)
  결과를 리포트에 반영했더니 BH 칸이 전부 "신규"로만 나옴 — 사용자가 "처음 수집해도 리포트에
  반영되게 해줘야지"라고 지적
- 원인: `buildStockComparison`이 "직전 스냅샷이 존재하는지"를 **전체 히스토리 기준**(2개
  이상 스냅샷 존재)으로만 판단하고 있어서, BH의 직전 스냅샷이 실제로는 빈 배열(수집 실패)
  이었다는 걸 구분하지 못함 → 빈 배열과 비교하니 모든 상품이 "이전 데이터에 없음"으로 잡혀서
  진짜 신규가 아닌데도 전부 신규로 표시됨. PW는 직전 스냅샷에 정상 데이터가 있어서 문제 없었음
- [x] `buildStockComparison`(`stock-report.js`)이 store별로 `storeComparable`(직전 스냅샷의
  해당 store 배열이 비어있지 않았는지)을 따로 계산해서 결과에 포함하도록 수정
- [x] `html-report.js`(SNS 표 우측 매출 컬럼)와 `renderStockSectionHtml`(하단 독립 섹션)
  둘 다, 전체 `previousTakenAt` 대신 store별 `storeComparable`을 기준으로 비교 가능 여부를
  판단하게 수정. 비교 불가한 store는 "신규"가 아니라 **현재 재고 수치를 그대로** 보여줌
  (진짜 신규 상품 표시는 store 자체는 비교 가능한데 그 상품만 직전에 없었던 경우에만 사용)
- [x] `npm run verify`에 이 시나리오(직전 스냅샷에서 한 store만 0건) 회귀 테스트 추가

### HTML 리포트 파일명에 생성 시각 추가 + 예전 파일 old/로 자동 이동
- `reports/sns-report.html`이 매번 같은 이름으로 덮어써지던 걸, 사용자가 "파일명에 현재시각
  추가하고, 새로 뽑으면 이전 건 old 폴더로 자동 이동"해달라고 요청
- 엑셀(`sns-report.xlsx`)은 이미 재실행해도 같은 파일 안에 시트를 쌓아서 히스토리를 관리하는
  기존 기능(테스트로 보장됨)이 있어서, 이것까지 타임스탬프 파일명으로 바꾸면 "한 파일에 시트
  누적" 방식이 무의미해짐 — 범위를 확인한 후 **HTML만 적용, 엑셀은 그대로 유지**로 결정
- [x] `report-archive.js` 신설 — `archiveAndGetPath(dir, baseName, ext)`: dir 안의
  `baseName*.ext` 패턴 파일(예전 고정이름 `sns-report.html` + 예전 타임스탬프 파일 전부)을
  `dir/old/`로 옮기고, 이번에 쓸 `sns-report_YYYYMMDD_HHMM.html`(KST, 콜론 없음 — Windows
  파일명 제약) 경로를 반환
- [x] `run.js`/`rebuild-report.js`가 HTML 저장 직전에 이 함수를 호출해서 경로를 받아 쓰도록 수정
- [x] `npm run verify`에 회귀 테스트 추가(고정이름+타임스탬프 파일 둘 다 old/로 이동되는지,
  엑셀 파일은 안 건드리는지) + 실제 캐시로 `node rebuild-report.js` 돌려서 old/ 폴더 생성과
  파일 이동까지 실제로 확인

### 매칭 안 됨 목록 이중 스크롤 제거 + BH도 PW처럼 역산 추정치로 형식 통일
- 사용자 피드백 2건: (1) "매칭 안 됨" 목록은 `<details>`로 이미 접혀있는데, 펼쳤을 때도
  `.unmatched-list{max-height:220px;overflow-y:auto}`로 또 스크롤해야 해서 불편 (2) BH가
  처음 수집됐을 때 PW는 "역산해서 몇 개 팔렸는지"로 보여주는데 BH는 그냥 "4,977개"처럼 raw
  재고 숫자로만 나와서 양식이 다름 — "우리(PW)처럼 똑같이 역산해서 보여줘"
- [x] `.unmatched-list`의 `max-height`/`overflow-y` 제거 — 이중 스크롤 없이 펼친 만큼 그대로 다 보임
- [x] 실제 이전 스냅샷이 없는 상품(BH 첫 유효 수집, 또는 개별 신규 상품)은 현재 재고를 가장
  가까운 1000단위로 올린 값을 "초기 판매한도 추정치"(`estimateInitialCap`)로 가정하고 거기서
  역산한 `estimatedDelta`를 계산해서, 실제 delta가 있는 PW와 **동일한 순위+변화량 형식**으로
  표시. 추정치인 경우만 "추정" 문구 + 점선 밑줄(`sm-estimate`) + 툴팁으로 구분해서 정확도
  차이를 숨기지 않음. 이걸로 기존 "신규"(개별 상품) / "현재 재고만 표시"(store 전체) 두 가지
  특수 케이스가 하나의 "추정 표시"로 단순화됨(`rankStockProducts`가 `hasComparison` 파라미터
  없이 `effectiveDelta`—실제 delta 있으면 그거, 없으면 추정 delta—기준으로 통일해서 랭킹)
- [x] `npm run verify` 통과 + 가짜 재고 히스토리로 실제 리포트 생성 + Playwright 스크린샷으로
  "매칭 안 됨" 이중스크롤 사라짐 / BH가 "🥇 (-386 추정)" 형식으로 PW와 동일하게 나오는 것 확인

### 인스타 세션 일주일도 안 돼서 만료 — 반자동 로그인 도우미 스크립트로 번거로움 줄임
- 사용자가 "일주일마다 F12/쿠키표 복사 계속 해야 하냐, 불편하다. 영구적인 방법 없냐, ID/PW
  줘도 상관없다(부계정)"고 문의
- 완전 자동 재로그인(ID/PW 저장 후 스크립트가 자동입력)은 권하지 않기로 함: 메타/X는 자동화된
  로그인 시도(자동입력+비정상 패턴)를 사람 로그인보다 훨씬 더 의심스럽게 보기 때문에, 오히려
  체크포인트/이상로그인 알림으로 이어져 지금보다 세션이 더 자주 끊길 위험이 있음. 지금 방식
  (사용자가 진짜 사람으로 로그인한 세션을 재사용)이 봇 탐지 관점에서 오히려 더 안전한 쪽
- 대신 사용자에게 두 선택지를 제시하고 **반자동 로그인 도우미 스크립트**를 선택받음 — 로그인
  자체는 여전히 사람이 실제 브라우저에서 직접 하되(2단계인증도 그대로 대응 가능), 그 이후의
  번거로운 부분(F12 켜기 → Application/Cookies 표 찾기 → 전체선택/복사 → 텍스트파일로 저장 →
  변환 스크립트 실행)을 없애는 데 집중
- [x] `login-session.js` 신설 — `node login-session.js instagram`(또는 `twitter`) 실행하면
  Playwright가 실제 브라우저 창을 띄워줌 → 그 창에서 로그인 직접 진행 → 터미널에서 엔터 치면
  그 시점 로그인 상태를 `context.storageState()`로 바로 `instagram-session.json`/`x-session.json`
  형식에 맞게 저장. 로그인 성공 여부를 셀렉터로 자동 감지하는 방식은 안 씀(인스타/X UI가 자주
  바뀌고 체크포인트 화면도 다양해서 오탐 위험) — 사람이 직접 "다 됐다"고 확인하는 엔터 방식이
  훨씬 안정적
- 세션 만료 주기 자체는 그대로(며칠~1~2주 단위로 다시 로그인 필요할 수 있음) — 이건 메타/X
  정책 영역이라 코드로 없앨 수 없는 부분. 이번 개선은 "매번 해야 하는 번거로움"만 줂

### `run.js`에서 플랫폼 하나만 골라서 수집 가능하게
- 세션 갱신 도우미(`login-session.js`)로 인스타 세션만 새로 만든 상황에서, 매번 트위터까지
  같이 재수집할 필요 없이 인스타만 다시 돌리고 싶다는 요청
- [x] `node run.js`(둘 다, 기존 기본값 그대로) / `node run.js twitter` / `node run.js instagram`
  로 특정 플랫폼만 골라 수집 가능하게 `process.argv[2]`로 필터링
- 특정 플랫폼만 다시 수집하면 캐시(`_last-collection.json`)에 있던 **다른 플랫폼 데이터가
  사라지면 안 되므로**, 기존 캐시를 불러와서 방금 수집 안 한 플랫폼 항목은 그대로 유지하고
  이번에 수집한 플랫폼 항목만 교체해서 병합(`mergeWithCache`) — 데이터 손실 방지 우선 원칙
- [x] 병합 로직 수동 검증(node -e로 시뮬레이션: 트위터 유지+인스타만 교체되는지 확인) +
  `npm run verify` 통과(모킹 테스트 자체는 영향 없음, run.js는 실제 브라우저 필요해서 기존부터
  모킹 테스트 대상 아님 — PLAN.md 기존 방침과 동일)

### 인스타가 정상 세션인데도 계속 재로그인 요구 — navigator.webdriver 자동화 탐지 흔적 숨김
- 사용자 질문: "세션 왜 자꾸 시크릿모드로 뜨는거야? 자꾸 로그인하라고 뜨잖아"
- 확인해서 답변: Playwright가 띄우는 브라우저 창은 북마크바/확장프로그램/저장된 자동완성이
  하나도 없는 완전 새 프로필이라 겉모습이 시크릿창이랑 비슷해 보이는 것뿐, 실제 시크릿모드는
  아님(로그인 프롬프트 원인과는 무관). 진짜 원인으로 의심되는 건 **`navigator.webdriver`**
  — Playwright/Selenium 등 자동화 브라우저는 이 값이 기본적으로 `true`라서, 메타(인스타)가
  이걸 감지하면 쿠키가 유효해도 "의심스러운 기기"로 판단해 재로그인/체크포인트를 요구할 수
  있음. 트위터(X)는 이 부분에 덜 엄격해서 지금까지 잘 됐던 것으로 보임
- [x] `chromium.launch()`에 `args:['--disable-blink-features=AutomationControlled']` 추가 +
  `context.addInitScript()`로 `navigator.webdriver`를 `undefined`로 덮어씀 — `instagram.js`와
  `login-session.js`(로그인 자체를 하는 브라우저이니 여기서부터 자동화 흔적을 지워야 그 세션이
  "의심스러운 기기"로 낙인 안 찍힘)에 적용. `twitter.js`는 지금 잘 되고 있어서 안 건드림
- ⚠️ 이게 100% 해결책인지는 검증 못 함 — 메타의 탐지는 이 값 하나만 보는 게 아니라 다른
  신호(플러그인 목록, WebGL 지문 등)도 볼 수 있어서, 그래도 재로그인 요구가 계속되면 별도로
  더 봐야 함. 사용자가 실제로 `login-session.js`로 세션 다시 만든 뒤 며칠 지나서 재확인 필요

### 📦 매출 매칭 컬럼 순위 표기 불명확 — "위" 붙여서 명확화
- 사용자 질문: "경쟁사 매출칸에 14 (-386 추정) 이거 대체 뭐 말하는거야?" — 1~3위는 메달
  이모지라 순위인 게 바로 보이는데, 4위부터는 그냥 숫자만 나와서 "14"가 순위인지 다른 값인지
  안 보여 혼란스러웠음
- [x] `stockMatchCell`에서 4위 이상은 `${rank}위`로 표시(1~3위는 메달 그대로 유지) —
  "14위 (-386 추정)"처럼 순위와 변화량이 헷갈리지 않게 함
- [x] `npm run verify`에 회귀 테스트 추가(필러 상품 3개로 은혼 상품을 4위로 밀어서 "4위" 표기
  확인)

### 재고 스냅샷 대상을 스토어 메인 대신 이번달 카테고리 페이지로 + BH 로그인 게이트 재발
- 사용자 피드백: "스토어 메인에서 하려니까 자꾸 다른 월 상품까지 끌어와버리네, 제일 중요한 건
  이번달 판매데이터인데" — 네이버에 PW/BH 둘 다 월별 카테고리를 따로 운영하고 있어서 그
  카테고리 URL을 대신 알려줌
- [x] `STORES`를 스토어 메인 URL 대신 이번 달(7월) 카테고리 URL로 교체(PW `?st=POPULAR`,
  BH `?st=TOTALSALE`, 둘 다 `size=80`) — 다음 달부터는 그 달 카테고리 URL로 갱신 필요
- 실행해보니 PW는 51건 정상 수집됐는데 **BH는 또 0건** — 카테고리 페이지도 스토어 메인과
  똑같이 로그인 게이트에 걸림(이전에 메인 페이지에서 겪었던 것과 동일 증상 재발)
- [x] `naver-stock.js`의 `fetchProductPage`에 `warmupUrl` 옵션 추가 — 같은 브라우저
  컨텍스트에서 먼저 파워링크 랜딩 링크(게이트를 풀어주는 것으로 확인된 링크)를 방문한 다음,
  그 세션 상태 그대로 진짜 목표 URL(카테고리 페이지)로 이동. 게이트가 스토어 단위로 걸리는
  거라면 메인 페이지뿐 아니라 하위 페이지에도 이 세션이 그대로 적용될 것으로 예상하고 시도
- [x] `naver-stock-snapshot.js`의 BH 항목에 `warmupUrl`(파워링크 링크)을 추가해서
  `getProductStock(url, { warmupUrl })`로 전달
- ⚠️ 아직 실사용 검증 전 — 사용자가 로컬에서 재실행해서 BH가 이번엔 카테고리 상품 건수로
  정상 수집되는지 확인 필요. 샌드박스는 naver.com 자체가 막혀있어서 여기선 검증 불가
- **재검증 결과: warmup 1차 시도도 실패(여전히 0건)**. 추정 원인 수정: `page.goto()`는 기본
  Referer 헤더를 안 보내서, 게이트가 세션/쿠키가 아니라 "그 페이지에서 자연스럽게 이동해왔는지
  (Referer)"로 판단하는 것일 수 있음 → 워밍업 도착 후 실제 URL(`page.url()`)을 다음 이동의
  `referer`로 명시하도록 수정(`page.goto(url, { referer })`)
- [x] 위 referer 수정 반영, `npm run verify` 통과 — 아직 실사용 검증 전
- 이것도 안 되면 다음 시도: Referer/세션이 아니라 실제 DOM 링크 클릭(카테고리 탭 클릭)으로
  넘어가야 통과되는 구조일 수 있음 — 그때 다시 봐야 함
- **재검증 결과: referer 방식도 실패**. 사용자가 직접 "경쟁사 신제품 카테고리로 가는 링크"를
  찾아줌(`m.site.naver.com/1X24n` 단축링크) — 이건 게이트 없이 통과됨(40건 수집 성공).
  [x] BH의 `url`을 이 단축링크로 교체(warmupUrl 제거, 더 이상 필요 없음)

### BH 단축링크가 모바일 페이지라 40개씩만 나옴 — 페이지네이션 추가
- 사용자 피드백: BH 카테고리에 실제로는 페이지가 2개 있는데(40개 초과), 단축링크는 모바일
  뷰라 페이지당 40개로 고정된 것 같고, 그렇다고 데스크톱 `size=80` 링크로 바로 가면 다시
  로그인 게이트에 걸림
- [x] `naver-stock.js`에 `getProductStockAllPages(entryUrl)` 추가 — 첫 페이지 방문 후 도착한
  실제 URL에 `page=` 파라미터가 있으면 2, 3...으로 바꿔가며 같은 브라우저 세션 안에서
  (`referer`를 이전 페이지 URL로 넘겨서) 이어서 방문. 새로 나온 상품이 없는 페이지를 만나면
  마지막 페이지로 판단하고 멈춤(무한루프 방지용 `maxPages` 안전장치도 있음). `page=` 파라미터
  자체가 없는 단일 페이지 구조 URL이면 1페이지만 보고 안전하게 끝냄
- [x] `naver-stock-snapshot.js`의 BH 항목에 `paginate: true` 추가해서 이 함수를 쓰게 함(PW는
  이미 한 페이지에 다 들어와서 그대로 `getProductStock` 유지 — 불필요한 페이지2 요청 안 하게)
- [x] `withPageParam` 순수 로직만 export해서 `npm run verify`에 단위 테스트 추가(다른 쿼리
  파라미터 유지되는지). 페이지 넘기며 실제로 브라우저 세션을 이어가는 부분은 twitter.js/
  instagram.js와 같은 이유로 모킹 테스트 대상 아님 — 실사용 검증 필요
- ⚠️ 아직 실사용 검증 전 — 사용자가 로컬에서 재실행해서 BH가 40개 넘게(2페이지 다 합친 만큼)
  수집되는지 확인 필요
- **재검증 결과: page= 방식은 효과 없음(그대로 40건)** — 도착한 URL에 `page=` 파라미터가
  없거나(모바일 무한스크롤형 페이지라 쿼리 파라미터로 페이지 안 나눔), 있어도 다음 페이지가
  똑같은 내용을 돌려준 것으로 추정. `instagram.js`가 프로필 그리드를 스크롤해서 더 로드하는
  것과 같은 구조일 가능성이 높음
- [x] `getProductStockAllPages`에 스크롤 기반 로딩 추가 — page= 방식으로 안 늘어나면
  `page.mouse.wheel(0, 6000)`으로 실제 스크롤해서 새 상품이 로드될 때까지 기다림
  (`instagram.js`의 `page.mouse.wheel(0, 2500)` 스크롤과 같은 방식, 이미 이 사이트 계열에서
  통하는 걸로 검증된 패턴). 연속 2번(`stableRounds`) 늘어난 게 없으면 다 모았다고 판단하고 멈춤
- ⚠️ 이번에도 아직 실사용 검증 전 — 재실행해서 확인 필요
- **재검증 결과: 데스크톱 뷰포트 스크롤도 효과 없음(또 그대로 40건)**. 사용자에게 실제
  브라우저에서 스크롤하면 더 로드되는지/더보기·페이지 버튼이 있는지 물어봤지만 답을 못 받고
  같은 결과만 반복 보고받음 — 계속 묻기보다 원인 후보를 좁혀서 다음 시도로 진행하기로 함
- 새 추정 원인: `m.site.naver.com`은 모바일 전용 URL인데 지금까지 전부 **데스크톱 뷰포트로
  열어서** 접근했음 — 모바일 페이지의 지연로딩 스크립트가 뷰포트 크기/터치 지원 여부를 보고
  동작을 바꾼다면, 데스크톱 뷰포트에서는 스크롤 이벤트 자체가 무시됐을 수 있음
- [x] `getProductStockAllPages`에 `mobile` 옵션 추가 — 켜면 Playwright의 `devices['iPhone 13']`
  프리셋(실제 아이폰 뷰포트+UA+터치 지원)으로 컨텍스트를 만들어서 재시도. BH에
  `mobile: true` 지정
- ⚠️ 또 아직 실사용 검증 전 — 이것도 안 되면 다음엔 추측 대신 사용자에게 개발자도구
  Network 탭에서 스크롤할 때 호출되는 API 요청 URL을 직접 캡처해서 공유해달라고 요청할 것
  (더 이상 블라인드로 방식을 바꿔가며 시도하는 건 비효율적)
- **재검증 결과: mobile 에뮬레이션은 오히려 0건(서비스 접속 불가 페이지)** — 후퇴, 즉시
  `mobile:true` 제거해서 40건 되던 상태로 원복
- **핵심 정보 확보**: 사용자가 확인해준 실제 사실 — (1) 이 링크는 원래 40개짜리 고정 목록이라
  스크롤해도 그대로 끝(무한스크롤 아님, 지금까지의 스크롤 시도 방향 자체가 틀렸었음) (2) "더보기"
  버튼은 없고 **번호 페이지네이션(1, 2, 3...)이 있음** — 즉 page= 파라미터 방식이 원리상 맞는
  방향이었을 수 있는데, 파라미터 이름/구조를 잘못 짚었을 가능성이 큼(m.site.naver.com 리다이렉트
  후 도착 URL의 실제 페이지네이션 파라미터가 "page"가 아닐 수 있음)
- 더 이상 방식을 블라인드로 바꿔가며 시도하지 않고, 사용자에게 2페이지 버튼을 직접 클릭(또는
  링크 주소 복사)해서 실제 2페이지 URL을 받아 1페이지 URL과 비교하는 방식으로 정확히 확인하기로
  전환 — 답변 대기 중
- **정답 확보**: "링크 주소 복사"는 페이지 자체가 붙여둔 href(`?NaPm=...#`, 클릭 시 JS가 가로채는
  용도라 그대로 못 씀)였고, 실제 "주소창 입력값 복사"(클릭 후 진짜 이동한 URL)가 정답:
  `...?st=TOTALSALE&dt=IMAGE&page=2&size=40` — 도메인은 그대로 `smartstore.naver.com`, size는
  40(80 아님), page만 2로 바뀜. 추측이 아니라 정확한 패턴을 확보함
- [x] `getProductStockAllPages`에 `pageUrl(pageNum)` 옵션 추가 — 명시하면 이걸로 각 페이지
  URL을 직접 만들어서 이동(추론 없이 확인된 패턴 그대로 사용), 스크롤/모바일 에뮬레이션 관련
  코드는 이제 이 방식엔 안 쓰여서 제거(단순화)
- [x] `naver-stock-snapshot.js`의 BH에 이 정확한 `pageUrl` 템플릿 지정 —
  `st=TOTALSALE&dt=IMAGE&page=${n}&size=40`, entryUrl(m.site.naver.com 단축링크)로 게이트를
  먼저 통과시킨 다음 그 세션 안에서 이 패턴으로 2, 3페이지 이동
- [x] 템플릿 문자열이 사용자가 확인해준 실제 URL과 정확히 일치하는지 수동 검증 + `npm run
  verify` 통과
- **✅ 실사용 검증 성공** — PW 51건, BH 57건(40건 고정이던 게 페이지네이션으로 57건까지
  정상 수집됨) 확인. BH 로그인 게이트/카테고리 필터링/페이지네이션 이슈 전부 해결됨

### 실제 버그 발견: "컬렉션"이 진짜 라인명(GEM/G.M.G/룩업)과 똑같이 취급돼서 매칭이 막힘
- 사용자가 "매칭 안 됨" 목록에서 짝을 여러 개 짚어주고 "키워드가 분명히 겹치는데 왜 미매칭인지
  셀프 점검해보라"고 요청 — `extractKeywords`/`detectProductLine`을 실제 게시물 원문에 직접
  돌려서 근거 기반으로 진단함(추측 아님)
- **근본 원인**: `KNOWN_PRODUCT_LINES`에 `'컬렉션'/'Collection'`이 GEM/G.M.G/룩업 같은 진짜
  구분되는 라인 브랜드명과 나란히 등록돼있었음. 그런데 "컬렉션"은 실제로는 특정 라인 이름이
  아니라 그냥 "~모음/시리즈"라는 뜻의 흔한 일반 단어 — 경쟁사가 "G.M.G **컬렉션**"이라고 쓰면
  배열에서 '컬렉션'이 'G.M.G'보다 먼저 나와서 `detectProductLine`이 라인을 '컬렉션'으로
  잘못 확정해버리고, 당사(라인='G.M.G' 또는 null)와 "라인이 다르다"고 판단해서 매칭이 막힘 —
  키워드는 2개 이상 겹쳐도 라인 불일치면 무조건 스킵되는 로직(Union-Find)이라 이거 하나로 여러
  건이 한꺼번에 막혀있었음
- [x] `'컬렉션'/'Collection'`을 `KNOWN_PRODUCT_LINES`에서 빼고 `GENERIC_KEYWORDS`로 재분류
  (실제 라인 판단에는 더 이상 안 쓰이고, 키워드 매칭 시 걸러내는 흔한 단어로만 취급)
- 같은 패턴으로 "인사이드 판타지"(경쟁사, 한글 표기) vs "INSIDE FANTASY"(당사, 영문 표기)도
  라인이 안 통일돼있던 걸 발견 → `LINE_ALIASES`에 추가(스케일↔POP와 같은 방식)
- [x] 수정 후 재검증: PW 건담(16 아무로 레이/17 세이라 마스/SET) ↔ BH 건담 G.M.G 컬렉션
  여러 건, PW 야마토 ↔ BH 야마토가 **자동으로** 매칭됨(수동 매칭 필요 없어짐) — 근거:
  라인이 둘 다 'G.M.G'로 일치, 둘 다 null로 일치
- ⚠️ 부작용 발견(투명하게 공개): "페르소나5" PW/BH 쌍은 원래 이 버그 때문에 **둘 다 똑같이
  잘못된 '컬렉션' 라인값**을 가져서 우연히 매칭되고 있었음 — 수정 후엔 당사만 "스케일"(→POP)을
  명시하고 경쟁사는 라인 관련 단어를 전혀 안 써서(line=null) 라인이 달라져 오히려 매칭이
  깨짐(키워드는 4개나 겹치는데도). 근본적으로 "한쪽만 라인을 명시한 게시물"은 지금 로직
  설계상(라인 불일치 시 무조건 차단) 어차피 막히는 케이스라, 수동 매칭으로 보완
- [x] 팩맨(키워드 겹침 1개뿐이라 2개 기준 미달, 라인 수정과 무관하게 여전히 수동 필요)과
  페르소나5, 이렇게 2건을 `manual-matches.json`에 추가
- 사용자가 짚어준 나머지 2쌍(PW 이벤트감사글↔BH 건담, PW 원피스↔BH 블리치)은 실제 원문을
  대조해보니 **키워드가 전혀 안 겹침**(완전히 다른 내용) — 실수로 번호를 잘못 짚었을 가능성이
  높아서 수동 매칭에 반영 안 하고 사용자에게 재확인 요청
- [x] `npm run verify` 통과(회귀 없음 확인) + 트위터 매칭 안 됨 건수 실제로 PW 12→5, BH 18→11로
  감소한 것 확인(rebuild-report.js로 재생성)
- **정정**: 사용자가 다시 확인해준 리스트를 보니, 내가 짚었던 "번호 착각"은 사실 내 쪽의
  착각이었음 — 사용자가 보고 있던 리포트는 그 사이 데이터가 갱신(재수집)돼서 번호가 달라져
  있었는데, 나는 이전(스테일) 캐시 기준 번호로 대조하고 있었음. 사용자가 준 전체 목록(PW1-10,
  BH1-13) 원문으로 다시 대조하니 "#3-#8"(건담 SET), "#4-#7"(야마토), "#9-#11"(하이큐)은 전부
  실제로 같은 상품이 맞았고, 하이큐/춘하추동대행자 쌍은 이미 라인 버그 수정으로 자동 매칭됨
  (라인 둘 다 null, 키워드 다수 겹침 — 확인 완료)
- **"경쟁사 #1 #2 #3 #13 삭제" 요청도 이제 이해됨**: 이 번호들은(사용자의 최신 리스트 기준)
  전부 쿠폰/이벤트/세관 공지처럼 **상품이 아닌 게시물**이었음(내가 이전 스테일 번호로 확인했을
  때는 엉뚱하게 실제 상품 게시물(#13)로 오인했었음 — 정정)
- [x] `ignore-posts.json` 신설 + `aggregate.js`의 `buildProductComparison`/`buildComparisonReport`에
  `ignorePosts` 파라미터 추가 — 여기 지정된 링크는 계정 총계(팔로워/게시물 지표)에는 그대로
  포함되지만 "매칭 안 됨" 목록에는 안 보이게 걸러냄(완전히 지우는 게 아니라 "상품이 아니라서
  일부러 뺐다"를 명시적으로 관리 — manualMatches와 마찬가지로 데이터를 조용히 삭제하지 않는
  기존 설계 철학 유지)
- [x] 확인 가능했던 공지성 게시물 3건(쿠폰발급/이벤트당첨자/세관검사)의 실제 링크를 캐시에서
  찾아서 `ignore-posts.json`에 추가. 사용자가 말한 "#2"(신제품 구경 할인쿠폰 이벤트)는 이
  샌드박스의 스테일 캐시엔 없는 게시물(사용자가 그 사이 재수집해서 새로 생긴 것)이라 링크를
  못 찾음 — 사용자가 직접 `ignore-posts.json`의 `twitter.bh` 배열에 그 링크만 추가하면 됨
- [x] `npm run verify`에 ignore-posts 필터링 회귀 테스트 추가 + `rebuild-report.js`로 실제
  반영 확인(BH 매칭 안 됨 11→8, 공지 3건 제외됨)

### 재고 지표를 "직전 스냅샷 대비 변화"에서 "총 판매 추정치"로 전면 재설계
- 사용자 피드백 3건: (1) "PW 매출 칸이 15위 (0), 5위 (-131 추정)처럼 나오는데, 몇 개 팔렸는지
  추이 변화가 궁금한 게 아니라 리포트를 언제 뽑든 현시점 기준 몇 개 팔렸는지가 궁금하다.
  추이 변화는 스냅샷으로 갈음할 수 있는 거 아니냐" (2) "재고 스냅샷 섹션도 초기한도에서 뺀
  형태로만 나와서 알아보기 불편하다, 200개 (9800) 이렇게 병기하든지" (3) "표 폭이 브라우저
  최대폭을 애매하게 초과하는데 매출란 폭 줄이면 다 들어갈 것 같다"
- **핵심 방향 전환**: 기존엔 "직전 스냅샷 대비 delta"(그 사이 기간에만 몇 개 팔렸는지)를
  기본 지표로 썼는데, 이건 스냅샷을 찍는 시점/주기에 따라 값이 흔들림(스냅샷 사이 시간이
  짧으면 작은 값, 길면 큰 값). 사용자가 원하는 건 "이 상품을 처음 관측한 시점부터 지금까지
  총 몇 개 팔렸는가"라는 **스냅샷 주기와 무관하게 안정적인 값**
- [x] `stock-report.js`의 `buildStockComparison`을 재설계 — 상품별로 "과거 스냅샷(최신 제외)
  중 가장 먼저 관측된 재고"(firstStock)를 전체 히스토리에서 찾아서 `totalSold = firstStock -
  현재재고`로 계산(스냅샷이 몇 개든, 언제 찍었든 안정적). 이번에 처음 관측된 상품(firstStock
  없음)만 기존의 초기한도 추정(`estimateInitialCap`)으로 대체하고 `totalSoldIsEstimated:true`로
  표시. 직전 스냅샷 대비 `stockDelta`는 필드로는 남겨두되(참고용) 화면 기본 지표에서는 뺌
  — 사용자 말대로 "추이는 스냅샷으로" 봐도 되는 부분이라 굳이 지우지 않고 필드만 유지
- [x] `rankStockProducts`/`findStockMatch`가 이제 `totalSold` 기준으로 순위/매칭
- [x] SNS 표 우측 매출 칸(`stockMatchCell`): "15위 (0)"처럼 순위+변화량 형식 대신 "15위 200개"
  처럼 순위+총 판매추정 개수로 변경(부호/괄호 없이 그냥 개수) — 추정치인 경우만 뒤에 "*" 표시
- [x] 하단 "재고 스냅샷" 섹션(`renderStoreTable`): "이전 재고"/"변화" 두 컬럼으로 나뉘어 있던
  걸 "총 판매추정 (재고)" 한 컬럼으로 합쳐서 "513개 판매추정 (재고 9,486개)"처럼 병기 —
  사용자가 제안한 "200개 (9800)" 형식 그대로 반영
- [x] 표 너비: `.metric{min-width:170px}`가 매출 칸에도 그대로 적용돼서 표가 애매하게
  넘쳤었음 — 매출 칸은 "3위 200개" 정도의 짧은 텍스트만 들어가므로 `td.metric.sm-match` 등
  전용 규칙으로 `min-width:64px`로 좁힘
- [x] `npm run verify` 전체 갱신(새 필드명 totalSold/totalSoldIsEstimated로 목/단정 교체) 통과
  + 가짜 재고 히스토리로 실제 리포트 생성 + Playwright로 (a) 표 `scrollWidth === clientWidth`
  (가로 스크롤 불필요) 확인 (b) 하단 섹션 "513개 판매추정 (재고 9,486개)" 형식 확인

### 🟢 지금 가능한 부분 중 남은 것
- [x] 결과 취합(비교표+비율 계산) 로직 — `aggregate.js` 완료
- [x] 엑셀 자동저장(히스토리 누적) — `excel.js` 완료
- [x] 모킹 데이터로 내부 로직 검증 — `npm run verify` 통과
- [ ] `twitter.js`/`instagram.js` 실제 검증 후, 수집 결과 형태(특히 인스타 comments가 실제로도
  null이 잦을지)에 맞춰 `aggregate.js`의 필드 매핑 미세조정 필요할 수 있음
- [ ] 팔로워수 등 계정 단위 지표는 아직 수집기에 없음 — 필요하면 추후 추가 논의

## HTML 리포트 가독성 개선 (`html-report.js`)
- [x] 열 헤더를 아이콘으로 축약: 리트윗=♻️, 좋아요=♥️, 댓글=💬, 시각=⏰ (예: "PW ♻️", "♻️차이")
- [x] 상품 행마다 있던 "전체 펼치기/접기" 필요성 반영 — 각 플랫폼 섹션 제목 옆에
  "전체 펼치기"/"전체 접기" 버튼 추가(`toggleAllEmbeds(platformKey, forceOpen)`), 플랫폼별로만
  동작(트위터 버튼 눌러도 인스타 행은 안 건드림)
- [x] 차이 칸 시각화: 프로그레스 바(단일값 대 한계치용) 대신, PW vs BH는 "부분 대 전체" 비교라
  **가로 분할 바**(막대 하나를 PW/BH 비율로 나눠 채움, 2px 흰 간극으로 구간 분리)로 결정
  — `dataviz` 스킬 기준 확인. 기존 주황(PW `#e8590c`)/빨강(BH `#c0504d`) 색상은
  `validate_palette.js`로 색맹 안전성 재검증 통과(그대로 사용)
- [x] `npm run verify` 통과 확인 + 실제 캐시 데이터(`_last-collection.json`)로 리포트 재생성 +
  Playwright 스크린샷으로 아이콘/분할바/토글전체 정상 렌더링 눈으로 확인

### 2차 개선 — 분할바를 더 넓게 + 브랜드 컬러 + 행 강조
- [x] PW/BH 숫자 칸 + 차이 칸(3칸)을 지표별로 1칸으로 병합, 그 안을 넓은 분할바로 채움
  (♻️/♥️/⏰ 각 1칸) — 칸이 많아서 분할바가 작아 보이던 문제 해결
- [x] 숫자 라벨은 바 안이 아니라 **바 양 끝 바깥쪽**에 고정 — 비율이 크게 벌어지면(예: 355:1)
  작은 쪽 구간이 라벨을 담을 폭이 안 나오므로, "잘리는 라벨"보다 안전한 선택
- [x] ⏰(시각)은 PW/BH 합이 의미 없는 값(부분-전체 관계 아님)이라 비율 바로 만들지 않고,
  칩 2개(50:50)로 시각 계열만 맞추고 실제 분 차이는 캡션으로 정확히 표기
- [x] PW 브랜드 컬러를 주황(`#e8590c`)→파랑(`#1971c2`)으로 변경(BH는 빨강 그대로) —
  `validate_palette.js`로 BH 빨강 대비 색맹 안전성 재검증 통과
- [x] 경합=연노란색(`#fff9db`), 약세=연빨강(`#fff0f0`) 행 배경 강조 추가. 기존 "상위 3위 연노랑
  배경"은 제거 — 순위 강조(연노랑)와 경합 강조(연노랑)가 같은 색이면 혼동되므로, 순위는
  메달 아이콘(🥇🥈🥉)만으로 표시
- [x] `npm run verify` 통과 + 실제 데이터로 재생성 + Playwright 스크린샷으로 병합 칸/바깥쪽
  라벨/행 강조 정상 렌더링 확인

### 3차 개선 — 시각 칸을 diverging(기준선 대비 편차) 바로 교체
- [x] 시각 칸(칩 2개 나열)이 "차이가 시각적으로 안 느껴진다"는 피드백 반영 — PW/BH 시각은
  부분-전체 관계가 아니라 **기준선 대비 편차** 문제라 분할바 대신 diverging 형태로 변경
- [x] `aggregate.js`: `buildProductEntry`에 부호 있는 `timeDiffSignedMinutes`(BH−PW, 분) 추가
  (기존 `timeDiffMinutes`는 절대값 그대로 유지, 엑셀 등 기존 사용처 영향 없음)
- [x] `html-report.js`: 컬럼 중앙에 파란 기준선(=PW 시각, 0)을 두고, BH가 늦으면 빨간 점이
  오른쪽, 빠르면 왼쪽으로 이동하는 diverging 바로 시각 칸 교체. 스케일은 4시간(240분) 고정 —
  벗어나는 이상치는 점을 끝까지 밀어붙이고 이중 테두리로 "스케일 밖" 표시, 정확한 분 차이는
  캡션 텍스트로 항상 그대로 표기(잘림 없음). PW/BH 실제 시각은 툴팁으로 이동
- [x] `npm run verify` 통과 + 실제 데이터로 재생성 + Playwright 스크린샷으로 확인

### 5차 개선 — 시각 칸 중앙 부호 숫자 + 헤더에 PW/BH 파이차트 추가
- [x] 시각 칸: 막대 밑 원본 시:분 표시를 없애고, PW 기준 부호 있는 분 차이 숫자 하나만 중앙에
  표기 — `+분`(PW가 먼저)은 파란색, `-분`(BH가 먼저)은 빨간색, `PW`/`BH` 글자 자체는 안 씀
  (색만 보고도 어느 쪽이 우세인지 알 수 있게). 실제 시:분은 툴팁으로 유지
- [x] 각 플랫폼 헤더 카드 영역에 지표별(리트윗/좋아요 등) PW vs BH 파이차트 추가 — 기존
  숫자 카드(1,354/165)는 그대로 두고 옆에 병렬로 배치. CSS `conic-gradient`로 구현(2조각
  파이라 SVG 불필요), 색상은 표와 동일한 PW 파랑/BH 빨강, 퍼센트 라벨 병기(색만으로 판단 안
  하게)
- [x] `npm run verify` 통과 + 실제 데이터로 재생성 + Playwright 스크린샷으로 확인

### 6차 개선 — 시각 칸에 원본 시:분 복원 + 헤더 카드 줄바꿈 버그 수정
- 사용자 피드백: (1) 5차에서 중앙 부호 숫자를 추가하면서 원본 시:분을 지워버렸는데, 둘 다
  같이 있어야 한다고 정정 (2) 헤더 카드가 좁은 화면에서 "위 5칸 아래 2칸"으로 줄바꿈될 때
  아래로 넘어간 카드 1~2개가 유독 커 보임 — 확인해보니 실제로 버그였음
- [x] 시각 칸: `timeOnly()` 복원해서 막대 밑에 `[PW 시:분]  [부호 숫자]  [BH 시:분]` 3분할로
  같이 표시 — 원본 값과 계산된 차이를 둘 다 보여줌
- [x] 헤더 카드 줄바꿈 버그 원인: `.card{flex:1}`이라 줄바꿈된 두 번째 줄에 카드가 적게
  남으면(예: 2개) 그 둘이서 남는 공간을 다 나눠 가지면서 비정상적으로 커짐 → `flex:0 1 190px`로
  고정폭에 가깝게 바꿔서 몇 개가 남든 크기 일정하게 유지
- [x] 추가로 지표별 PW/BH/파이 카드 3개를 `.card-group`으로 묶어서, 줄바꿈이 일어나도 그룹이
  통째로 다음 줄에 내려가게 함 — 그룹 내부가 갈라져서 짝 없이 혼자 남는 카드가 생기는 문제
  자체를 없앰
- [x] `npm run verify` 통과 + 실제 데이터로 재생성 + 좁은 뷰포트(1280px/900px)로 Playwright
  스크린샷 찍어서 줄바꿈 시나리오까지 확인

### 7차 개선 — 헤더 카드 레이아웃 재구성 + IP 컬럼 폭 축소 + 헤더에 라벨 추가
- [x] 헤더 카드: "큰카드+그룹1"이 한 줄(4칸), 그룹2가 다음 줄(3칸)이라 4/3으로 언밸런스했던
  걸, 큰카드(매칭된 상품)를 좌측에 두고 우측에 지표별 그룹(PW/BH/파이 3칸)을 세로로 쌓아서
  3칸+3칸으로 균형 잡음 (`.card-hero` + `.card-groups`)
- [x] IP 컬럼이 내용 기준 자동폭이라 여백이 크게 남았음 — `max-width`만으론 auto-layout
  테이블에서 강제가 안 돼서(실측해보니 173px로 안 줄어듦) `<colgroup>`으로 해당 컬럼에
  명시적 `width:130px` 지정, 넘치는 텍스트는 ellipsis로 자르고 `title` 속성으로 전체 이름은
  마우스 오버 시 확인 가능하게 남김
- [x] 컬럼이 넓어진 김에 아이콘만 있던 헤더에 라벨 추가: "♻️ 리트윗", "♥️ 좋아요", "⏰ 시각"
- [x] `npm run verify` 통과 + 실제 데이터로 재생성 + Playwright로 컬럼 실측 폭(130px) 확인 +
  스크린샷으로 레이아웃 확인

### 8차 개선 — 반응형 대응 + 네이버 연동 배치 방침
- [x] 모바일(390px)/태블릿(768px) 폭으로 직접 스크린샷 찍어서 확인해보니 헤더 카드가
  `.card-hero{flex:0 0 200px}`(shrink:0) 때문에 좁은 화면에서 안 줄어들고 찌그러지는 문제
  발견 → `@media (max-width:860px)`에서 `.cards`를 세로(column)로 바꿔서 큰카드가 전체
  폭으로 위에, 지표 그룹들이 그 밑에 쌓이게 수정. 태블릿(768px)에선 그룹 내 3칸(PW/BH/파이)도
  안 잘리고 한 줄에 다 들어감, 모바일(390px)에선 카드가 세로로 자연스럽게 쌓임
- 방침(코드 반영 아직 아님, 다음에 네이버 연동 붙일 때 지킬 것): 지금 SNS 비교 화면은
  이대로 유지 — 네이버 판매데이터 연동 섹션은 같은 화면에 나란히/반응형으로 욱여넣지 않고
  **아래로 스크롤해야 나오는 별도 섹션**으로 추가하기로 함(안 그러면 지금도 이미 표가 넓은데
  더 좁아짐)
- [x] `npm run verify` 통과 + 실제 데이터로 재생성 + 여러 뷰포트로 Playwright 스크린샷 확인

## 버그: 자사 게시물 "다시 리트윗"이 원본 게시물 자체를 통째로 누락시킴
- 사용자가 매출 1위 상품의 PW 링크를 줬는데 캐시(`_last-collection.json`)에 아예 없음 확인.
  BH 쪽 대응 게시물은 정상 수집됨 — 매칭 실패가 아니라 수집 단계에서 빠진 것.
- 진단: 트위터 status ID에 타임스탬프가 인코딩돼 있어서(snowflake) 디코딩해보니 7/2 17:01:13
  KST — 수집 기간(7/1~7/2) 안. PW 수집 타임라인을 보면 7/1 12:20 이후 다음 게시물이 7/2
  17:04로 뛰는데, 그 사이 3분 지점(17:01)에 정확히 이 게시물이 있어야 함 → 날짜 밖이라
  걸러진 게 아니라 뭔가 이 한 건만 특이하게 누락됨.
- 원인 확인(`twitter.js` `collectOnce()`): 리트윗/고정 게시물 제외 로직이 `socialContext`
  라벨(예: "OO님이 리트윗함")이 있으면 통째로 스킵하면서 그 링크를 `_seenLinks`에 추가해버림.
  그런데 사용자가 확인해준 것처럼 이 계정은 고정이 아니라 **주요 상품을 자기가 다시
  리트윗하는 습관**이 있음 — 자사 원본 게시물을 다시 리트윗하면 피드 맨 위에 이 부스트된
  카드(socialContext 라벨 있음, 원본 링크 그대로)로 먼저 마주치는데, 이때 링크를
  `_seenLinks`에 넣어버리면 스크롤이 내려가서 이 게시물의 자연스러운(라벨 없는) 위치에
  도달했을 때도 "이미 처리함"으로 오인해서 두 번 다 놓쳐버림 — 원본 데이터를 한 번도 저장
  못 하고 완전히 사라짐.
- [x] 수정: `socialContext`(리트윗/고정) 및 인용 트윗 중첩 카드 제외 분기에서
  `_seenLinks.add(link)` 제거 — 스킵만 하고 링크는 "본 것"으로 마킹 안 함. 그래야 스크롤
  중 이 카드가 화면에 남아있는 동안엔 매 tick마다 계속 스킵되지만(부작용 없음), 나중에
  자연스러운(라벨 없는) 위치에 도달하면 정상적으로 수집됨.
- ⚠️ 이 수정은 브라우저/실제 세션 없이는 검증 불가(twitter.js는 모킹 검증 대상 아님,
  README/PLAN 기존 방침과 동일) — 사용자가 로컬에서 `node run.js`로 재수집해서 이 게시물이
  이제 잡히는지 실제로 확인 필요. (검증은 다음으로 미루고, 이미 수집된 캐시 기준으로 HTML
  시각화 다듬기를 먼저 진행하기로 함)

### 4차 개선 — 시각 diverging 바 스케일을 10분으로, 원본 시:분 표시 추가
- [x] 실제 데이터 대다수가 10분 이내 차이라 스케일 4시간(240분)이 너무 커서 정작 흔한 값들이
  다 중앙에 뭉쳐 보임 → `TIME_SCALE_CAP_MINUTES`를 10으로 낮춰서 흔한 범위 안에서의 편차를
  크게 벌려 보이게 함
- [x] 228분/1855분처럼 스케일을 훨씬 벗어나는 이상치는 막대를 끝까지 채운 뒤 점 반경을
  8px→14px로 키우고 옅은 링(halo)을 둘러서 "정상 범위 한참 벗어남"을 크기로 구별
  (`.divbar-dot.clipped`)
- [x] 행마다 있던 "PW/BH N분 먼저" 상대적 문구 캡션을 없애고, 막대 밑에 **원본 시:분**(날짜
  제외, 예: `12:20 ... 16:10`)을 PW/BH 순서로 나란히 표시 — `timeOnly()`로 `formatKstTime`
  결과에서 날짜 부분만 잘라냄. 방향(왼쪽=BH 먼저/오른쪽=BH 늦음) 안내는 각 행 대신 리포트
  하단 안내문에 한 번만 정리해서 남김(툴팁에도 정확한 분 차이 유지)
- [x] `npm run verify` 통과 + 실제 데이터로 재생성 + Playwright 스크린샷으로 확인
