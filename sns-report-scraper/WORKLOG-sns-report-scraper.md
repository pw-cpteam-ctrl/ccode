# WORKLOG-sns-report-scraper.md

`WORKLOG-작성-규칙.md` 기준으로 작성 — 쉬운 설명을 먼저 쓰고, 코드 용어는 맨 뒤 괄호 안에
병기. 그날 있었던 요청과 처리 흐름을 있는 그대로 기록.

---

<details open>
<summary>2026-08-07 (오늘)</summary>

## 2026-08-07

### 재고 추이 그래프 문구가 데이터가 사라진 것처럼 보이는 문제
- **무슨 문제/요청이 있었나**: 종합표의 추이 자리에 "스냅샷이 더 쌓이면 추이 그래프가
  여기 표시됩니다(현재 1개 시점)"이 떠서, 마치 그동안 쌓아둔 스냅샷이 전부 날아간 것처럼
  읽혔음. 스냅샷 저장 로직·설치 과정에서 reports 폴더를 빼먹은 건 아닌지까지 의심하게 됨.
- **확인해보니**: 사용자가 준 원본 파일의 실제 시각(`takenAt`)을 보니 7/21·8/4·8/6 스냅샷
  3개가 전부 멀쩡히 남아 있었음. 문구의 "1개 시점"은 전체 스냅샷 개수가 아니라 **그
  상품이 관측된 시점 수**였음 — 8/6에 새로 예약이 열린 신상품은 이전 스냅샷엔 아예 없으니
  1개로 나오는 게 정상 동작이었고(긴토키 양이지사·카구라·히라코·이타치·아마미야·미나토
  전부 7/21·8/4엔 없음을 확인), 데이터 유실이 아니라 문구가 그 의미를 못 살린 것.
- **어떻게 처리했나**: 안내 문구를 "이 상품은 아직 한 시점에서만 관측됨(최근 추가된 상품일
  수 있음)" 및 "이 상품은 지금까지 N개 시점에서만 관측됨" + "스냅샷이 3개 시점 이상 쌓이면
  추이 그래프가 표시됩니다"로 바꿔서 "전체 스냅샷 수가 아니라 이 상품 기준"이라는 의미를
  드러냄.
- **결과**: `npm run verify` 전체 통과, 문구 검증 케이스도 그 의미가 남아있는지 확인하도록
  갱신.

### "재고만 찍기" 버튼 요청 (SNS 수집 없이)
- **무슨 문제/요청이 있었나**: "sns 수집은 안 하고 재고만 찍는기능은 없지?"라는 질문 →
  "ㅇㅇ 찍어"로 추가 확정. 재고 스냅샷 자체(`naver-stock-snapshot.js`)는 이미 단독 실행
  가능했지만 대시보드엔 버튼이 없어서, 재고만 찍으려면 검은 창(명령어 창)을 열어야 했음 —
  이 프로젝트가 원래 없애려던 방식.
- **확인해보니**: 재고 추이 그래프는 같은 상품이 3개 시점 이상 관측돼야 나오는데, 지금
  구조는 SNS 수집을 돌릴 때만 스냅샷이 같이 쌓여서 시점을 모으려면 매번 몇 분짜리 수집을
  돌려야 했음. 재고 스냅샷은 공개 페이지라 로그인 세션 없이 수십 초면 끝나서 SNS 수집과
  분리해도 문제 없음을 확인.
- **어떻게 처리했나**: 기존 "캐시로 다시 만들기"(`/api/rebuild`)와 같은 구조로
  `POST /api/stock-snapshot`을 추가해 `naver-stock-snapshot.js`를 실행하도록 연결하고,
  대시보드 4번 패널로 "📦 재고만 찍기" 버튼을 추가(뒤 패널 번호를 밀면서, 기존에 "6."이
  두 번 겹쳐 있던 번호 중복도 같이 정리).
- **결과**: 서버를 띄우고 실제로 POST 호출해서 jobId 발급·작업 실행을 확인. Playwright로
  대시보드를 열어서 버튼이 뜨고 패널 번호가 1~7로 정상 순서인지도 눈으로 확인. 재고
  스냅샷 스크립트 자체를 끝까지 실행하는 건 이 작업 환경에 크롤링용 브라우저가 없어서
  확인 못 했음(사용자 PC엔 같이 설치돼 있어서 실제로는 문제 없음) — 배선(연결)만 확인.

### "미매칭 안 고쳐졋는데 뭐냐" — 8/6에 고친 매칭 개선이 이 리포트엔 안 먹힌 것처럼 보임
- **무슨 문제/요청이 있었나**: 8/7 09:43 리포트를 보고, 은혼 카라트 긴토키·은혼 카구라·
  나루토 미나토·헤도라·카이타닉스 등 PW-BH 쌍이 여전히 미매칭으로 남아있다고 지적. 바로
  전날(8/6) 짝이 명백한데 놓치던 매칭 문제 2건(희귀 토큰 1개/붙여쓰기 차이)을 고쳤다고
  보고했었는데, 정작 실제 리포트에는 반영이 안 된 것처럼 보임.
- **확인해보니**: 리포트 HTML 자체엔 미매칭 게시물 본문이 70자 미리보기로만 들어있어서
  (`unmatchedList`), 실제 캡션 전문 없이는 8/6에 넣은 매칭 개선이 이 특정 쌍들에 왜 안
  먹혔는지 코드만 보고 재현할 방법이 없음. 예전에도 비슷한 상황에서 오래된 캐시로 잘못
  분석했다가 사용자가 실제 데이터를 보여줘서 재검토한 전례가 있어서, 이번엔 추측성 재구성
  텍스트로 또 틀린 결론을 내는 위험을 피하기로 함.
- **어떻게 처리했나**: 짐작으로 진단을 마무리 짓지 않고, 8/7 수집 원본 캐시(실제 캡션 전문이
  들어있는 파일)를 사용자에게 요청해서 그걸로 다시 확인하기로 함. 대신 이 문제를 당장 겪지
  않아도 되도록, 바로 아래 "합치기" 기능을 먼저 만들어서 사용자가 지금 이 쌍들을 직접 한
  행으로 묶어 쓸 수 있게 함.
- **결과**: 근본 원인은 아직 미확인 상태 — 실제 원본 캐시가 있어야 다음 진단이 가능함을
  솔직하게 남겨둠. 짐작만으로 "고쳤다"고 다시 말하지 않기로 함.

### 표에 추가해도 PW/BH가 따로 나뉘는 문제 — "합치기" 기능 신규 추가
- **무슨 문제/요청이 있었나**: "수동매칭도 어떻게 쓰라는거야 같은상품이어도 표에추가누르면
  각각 pw따로 bh따로추가돼서 나뉘잖아 예를들어 #2랑 #4가 같다는 뭔 기능이라도 넣어야하는거
  아니냐" — 기존 "표에 추가" 버튼은 PW/BH 게시물을 각각 독립된 "반대쪽 짝 없음" 행으로만
  넣어서, 사람이 봤을 때 같은 상품인 두 게시물을 진짜 짝으로 묶어 보여줄 방법이 전혀 없었음.
- **확인해보니**: 진짜 짝짓기 저장 방식(`manual-matches.json` + `extractManualMatches`,
  링크로 PW/BH를 지정하면 자동 매칭보다 먼저 적용됨)이 `aggregate.js`/`rebuild-report.js`엔
  이미 있었지만, 리포트 화면(`html-report.js`)에는 이 방식으로 가는 버튼이 하나도 없었음 —
  "표에 추가"(`manual-posts.json` 경로, 자동 매칭 재시도용)만 연결돼 있었던 것.
- **어떻게 처리했나**: 미매칭 목록 각 항목에 체크박스를 추가하고, 사용자가 PW쪽·BH쪽에서
  같은 상품이라고 판단한 게시물들을 체크한 뒤 "체크한 PW+BH 게시물 하나로 합치기" 버튼을
  누르면 — 좋아요/리트윗 합산, 최초 게시 시각, 우세/경합/약세 판정까지 자동 매칭 상품 행과
  똑같은 방식으로 계산해서(`aggregate.js`의 `buildProductEntry`와 같은 로직을 화면 쪽
  자바스크립트로도 재구현) 진짜 PW+BH 짝 행 하나로 표에 즉시 추가함. 저장할 때는
  `manual-matches.json`과 바로 호환되는 `{pw, bh, label}` 형태로 내보내는 별도 저장 상자를
  새로 만듦(기존 "표에 추가" 저장 상자와는 별개).
- **결과**: 실제 리포트 파일을 만들어 Playwright로 직접 조작해서 확인 — 미매칭 PW 게시물
  1개(리트윗10·좋아요100)와 BH 게시물 1개(리트윗8·좋아요80)를 체크하고 합치기를 누르면
  표에 "은혼 카구라" 한 행(리트윗 10:8, 좋아요 100:80, 시각차 +30분, 결과 우세)이 즉시
  생기고, 두 미매칭 목록에서 원래 항목이 정상적으로 빠짐. 저장 상자에도
  `{"pw":["..."],"bh":["..."],"label":"..."}` 형태로 정확히 나오는 것 확인. 삭제 버튼을
  눌러 행과 저장 상자가 같이 정리되는 것도 확인.

</details>

<details>
<summary>이전 기록 (2026-08-06 이전)</summary>

## 2026-08-06

### 마우스오버하면 다른 상품 매출이 붙어있는 문제 (재고 매칭 오류·중복)
- **무슨 문제/요청이 있었나**: "G.E.M. 시리즈 우치하 이타치 & 사스케 [재판]" 행에 664개가
  찍혀 있어서 봤더니 마우스오버 하니 닌자대전 매출이 붙어있음. 은혼 6위·12위·16위도 마찬가지.
  "매칭안된개시물은 왜매칭안되는지 점검해야할거아냐"라며 전반적인 자가점검도 함께 요청.
- **확인해보니**: 원인이 3가지 겹쳐 있었음. (1) 라인이 맞는 재고 후보가 하나도 없으면
  필터를 통째로 무시하고 아무 후보나 골랐음(`if (refined.length > 0) candidates = refined`) —
  재고 목록에 아직 없는 신제품(예약 개시 전) 게시물이 정확히 이 경우라 무관한 상품이 붙음.
  (2) 라인 비교가 문자열 포함(`name.includes(line)`)이라 "GEM 테노히라 미나토"가 실제로는
  테노히라 라인인데 GEM 라인 후보로도 잡힘. (3) 후보가 여럿이면 "판매량이 가장 큰 것"을
  임의로 골라서, 캐릭터가 다른 상품들이 전부 후보인 경우 제일 큰 게 이겼음(664개 닌자대전).
  거기에 더해 행마다 따로 재고를 찾다 보니(`findStockMatch`) 같은 재고 상품이 여러 행에
  중복으로 붙는 문제도 있었음(BH "14개*"가 나루토 질풍전 3개 행에 동시에).
- **어떻게 처리했나**: 라인이 맞는 후보가 없으면 매칭하지 않도록 변경, 라인 비교를
  `detectProductLine` 결과끼리 비교로 변경, 후보가 여럿이면 게시물 본문 키워드를 단서로
  점수를 매겨 고르되 동점이면(구분 근거 없음) 매칭하지 않도록 변경. 표 전체를 한 번에
  배정하는 `assignStockMatches()`를 새로 만들어 재고 상품 하나가 최대 한 행에만 붙게 함.
- **결과**: 실제 스냅샷으로 재검증 — 재고 중복 계상 트위터/인스타 모두 0건, 틀린 매칭
  (닌자대전·카카시)이 사라지고 남은 매칭은 전부 정확함(예: 헌터x헌터 룩업 → PW "키르아
  조르딕 룩업"/BH "룩업 키르아 조르딕"). 매칭 수가 줄어든 건 과보수가 아니라 신제품에
  억지로 다른 상품을 붙이던 걸 그만둔 결과. `npm run verify` 전체 통과.

### 989개 판 게 아니라 그냥 입고상품인데 판매추정이 만들어지는 문제
- **무슨 문제/요청이 있었나**: "이거 989개 판 게 아니고 그냥 입고상품이라 11개 남은건데....
  예약상품 입고상품 세는방식 다르다고 진작 말핮ㅣ낳앗나"
- **확인해보니**: `PLAN.md`에 처음부터 "예약 상품(9000~10000대에서 줄어든 만큼이 판매량)과
  이미 발매된 입고 상품(82/24/15/6처럼 낮은 숫자가 진짜 물리 재고)은 세는 방식이 다르다"고
  적혀 있었는데, 7/10에 "항상 초기한도 역산"으로 바꾸면서 이 구분을 통째로 버렸던 게 원인.
  초기한도를 최소 1000으로 강제하니 재고 11개 → 1000-11=989개로 판매량이 창작됐고, 그
  가짜 수치가 매출 순위 1·2위를 차지해 진짜 예약 판매량을 밀어냄.
- **어떻게 처리했나**: `[예약]` 표기 + 원본 재고가 기준치(처음 1000개로 제안했다가 "1천개에서
  확장해서 2000개 이하는 다 예약수량이아닌 재고수로 봐줘"라는 사용자 지시로 2000개로 확정)를
  둘 다 만족할 때만 예약 상품으로 판정하도록 `isPreorderProduct()` 추가(표기만으로는
  부족함 — "[예약] 파피몬 룩업 (재판)"이 재고 22개인 재판 상품 사례가 실제로 있었음). 예약
  상품만 초기한도 역산, 입고 상품은 판매량을 지어내지 않고 null로 둠. 곁들여 괄호형
  "(N종세트)" 상품은 1세트 팔릴 때 재고가 N개씩 차감되므로 재고/판매량을 N으로 나눠 환산하는
  `parseSetSize()`도 추가(괄호 밖 "2종세트" 같은 상품명은 나누지 않음 — 실제 스냅샷 두 개로
  괄호형은 감소량이 전부 N의 배수, 괄호 밖은 배수가 아님을 확인하고 구분).
- **결과**: 기존 테스트 3건이 옛 동작(이 버그)을 정답으로 못박고 있어서 바로잡고 회귀
  테스트 2건 추가. 실제 스냅샷으로 재생성해서 989개·944개 가짜 수치가 사라지고 진짜 예약
  판매량(857·647·503개)이 순위를 되찾은 것 확인. `npm run verify` 전체 통과.

### 지표 0인데 "경합"으로 나오는 문제 + 시각 차이 표기 모순
- **무슨 문제/요청이 있었나**: 전체 리포트를 자가점검하며 찾은 표시 문제 2건 — 수치 계산
  자체는 정확했지만 보여주는 방식이 헷갈렸음.
- **확인해보니**: (1) 다마고치·보톰즈·사이버포뮬러 등 5건이 리트윗 0·좋아요 0인데 "경합"
  으로 나와서 "박빙이었다"는 뜻으로 읽힘 — 실제로는 양쪽 다 반응이 없었던 것이라 우열을
  논할 근거 자체가 없음. (2) "17:02 / 17:02 → −1분"처럼 시각이 같은데 차이가 있는 것처럼
  보임 — 화면 시각은 분까지만 표시하는데 차이는 초 단위로 계산해 반올림하다 보니(실제
  -0.78분) 생긴 표기상 모순.
- **어떻게 처리했나**: (1) verdict에 '지표 없음'을 추가하고 회색 중립 배지로 구분. (2)
  1분 미만 차이는 "−47초"처럼 초로 표기하고, 부호(누가 먼저 올렸는지)도 초 단위 값 기준으로
  판정하도록 변경(분으로만 반올림하면 0분이 되면서 방향이 사라지는 문제가 있었음).
- **결과**: `npm run verify` 전체 통과.

### 짝이 명백한데 놓치던 매칭 두 유형 (희귀 토큰 1개 / 붙여쓰기 차이)
- **무슨 문제/요청이 있었나**: 리포트의 "매칭 안 된 게시물" 목록을 점검해서 찾은 건 — 눈으로
  보면 명백히 짝인데 자동으로 안 묶이는 케이스 2가지.
- **확인해보니**: (1) "겹친 토큰 2개 이상" 조건을 고정으로 요구하다 보니, 상품명이 짧아서
  남는 공유 키워드가 하나뿐인 게시물이 통째로 빠짐(예: 자사 "... INSIDE FANTASY 헤도라"
  vs 경쟁사 "인사이드 판타지 헤도라" — 라인명 제거 후 공유 토큰이 `헤도라` 하나뿐). (2)
  붙여쓰기 차이로 토큰이 갈려서 겹치는 게 사라짐(자사 "슈퍼커브110" vs 경쟁사 "슈퍼 커브
  110" — 토큰이 `슈퍼커브` 하나 대 `슈퍼`+`커브` 둘로 갈려 공유 토큰이 `혼다` 하나뿐,
  유사도 0.13).
- **어떻게 처리했나**: (1) 그 하나의 공유 토큰이 배치 안에서 두 게시물에만 등장하는
  희귀 토큰(`RARE_TOKEN_MAX_DOCS=2` 이하)이면 오히려 확실한 근거이므로 통과시키도록 변경.
  (2) `sharedTokens()`를 추가해 한쪽이 다른 쪽을 포함하는 경우도 공유로 인정(2글자 이상만
  참여) — 구현 중 첫 매칭에서 멈추면(`break`) `슈퍼커브`가 `슈퍼`만 잡고 `커브`를 놓쳐
  점수가 기준(0.34) 미달인 0.25에 머물렀던 걸, 포함되는 조각을 전부 모으도록 고쳐서 0.38로
  통과시킴.
- **결과**: 8/6 리포트의 실제 캡션으로 두 케이스 모두 매칭 확인. 과병합 부작용 여부도 실제
  수집 데이터로 회귀 확인 — 수정 전후 완전히 동일함(트위터 19개 상품행·최대그룹 4건·확인
  필요 0, 인스타 17개·4건·0). `npm run verify` 전체 통과.

### 상품명 가운데에 점(.)만 덩그러니 남는 문제
- **무슨 문제/요청이 있었나**: 상품명이 "은혼 . Carat 긴토키", "나루토 질풍전 . . 나미카제
  미나토"처럼 나오는 표시 오류.
- **확인해보니**: "G.E.M."을 "GEM"으로 정규화할 때 마지막 점이 남고, 그 뒤 GEM이 라인명으로
  분리되면서 점만 상품명에 남아있는 것 — 앞뒤 기호만 정리하고 가운데는 안 보던 게 원인.
- **어떻게 처리했나**: 글자/숫자가 하나도 없는 토막은 상품명이 될 수 없으므로 제거하도록
  `splitIpAndLine`에 정리 단계 추가.
- **결과**: `npm run verify` 전체 통과, 실제 수집 데이터로 매칭 결과 회귀 없음.

## 2026-08-05

### 문호 스트레이독스 게시물이 왜 미매칭인지
- **무슨 문제/요청이 있었나**: 문호 스트레이독스 게시물 링크를 주며 "왜 수집이 안 됐냐"고
  물음. 실제로는 수집은 됐는데 "매칭 안 됨" 목록에 숨어 있었음.
- **확인해보니**: 자사는 이 서브라인을 "테노히라"(일본어 음역)로 쓰고 경쟁사는 항상
  "G.E.M.시리즈 손바닥 OO"(한글 번역 + 상위 브랜드명)로만 써서 "테노히라"라는 말 자체를
  안 씀(나루토/귀멸의 칼날 등 다른 게시물 4건에서도 같은 패턴). 라인을 정하는 로직
  (`detectProductLine`)은 등록 순서상 먼저 나온 라인명을 우선하는데, 후보 목록
  (`KNOWN_PRODUCT_LINES`)에서 GEM이 테노히라보다 앞에 있어서 하위 서브라인(테노히라)이
  상위 브랜드(GEM) 뒤에 가려짐 — 경쟁사는 항상 line=GEM, 자사는 항상 line=테노히라로 갈려서
  매칭 조건(라인이 정확히 같아야 함)에서 계속 막혔음.
- **어떻게 처리했나**: `KNOWN_PRODUCT_LINES`에서 "테노히라"를 GEM보다 앞으로 옮기고(하위
  서브라인을 상위 브랜드보다 먼저 검사하는 기존 원칙 그대로 적용), "손바닥"을 새 후보로
  추가. `LINE_ALIASES`에 "손바닥" → "테노히라" 별칭도 추가(스케일↔POP와 같은 방식).
- **결과**: `npm run verify` 전체 통과, 실제 캐시 데이터로 해당 게시물이 "문스독(테노히라)
  PW1/BH1"로 정상 매칭됨을 확인, 전체 매칭 결과(19개 상품행/최대그룹 4건) 회귀 없음.

### 인스타 좋아요·댓글이 이유 없이 null로 빠지는 문제 (레이스 컨디션)
- **무슨 문제/요청이 있었나**: 경쟁사(BH) 인스타 게시물 하나(문호 스트레이독스, 실제
  좋아요 303)가 캐시에 likes=null로 남아있어서 원인을 물음. 처음엔 "캐러셀(사진 여러
  장)이라 좌표가 밀렸나" 의심했으나, 다른 정상 수집된 게시물도 똑같이 3장짜리 캐러셀임을
  확인하고 그 가설은 기각.
- **확인해보니**: 페이지 이동 후 무조건 2초 고정으로 기다렸다가(`waitForTimeout(2000)`)
  바로 좋아요/댓글 숫자를 읽고 있었음. 인스타그램은 페이지 뼈대(사진·캡션)를 먼저 그리고
  좋아요/댓글 숫자는 뒤늦게 비동기로 채우는 방식이라, 2초 안에 그 숫자가 실제로 그려졌는지는
  그때그때 네트워크 상황에 좌우되는 레이스 컨디션이었음 — 게시물 구조 문제가 아니라 타이밍
  운의 문제. 07-13 트러블슈팅에 이미 남아있던 "같은 계정·같은 세션인데도 게시물마다
  성공/실패가 갈렸다"는 관찰과 정확히 일치. 07-13엔 "실패하면 무조건 재시도"로 고쳤다가
  전체 100% 실패로 악화시킨 전례가 있어(같은 2초 고정 대기를 반복해서 도움이 안 됐음) 이번엔
  다른 방식으로 접근.
- **어떻게 처리했나**: 무작정 재시도가 아니라 숫자가 실제로 화면에 나타날 때까지 최대 6초
  기다린 뒤 읽도록 변경(`page.waitForFunction`). 6초 넘게 안 나타나도 에러로 죽지 않고
  그대로 진행해 기존 동작(null)과 동일하게 안전히 폴백됨.
- **결과**: 실제 인스타그램은 로그인 세션이 필요해 이 작업 환경(외부망 차단)에서 확인
  불가 — 대신 좋아요 숫자가 페이지 로드 3초 뒤에 뒤늦게 나타나는 상황을 가짜 HTML
  페이지로 재현해서, 예전 방식(2초 고정 대기)은 실제로 그 숫자를 놓치고(null) 새 방식
  (뜰 때까지 대기)은 정상적으로 잡아내는 것(303)을 Playwright로 직접 실행해 확인함. 실제
  인스타그램 계정으로는 사용자가 재수집해서 확인 필요.

## 2026-08-04

### 리포트 수치가 우리 게시물에 없는 숫자로 나오는 문제 (트위터 8,344 / 인스타 34,267)
- **무슨 문제/요청이 있었나**: 트위터 표 1위 "악입문" 행에 리트윗 8,344 · 좋아요 9,453이
  찍혀 있는데, 실제 우리 게시물 중엔 그런 수치가 없다는 신고. 인스타도 1위 "신규" 행에
  좋아요 34,267이 나옴. 특정 게시물 링크를 주면서 "이건 왜 수집이 안 됐냐"고도 물음.
- **확인해보니**: 그 숫자는 게시물 하나의 값이 아니라 **한 행에 잘못 묶인 게시물 수십 개의
  합계**였음(트위터 38건, 인스타 49건이 각각 한 행). "수집 안 됨"으로 보인 게시물도 실은
  수집돼 있었고, 그 거대한 행 안에 섞여서 안 보였던 것. 처음엔 경쟁사가 매 상품마다
  똑같이 쓰는 홍보 문구("제작 결정", "뒷모습 공개", "국내외 사정에 따라 일부 변경") 때문으로
  보고 그 문구들을 매칭 제외 목록에 다섯 차례 추가했는데(`GENERIC_KEYWORDS`), 트위터는
  정리됐지만 **인스타는 문구를 20개 넘게 추가해도 45건에서 거의 줄지 않았음** — 이게
  "문구가 원인이 아니다"는 신호였는데 한동안 "인스타는 문구가 너무 많아 어렵다"로 잘못
  판단했음. 사용자 지적을 받고 구조를 다시 보니 진짜 원인은 게시물을 묶는 방식 자체였음:
  "겹치는 단어가 있으면 이어붙인다"를 사슬처럼 무한히 이어가는 방식이라(`Union-Find`,
  단일연결) A와 B가 묶이고 B와 C가 묶이면 A와 C까지 한 덩어리가 됨. 그래서 여러 상품을
  한꺼번에 언급하는 게시물(월간 신제품 공지, 이벤트 당첨자 발표) 하나가 "다리"가 되면
  무관한 상품이 전부 하나로 뭉쳤음. 반복 문구는 방아쇠일 뿐이라 목록을 늘리는 방식은
  끝이 없었던 것. 같은 증상이 7/6·7/13에도 재발한 이력이 있고, 7/10에 재고 매칭에서는
  이미 이 방식을 걷어냈는데 SNS 매칭엔 그 교훈을 옮기지 않은 상태였음.
- **어떻게 처리했나**: 묶는 방식을 두 가지로 바꿈. (1) 이번에 모은 게시물들 안에서 여러
  게시물에 반복 등장하는 단어일수록 자동으로 비중을 낮춤 — "반복되는 말이 곧 홍보 상용구"
  이므로 사람이 목록을 관리하지 않아도 새 상용구가 저절로 약해짐(`buildTokenWeights`,
  `1 + log(N/df)` 방식의 IDF 가중치). (2) 한 덩어리에 합류하려면 그 덩어리의 **모든**
  게시물과 닮아야 하게 조건을 강화 — 한 쌍만 닮았다고 두 덩어리가 합쳐지는 사슬이 아예
  불가능해짐(`clusterEntriesByProduct`, 완전연결 군집화). 추가로, 공지·이벤트 게시물을
  빼두는 목록이 화면 표시에서만 빠지고 정작 묶는 계산에는 그대로 들어가던 문제도 같이
  수정(`ignorePosts`를 매칭 대상에서 실제로 제외).
- **결과**: 실제 수집 데이터(7/10~8/4)로 다시 만들어 확인함 — 인스타는 45건 한 덩어리가
  상품 16개로 쪼개지고 최대 덩어리가 4건으로 줄어듦, 트위터도 38건 한 덩어리가 상품
  18개로 쪼개짐. 문제였던 "신규 좋아요 34,267"은 정상적인 "신규 1,200"(게시물 1건)이 됨.
  "혹시 잘못 묶였을 수 있음"을 알리던 경고 표시(⚠️ 확인 필요)도 0개가 됨. 자체 검증
  스크립트 전체 통과(`npm run verify`)에 실제 리포트 생성까지 해서 화면으로 확인함.
  참고로 이번에 손으로 추가했던 상용구 20여 개를 전부 되돌려도 결과가 거의 같았음 — 실제
  해결은 구조 교체였다는 뜻이고, 그 목록은 소규모 데이터용 안전망으로만 남겨둠.

### 인스타그램 임베드만 계속 안 보이는 문제
- **무슨 문제/요청이 있었나**: 리포트에서 "보기"를 눌렀을 때 X(트위터) 게시물은 카드로
  보이는데 인스타그램은 5분·30분을 기다려도 링크 상태로만 남음. 광고차단 프로그램 문제인가
  싶어 시크릿창으로도 열어봤지만 동일했음.
- **확인해보니**: 두 서비스의 임베드 처리 함수 모양이 다른데 코드가 그 차이를 감안하지
  않았음. 트위터 쪽은 "이 행만 처리해라"고 범위를 지정할 수 있는데(`widgets.load(row)`),
  인스타그램 쪽은 범위를 줄 수 없어서 부르면 **페이지 전체**의 임베드를 훑음
  (`instgrm.Embeds.process()`는 인자가 없음). 이번 리포트엔 인스타 임베드가 43개 있었고,
  한 행을 펼칠 때 호출된 처리가 아직 접혀 있어 크기가 0인 임베드까지 "처리 완료"로
  소진해버려서, 나중에 그 행을 펼쳐도 다시 그려지지 않았음. 트위터는 범위를 줘서 이 문제가
  없었기 때문에 "X만 되고 인스타는 안 되는" 비대칭이 생긴 것 — 코드의 비대칭이 증상의
  비대칭과 그대로 대응했음.
- **어떻게 처리했나**: 임베드 내용을 처음부터 화면에 두지 않고 보관함에 담아뒀다가, 그 행을
  펼치는 순간에만 실제 화면으로 옮기게 바꿈(`<template class="embed-src">`에 담고
  `mountEmbeds()`로 옮김). 보관함 안의 내용은 화면에 그려지지도, 처리 대상으로 검색되지도
  않으므로 펼친 행의 임베드만 처리됨. 부수 효과로 임베드 수십 개를 미리 화면에 두지 않게
  되어 리포트를 처음 열 때도 가벼워짐.
- **결과**: 실제 브라우저로 조작해서 확인함(Playwright) — 열기 전에는 화면에 올라간 임베드
  0개·보관함 대기 70개, 첫 행을 펼치면 그 행의 4개만 화면으로 올라오고 링크가 즉시 보이며,
  둘째 행을 펼쳐도 두 행이 각자 4개씩 정상 보유함(첫 행 처리에 소진되지 않음). 단
  **임베드 카드 자체가 최종적으로 그려지는지는 확인 못 했음** — 카드 렌더링은 트위터/인스타
  서버에서 위젯 스크립트를 받아와야 하는데 작업 환경이 외부망 차단이라 불가능했음. 위
  검증은 "펼친 행의 임베드만 처리 대상이 된다"는 동작까지이므로, 실제 카드가 뜨는지는
  사용자 환경에서 한 번 확인이 필요함.

### "대시보드 열기"를 눌렀는데 연결이 거부됨
- **무슨 문제/요청이 있었나**: 다운로드 안내 페이지의 "이미 설치했다면 → 대시보드 열기"를
  눌렀는데 `ERR_CONNECTION_REFUSED`(연결 거부)가 뜸.
- **확인해보니**: 코드가 변경된 건 없었음. 그 버튼은 주소창을 `http://localhost:4848`로
  이동시키는 링크일 뿐이고 프로그램을 켜주는 기능이 없어서, 리포트 프로그램(로컬 서버)이
  꺼져 있는 상태에서 누르면 응답할 상대가 없어 거부되는 게 정상 동작이었음. 이 버튼은
  "프로그램이 이미 켜져 있을 때 화면만 다시 열기" 용도.
- **어떻게 처리했나**: 우선 순서를 안내함(바탕화면 아이콘 `start-dashboard.bat`으로
  프로그램을 먼저 켠 뒤에 그 버튼을 쓰는 것). 전용 주소 방식(커스텀 프로토콜, 예:
  `snsreport://`)으로 웹페이지 버튼만으로 항상 실행되게 하는 건 설치 파일을 손봐야 하고
  기존 사용자는 재설치가 필요해서 보류하고, 그 대신 사용자가 확인해준 문구 수정안(바꾸기
  전에 컨펌 요청)을 반영 — 캡션을 "'SNS 리포트 실행'을 이미 실행 중이라면"으로 바꿔 실제
  필요 조건(설치 여부가 아니라 지금 켜져 있는지)과 맞추고, 버튼에 마우스오버 툴팁을 추가해
  눌러보기 전에 조건을 확인할 수 있게 하고, "잘 안 될 때" 섹션에도 이 증상 항목을 추가함.
- **결과**: 문구/툴팁 수정 반영. 실제로 이 조건(도구가 꺼져 있을 때 눌러서 거부되는 상황)을
  다시 겪는지는 이후 문의로 확인 예정.

### 지난 작업 파일이 없다고 잘못 답한 문제 (작업 환경 문제)
- **무슨 문제/요청이 있었나**: 매칭 코드를 찾는 과정에서 "이 프로젝트 폴더엔 파일이 4개뿐이라
  해당 기능이 저장소에 없다"고 잘못 답해 사용자가 크게 당황함. 다른 프로젝트(b2b 스크래퍼)를
  잘못 끌어와 언급한 실수도 있었음.
- **확인해보니**: 작업용 로컬 사본이 이 프로젝트 브랜치의 **아주 옛 시점(초안 4개 파일
  단계)에 멈춰 있었음** — 원격 브랜치에는 150개가 넘는 커밋이 이미 있는데 그 최신 내용을
  받지 않은 상태였고, 그걸 확인하지 않고 "없다"고 단정한 것이 원인.
- **어떻게 처리했나**: 원격 브랜치의 최신 내용을 직접 조회해서 파일 목록과 코드를 확인
  (`git ls-tree`/`git show`로 원격 커밋에서 바로 읽음). 두 이력을 억지로 합치면 기존 커밋이
  손상될 위험이 있어 병합은 하지 않고, 원격 브랜치 기준으로 작업 위치를 새로 맞춤.
- **결과**: 이후 파일을 정상적으로 찾아 원인 진단까지 진행함. 사용자가 계속 지적해준 브랜치
  경고 메시지도 작업 위치를 원격 기준으로 맞추면서 정리됨.

## 2026-07-22

### 업데이트 받았는데 새로 추가된 기능이 오류를 내는 문제
- **무슨 문제/요청이 있었나**: 스크린샷 기능이 추가된 뒤 업데이트를 받았는데, 특정 부품
  파일을 찾을 수 없다는 오류가 뜨며 실행이 안 됨.
- **확인해보니**: 업데이트 과정은 원래 "코드만 새로 받아오고, 이미 설치돼 있는 부품들은
  다시 안 건드린다"는 방식으로 짜여 있었음(복사 과정에서 `node_modules` 폴더는 항상
  제외). 그런데 스크린샷 기능을 만들면서 새로운 부품(`html2canvas`)이 하나 추가됐는데,
  업데이트 과정이 이 새로 추가된 부품까지는 자동으로 설치해주지 않아서 생긴 문제였음.
- **어떻게 처리했나**: 업데이트 과정(`update.bat`) 마지막에, 새로 추가된 부품이 있으면
  자동으로 같이 설치하는 단계(`npm install`)를 추가.
- **결과**: 이후로는 업데이트 한 번으로 새로 추가된 부품까지 같이 설치되도록 함. (다음
  실제 업데이트 때 정상 설치되는지는 계속 지켜볼 부분)

### 리포트 내용을 노션에 자동으로 보내고 싶다
- **무슨 문제/요청이 있었나**: 팀에서 쓰는 노션 문서에 리포트 표를 매번 손으로 옮기지 않고
  자동으로 보낼 방법을 문의.
- **확인해보니**: 노션과 직접 연결하는 정식 방법(노션 API 연동)이 있지만, 이 방법을 쓰려면
  회사 노션 관리자가 미리 "이 연결을 허용한다"고 승인해줘야 하는 구조였음 — 실제로
  시도해보니 이 승인이 회사 정책상 막혀 있어서, 코드로는 우회할 수 없는 관리자 영역
  문제로 확인됨. 관리자에게 따로 요청하는 건 부담스럽다는 뜻을 밝혀서, 그 대신 다른
  방법을 찾기로 함.
- **어떻게 처리했나**: 우선 노션 쪽 승인 없이도 바로 쓸 수 있는 방법으로, 리포트 내용을
  노션에 붙여넣기 좋은 표 형태의 글로 자동으로 만들어서 복사 버튼 하나로 가져갈 수 있게
  만듦(`buildMarkdownExport()`). 이후 알아보니 개인(또는 회사) 노션 계정으로 직접 연결을
  새로 만드는 방법(내부 통합)은 관리자 승인 없이도 가능하다는 걸 확인해서, 이 방법으로
  실제 자동 전송 기능(`notion-export.js`)도 별도로 만듦 — 개인 계정으로 연결을 새로
  만들고, 리포트를 보낼 노션 페이지에 그 연결을 허용해주기만 하면 버튼 한 번으로 표가
  그 페이지에 만들어지는 방식.
- **결과**: 복사-붙여넣기 방식은 바로 확인됨. 자동 전송 방식은 처음에 페이지 위치를
  잘못 지정해서(다른 예시 페이지 주소를 그대로 씀) 안 됐었는데, 본인 계정 안의 올바른
  페이지로 다시 설정한 뒤 실제로 노션에 표가 만들어지는 것까지 화면으로 확인함.

### 노션 표에 색깔·그래프 등 시각적으로 보기 좋게 개선
- **무슨 문제/요청이 있었나**: 자동 전송되는 노션 표가 글자만 있어서 밋밋하다며, 자사/
  경쟁사를 색깔로 구분해달라는 요청. 이어서 색깔 구분만으로는 한눈에 비교가 잘 안 된다며,
  막대그래프처럼 크기를 눈으로 비교할 수 있는 형태로 바꿀 수 있는지 문의. 또 이왕이면
  노션에서 흔히 보이는 트윗 카드 형태(2칸 나열)로 넣을 수 있는지도 확인 요청.
- **확인해보니**: 노션 표 셀 하나에 배경색을 입히는 것과, 글자를 색깔 블록 여러 개로
  나눠 넣어서 막대그래프처럼 보이게 만드는 것 둘 다 가능함을 확인. 다만 트윗을 눌러서
  붙여넣을 때 노션이 자동으로 만들어주는 "진짜 카드" 형태는, 노션이 화면에서 사람이
  직접 붙여넣을 때만 특별히 만들어주는 것이라 프로그램을 통한 자동 전송 방식으로는
  똑같이 재현이 불가능하다는 것을 실제 테스트로 확인함(직접 시도해봤지만 일반 링크
  형태로만 들어가고 카드로는 안 바뀜).
- **어떻게 처리했나**: 자사는 파란색, 경쟁사는 빨간색 배경(`blue_background`/
  `red_background`)을 표에 입히고, 우세/경합/약세 결과도 색깔로 구분되게 추가. 이어서
  숫자 칸 옆에 자사/경쟁사 비율만큼 색깔 블록을 채운 막대그래프 칸(`buildBarCell()`)을
  새로 추가. 트윗 카드 형태는 재현이 안 된다는 점을 안내하고, 그 부분은 보류.
- **결과**: 실제로 노션에 전송해본 화면을 확인 — 색깔 구분과 막대그래프 모두 정상적으로
  보임. 매우 만족스럽다는 반응 확인.

### 노션 표 칸 이름 줄이기 + 칸 너비가 매번 초기화되는 문제
- **무슨 문제/요청이 있었나**: 표에 그래프 칸까지 늘어나며 자리가 좁아지자, 칸 이름을
  줄여 공간을 아껴달라는 요청. 이어서 노션에서 손으로 칸 너비를 맞춰놔도 다음에 다시
  보낼 때마다 원래대로 되돌아가는 게 불편하다며, 칸 너비를 아예 프로그램에서 지정할 수
  있는지 문의.
- **확인해보니**: 칸 이름은 글자 대신 짧은 그림 문자(이모지) 하나로도 표현 가능해서
  공간을 크게 아낄 수 있음을 확인. 칸 너비는, 노션이 표를 다시 만들 때마다 매번 "새 표"로
  인식해서 예전에 손으로 맞춘 너비 정보가 통째로 사라지는 게 원인이었음 — 그리고 칸
  너비 자체를 프로그램에서 직접 지정하는 기능은, 노션이 공식적으로 지원한다고 확실히
  확인되지 않은 부분이라 섣불리 손대면 오히려 지금 되던 것까지 깨질 위험이 있다고 판단.
- **어떻게 처리했나**: 칸 이름을 전부 짧은 그림 문자 하나로 교체(`FIELD_LABELS`를 이모지로
  변경). 칸 너비 문제는, 매번 새 표를 만드는 대신 처음 한 번만 표를 만들고(그때 페이지·
  표 블록 위치를 `notion-config.json`에 저장) 그다음부터는 그 표 안의 내용(행)만 지우고
  새로 채워 넣는 방식으로 구조를 바꿔서, 노션에 저장된 표 자체가 유지되게 함(표가
  유지되면 손으로 맞춘 너비도 같이 유지될 것으로 기대).
- **결과**: 칸 이름 압축은 바로 확인됨. 칸 너비 유지 방식은 "이상하면 그냥 안 쓰기로" 하는
  조건으로 우선 적용해두었고, 실제로 다음 전송부터 정말 같은 표가 갱신되고 너비가
  유지되는지는 아직 실기기에서 확인 전 — 확인되는 대로 이 기록에 결과를 이어서 남길 예정.

## 2026-07-21

### 트위터 로그인이 맞는 비밀번호도 계속 틀렸다고 거부하다가 결국 잠기는 문제
- **무슨 문제/요청이 있었나**: 로그인 화면에서 분명 맞는 비밀번호를 쳤는데 "틀렸다"는
  안내만 계속 뜨고, 다른 방법으로도 그 비밀번호가 맞다는 걸 재차 확인했는데도 마찬가지.
  결국 여러 번 시도하다 보니 "시도 횟수를 초과했다"며 계정이 한동안 잠김. "일단 기다렸다
  다시 해보자" 정도의 대응은 근본적인 해결이 아니라며, 방식 자체를 바꿔달라고 요청.
- **확인해보니**: 자동화 프로그램이 로그인할 때 쓰는 전용 내장 브라우저를 트위터 쪽에서
  "사람이 아닐 수도 있다"고 의심해서 맞는 비밀번호도 그냥 차단해버리고 있었던 것으로
  확인됨. 즉 비밀번호 문제가 전혀 아니라, 로그인하는 브라우저 자체가 의심받고 있었던 것.
- **어떻게 처리했나**: 자동화 전용 내장 브라우저 대신, 이 컴퓨터에 실제로 깔려 있는 평소
  쓰는 크롬을 그대로 빌려서 로그인하도록 방식을 바꿈(`chromium.launchPersistentContext`로
  시스템 크롬을 전용 프로필에서 실행). 다만 평소 쓰는 개인 크롬 창과 완전히 분리된,
  로그인 전용의 새 크롬 서랍(프로필)을 하나 따로 만들어서(`chrome-profile/{platform}`
  폴더) 그 안에서만 로그인하고 저장하게 함 — 평소 쓰던 크롬 기록·로그인 정보와는 전혀
  섞이지 않음. 혹시 이 컴퓨터에 크롬이 아예 안 깔려 있는 경우를 대비해서, 그럴 땐 예전
  방식(자동화 전용 내장 브라우저, `chromium.launch()`)으로 자동으로 대신 실행되도록
  안전장치도 같이 마련.
- **결과**: 실제로 다시 로그인을 시도해본 결과 더 이상 비밀번호가 거부되지 않고 정상적으로
  처리됨을 확인.

### 리포트에서 트위터/인스타 표만 이미지로 따로 저장하고 싶다
- **무슨 문제/요청이 있었나**: 리포트 화면 전체가 아니라, 트위터 표와 인스타 표를 각각
  따로 이미지 파일로 저장할 수 있는 버튼을 추가해달라는 요청. 오류만 없으면 된다는 조건.
- **확인해보니**: 화면의 특정 영역만 그림으로 떠내는 별도 부품(`html2canvas`)을 새로
  가져오면 구현 가능. 처음 만들어서 테스트해보니, 캡처 버튼 글자와 원형 비율 그래프
  (그러데이션 형태를 이 부품이 못 그려서 회색으로 깨져 보임)가 저장된 이미지에 그대로
  찍혀 나오는 문제가 있었음.
- **어떻게 처리했나**: 캡처하는 그 순간에만 캡처 버튼과 원형 그래프를 잠깐 안 보이게
  숨겼다가(`visibility:hidden`), 캡처가 끝나면 다시 원래대로 보이게 처리. 저장되는 파일
  이름이 엉뚱한 기본값으로 나오는 문제도 있어서, 파일을 내려받는 방식을 `canvas.toBlob()`
  + `URL.createObjectURL()`로 교체.
- **결과**: 실제로 버튼을 눌러 트위터 표/인스타 표 각각을 캡처해본 결과, 불필요한 버튼이나
  깨진 그래프 없이 깔끔한 이미지로 저장되는 것을 확인.

## 2026-07-20

### 집에서 팀 배포용 꾸러미 처음부터 만들기
- **무슨 문제/요청이 있었나**: 회사 컴퓨터가 아니라 집 컴퓨터에서, 아무것도 안 깔려 있는
  상태부터 팀에 나눠줄 배포판을 만들 수 있는지 문의. 깃(버전 관리 프로그램)도 안 깔려
  있었음.
- **확인해보니**: 처음부터 순서대로 안내하면 되는 작업이었음 — 깃 설치, 프로그램 실행에
  필요한 별도 프로그램 내려받아 폴더 안에 넣기, 필요한 부품들과 크롤링용 내장 브라우저까지
  그 폴더 안에 설치.
- **어떻게 처리했나**: 각 단계를 하나씩 순서대로 안내하면서 중간중간 나오는 화면/오류를
  같이 보면서 진행. 중간에 인터넷 저장 공간 문제로 다운로드가 멈추는 것처럼 보이는 순간이
  있었으나 실제로는 다운로드가 끝까지 된 것으로 확인.
- **결과**: 대시보드 화면이 뜨고 로그인 버튼을 눌러 실제 로그인 창이 뜨는 것까지 확인 —
  배포판 재료 준비가 끝남.

### 배포용 실행 파일을 열면 한글이 깨져서 실행이 안 되는 문제 (여러 차례 반복)
- **무슨 문제/요청이 있었나**: 대시보드를 켜는 실행 파일과 업데이트용 실행 파일을 열면
  화면에 알아볼 수 없는 글자가 뜨면서 실행이 안 됨. 파일이 저장되는 방식을 바꿔가며 여러
  번 다시 시도했지만 매번 비슷한 증상으로 실패.
- **확인해보니**: 실행 파일 안에 한글 문구가 들어 있으면, 그 파일을 저장하는 방식에 따라
  컴퓨터가 문장을 통째로 다른 명령으로 잘못 알아듣는 경우가 있었음. 저장 방식을 이리저리
  바꿔봐도 근본적으로 "한글이 들어있다"는 것 자체가 계속 말썽의 씨앗이었음. 또한 깃허브
  페이지에서 파일을 내려받아 저장하는 방식 자체도 실제로는 로컬 파일이 안 바뀌는 경우가
  반복돼서, 다시 받는 방식으로는 해결이 안 됨을 확인.
- **어떻게 처리했나**: 두 실행 파일 모두 한글 문구를 아예 빼고 영어로만 다시 작성. 또
  깃허브에서 파일을 다시 받는 방식 대신, 전체 내용을 그대로 복사해서 메모장에 붙여넣고
  저장하는 방식으로 바꿈.
- **결과**: 이후로는 실행 파일이 깨지지 않고 정상적으로 실행됨을 확인. 업데이트용 실행
  파일 이름도 한글에서 영어로 바꿈(더 안정적이라서).

### 로그인 완료를 눌러도 버튼이 계속 눌리지 않는 문제 (여러 차례 반복)
- **무슨 문제/요청이 있었나**: 트위터 로그인을 마치고 대시보드의 "로그인 완료했어요"
  버튼을 눌러도, 화면의 다른 버튼들이 계속 눌리지 않는 상태(마우스 커서가 금지 표시로
  바뀜)로 남는 문제. 한 번 고쳤다고 생각했는데도 다른 상황에서 똑같은 증상이 반복됨.
- **확인해보니**: 원인이 하나가 아니라 여러 개였음. (1) 로그인 완료 버튼 자체도 다른
  버튼들과 함께 잠기도록 걸려 있어서 정작 그 버튼을 못 누르는 경우 (2) 화면을 새로고침한
  뒤 다시 접속했을 때, 이미 끝난 작업인데도 화면이 그 사실을 못 알아채고 계속 잠긴 채로
  남는 경우 (3) 가장 근본적인 원인 — 로그인 세션을 저장한 뒤 로그인창(브라우저)을 닫는
  과정이 간혹 끝나지 않고 멈춰버려서, 저장은 이미 다 끝났는데도 뒷단 프로그램이 "아직
  진행 중"이라는 상태로 영원히 남아있던 것.
- **어떻게 처리했나**: (1)(2)는 화면 쪽 잠금/새로고침 로직을 고쳐서 해결. (3)은 로그인창을
  닫는 과정에 제한 시간을 두고, 그 시간이 지나면 저장이 끝났으니 그냥 다음 단계로 넘어가게
  해서 영원히 멈추는 일 자체를 없앰.
- **결과**: 실제로 다시 로그인해서 완료 버튼을 눌러본 결과 버튼이 정상적으로 풀리는 것
  확인. 이후로도 재발 신고가 없어서 근본 원인이 (3)이었던 것으로 판단.

### 경쟁사 게시물 하나가 오늘도 또 빠졌다 (헌터x헌터 반복 누락)
- **무슨 문제/요청이 있었나**: 실제로 존재하는 경쟁사의 평범한 게시물 하나가 이번에도
  수집 결과에서 계속 빠짐 — 예전에도 같은 상품에서 비슷한 문제가 있었다며 재차 확인 요청.
  처음엔 "그 게시물이 원래 안 올라온 것 아니냐"고 잘못 짐작했다가, 실제 게시물 화면을
  받아서 확인한 뒤 진짜 존재하는 정상 게시물임을 확인.
- **확인해보니**: 화면에 "재게시함" 같은 표시가 붙은 글은 원래 걸러내고, 그 대신 그 글이
  원래 있던 자리(자기가 처음 올린 자리)에서 나중에 자연스럽게 다시 잡히도록 설계돼 있었음.
  그런데 이 게시물은 그 계정이 자기 글을 계속 반복해서 재게시하고 있어서, "원래 있던 자리"
  자체가 항상 그 재게시 표시로 덮여버려 한 번도 안 걸리고 매번 통째로 누락되고 있었던 것.
- **어떻게 처리했나**: "재게시함" 표시가 붙어 있어도, 그게 그 계정이 자기 자신의 글을
  다시 올린 경우라면 걸러내지 않고 그대로 집계하도록 예외 처리 추가.
- **결과**: 사용자가 이 재게시 게시물도 원래 집계 대상이 맞다고 재확인해줌(이전에도 이미
  합의된 방침). 처리 후 이 계정의 게시물이 정상적으로 잡히는지는 다음 수집에서 확인 예정.

### 기간 지정 화면이 접혀 있어서 불편하고, 날짜 두 번 고르는 게 번거롭다
- **무슨 문제/요청이 있었나**: "특정 기간 지정" 영역이 평소엔 접혀 있어서 펼치는 손이
  하나 더 들어감. 그리고 시작일을 고르면 종료일도 보통 같은 날인 경우가 많은데 매번
  따로 또 골라야 하는 게 번거로움.
- **어떻게 처리했나**: 그 영역을 접었다 펴는 방식을 없애고 항상 펼쳐진 상태로 고정. 그리고
  시작일을 고르면 종료일 칸에도 같은 날짜가 자동으로 채워지도록 함(원하면 종료일만 따로
  다시 바꿀 수 있음).
- **결과**: 클릭 한 번과 날짜 선택 한 번이 줄어듦.

### 팀 배포판을 어디서 어떻게 나눠줄지 다시 설계
- **무슨 문제/요청이 있었나**: 배포판을 실제 릴리스로 올리려는데, 지금 쓰는 저장소를
  그대로 공개하면 내부 문서나 세세한 개발 기록까지 팀원이 다 보게 되는 게 부담스럽다는
  고민. 또 허브 페이지의 작은 카드 하나에 설치 안내를 다 욱여넣기엔 처음 보는 사람이
  이해하기 어렵다는 지적.
- **확인해보니**: 릴리스는 저장소 전체가 아니라 이 프로젝트 폴더만 압축해서 올리는 것이라
  다른 프로젝트에 영향이 없다는 것부터 확인. 다만 저장소 자체를 그대로 쓰면 내부 문서가
  같이 노출되는 건 맞음.
- **어떻게 처리했나**: 팀에게 나눠줄 몫만 담을 별도의 새 저장소를 따로 파서 그쪽으로
  릴리스를 올리기로 함. 그리고 허브 페이지의 카드는 다운로드 버튼만 남기고, 설치 방법·
  주의사항·문제 해결법은 전용 안내 페이지를 새로 만들어 그쪽으로 옮김.
- **결과**: 새 안내 페이지가 만들어졌고, 허브 페이지(개발용 브랜치와 실제 서비스용 양쪽
  다)에서 그 페이지로 연결되도록 반영함.

### 다운로드 안내 페이지 다듬기 — 버튼 배치, 개인정보·주의사항 안내
- **무슨 문제/요청이 있었나**: 다운로드 버튼과 "이미 설치했다면 여기로" 안내가 세로로
  나열돼 있어서 답답해 보인다며 나란히 두 칸으로 배치해달라는 요청. 그리고 "이미
  설치했다면" 링크에 밑줄이 왜 있는지 궁금해함. 또 잘 안 될 때 안내가 어떤 상황을 말하는
  건지 모호해서 아무 때나 따라 할까 걱정된다는 지적. 마지막으로 로그인 정보를 수집하지
  않는다는 안내가 빠져 있다는 점도 짚음.
- **확인해보니**: 밑줄은 그 링크 하나만 밑줄 없애는 처리를 깜빡해서 생긴 것이었음(의도한
  디자인이 아니라 실수). 잘 안 될 때 항목은 증상 설명 없이 대응만 나열돼 있어서 모호해
  보일 수 있었음.
- **어떻게 처리했나**: 다운로드 버튼과 "이미 설치했다면" 버튼을 나란히 두 칸으로 배치(왼쪽
  버튼 안에는 용량 안내도 작은 글씨로 같이 넣음). 잘 안 될 때 항목은 "이런 증상이면 → 이렇게
  하세요" 형태로 증상과 대응을 짝지어 나눠 다시 씀. 로그인 정보는 본인 컴퓨터에만 저장되고
  외부로 전송되지 않으며, 수집하는 건 공개된 게시물 실적일 뿐 개인정보가 아니라는 안내
  문구를 새로 추가.
- **결과**: 실제 화면 미리보기로 배치와 문구를 확인한 뒤 반영. 두 저장소(개발용/실서비스용)
  양쪽에 다 적용함.

### 압축 파일 대신 설치 파일 하나로 배포하는 방식으로 전환
- **무슨 문제/요청이 있었나**: 지금처럼 "압축 파일 받기 → 직접 풀기 → 실행 파일 찾아서
  누르기"가 단계가 많다며, 자동으로 설치되는 방식(예: 흔히 보는 설치 마법사)으로 바꿀 수
  없는지 문의. 그 뒤로 이어서, 나중에 업데이트할 때도 설치 파일을 다시 만들어야 하는지,
  프로그램을 컴퓨터의 어느 위치에 설치해야 하는지, 실행 아이콘이 따로 생기는 게 맞는지
  등 설치 방식 전반에 대해 여러 차례 질문.
- **확인해보니**: 프로그램을 하나로 합쳐 실행 파일 자체로 만드는 방법은 이 프로그램이 쓰는
  내장 브라우저 구조상 불가능함을 확인. 대신 지금 폴더 구조를 그대로 감싸서 "설치 마법사"
  형태로 만들어주는 방식이 있고, 이 방식이면 지금까지 만들어둔 것을 하나도 바꿀 필요가
  없음을 확인. 다만 설치 위치를 잘못 잡으면(관리자 전용 구역) 실행 중 저장이 막혀서
  "수집은 되는데 저장이 안 되는" 문제로 이어질 수 있다는 점도 미리 확인.
- **어떻게 처리했나**: 설치 마법사를 만드는 안내서(레시피 파일)를 새로 작성 — 설치 위치를
  개인 저장 공간으로 고정하고, 관리자 권한 없이 설치되도록 하고, 바탕화면과 시작 메뉴에
  "실행"과 "업데이트" 아이콘을 각각 만들도록 구성. 평소 코드 개선은 여전히 업데이트 아이콘
  하나로 끝나고, 설치 마법사를 다시 만들 일은 프로그램 구조 자체가 바뀌는 아주 드문 경우
  뿐이라는 점도 정리해서 안내.
- **결과**: 실제로 이 레시피로 설치 파일을 만들어본 결과, 다른 컴퓨터에서 실행했을 때
  설치 마법사가 정상적으로 뜨고, 완료 후 생긴 바탕화면 아이콘으로 대시보드가 실제로
  켜지는 것까지 확인. 처음 실행 시 뜨는 윈도우 방화벽 허용 팝업은 정상적인 것으로 확인.
  이후 다운로드 안내 페이지도 설치 마법사 방식에 맞게 안내문을 다시 씀(압축 풀기 안내
  삭제, 바탕화면 아이콘 안내로 교체) 및 다운로드 버튼을 실제 설치 파일 직접 연결로 교체.

### 배포용 안내 페이지가 오래도록 갱신되지 않는 문제
- **무슨 문제/요청이 있었나**: 안내 페이지를 고쳐서 반영했는데도 실제 화면에는 예전 내용이
  계속 보인다는 신고. 브라우저를 새로고침해도 그대로였음. 또 화면에 뜬 관리 목록에서 이전
  작업이 "취소됨" 표시로 나오는 것을 보고 이것도 오류인지 문의.
- **확인해보니**: 파일 자체는 정상적으로 바뀌어 있었음(직접 원본을 대조해서 확인). 안내
  페이지로 내보내는 절차가 계속 순서를 기다리는 상태로 멈춰 있었던 것. "취소됨" 표시는
  그 사이 여러 번 다시 고쳐서 올리다 보니, 이미 순서를 기다리던 오래된 내보내기 작업이
  더 최신 것으로 자동으로 대체되면서 생긴 정상적인 현상이었음(오류 아님). 최종적으로는
  깃허브 서비스 자체의 일부 기능(자동 내보내기)이 그 시점에 실제로 문제를 겪고 있었다는
  것을 깃허브의 공식 상태 안내를 통해 확인.
- **어떻게 처리했나**: 별도로 손댈 부분은 없었음 — 원인이 이쪽 설정이 아니라 깃허브 쪽
  서비스 문제였기 때문에, 서비스가 정상화되기를 기다리는 것으로 정리.
- **결과**: 서비스 쪽 문제로 확인됐고, 정상화되면 이미 반영해둔 내용이 자동으로 화면에
  나타날 것으로 예상.

### 대시보드 화면 없이 명령어로 직접 쓰는 게 나은지 문의
- **무슨 문제/요청이 있었나**: 팀원용으로 만든 화면(대시보드)을 거치지 않고, 본인이 쓸
  때는 그냥 명령어 창에서 바로 실행하는 게 더 나은지 문의.
- **확인해보니**: 화면의 각 버튼은 그 뒤에서 결국 명령어 하나씩을 그대로 실행하는 것에
  불과함을 확인 — 화면이 뭔가 추가로 처리해주는 게 아니라 그대로 전달만 하는 구조였음.
  로그인 완료 버튼도 마찬가지로, 명령어 창에서 로그인 마치고 그냥 엔터를 치는 것과 동일한
  효과.
- **어떻게 처리했나**: 각 화면 버튼에 대응하는 정확한 명령어를 정리해서 안내.
- **결과**: 실제 컴퓨터에 이미 필요한 프로그램이 설치돼 있는 것을 확인했고, 명령어로 바로
  실행하는 방식으로도 화면과 완전히 동일하게 동작함을 확인.

## 2026-07-16

### run 명령어와 계정 하나만 보는 명령어가 헷갈린다
- **무슨 문제/요청이 있었나**: 자사·경쟁사를 항상 비교하는 원래 명령어(run)가, 계정
  하나만 따로 보는 명령어랑 이름이 비슷해서 헷갈릴 수 있다는 지적. 구분 지을 방법을 상의함.
- **확인해보니**: 파일 이름 자체를 바꾸는 게 가장 확실하다고 판단.
- **어떻게 처리했나**: 항상 자사·경쟁사를 비교하는 명령어 파일 이름 자체를 바꿔서 용도가
  이름만 보고도 구분되게 함. 관련된 다른 안내 문구들도 다 새 이름으로 맞춤.
- **결과**: 새 이름으로 실행해서 정상 동작하는지 확인함.

### 매번 날짜를 두 번 입력하는 게 번거롭다
- **무슨 문제/요청이 있었나**: 오늘 하루만 보고 싶을 때도 시작일·종료일을 매번 손으로
  입력해야 해서 번거로움 — "today"처럼 짧은 말로 대체할 수 있는지 물어봄.
- **확인해보니**: 오늘 날짜를 계산해서 시작·종료에 그대로 넣어주면 되는 간단한 방식으로
  가능. 다만 서버가 어느 나라 시간대로 돌아가든 상관없이 한국 시간 기준으로 "오늘"을
  계산해야 날짜가 어긋나지 않음.
- **어떻게 처리했나**: "today"라고만 입력하면 한국 시간 기준 오늘 날짜로 자동으로
  채워지게 함. 덧붙여, 예전부터 있던 설명 문구 중 "종료일은 포함 안 됨"이라고 적혀
  있던 게 실제 동작과 반대라는 것도 이번에 같이 발견해서 바로잡음 — 실제로는 종료일도
  포함되는 방식이었는데, 반대로 알고 있으면 하루치만 보려다가 실수로 이틀치를 수집했을
  수 있음.
- **결과**: 실제로 입력해서 오늘 날짜가 정확히 들어가는지 확인함.

### 미매칭 게시물 두 건, 직접 짝지어 달라는 요청
- **무슨 문제/요청이 있었나**: 리포트에 안 묶인 게시물 목록을 보고, 특정 번호끼리("이벤트는
  몇 번, 신제품 안내는 몇 번") 직접 짝지어 달라고 요청.
- **확인해보니**: 이미 만들어져 있던 "번호로 수동 짝짓기" 기능을 그대로 쓰면 됨.
- **어떻게 처리했나**: 알려준 번호에 맞는 게시물끼리 짝지어서 목록에 추가.
- **결과**: 반영 확인.

### 이 상품(신작 발표 게시물)은 왜 수집이 안 됐을까
- **무슨 문제/요청이 있었나**: 실제로 올라온 게 확실한 게시물인데 리포트에 전혀 안
  보인다는 문제 제기. 리트윗도 답글도 아니고 다른 정상 게시물이랑 형식이 똑같다고 함.
- **확인해보니**: 처음엔 "고정된 게시물이라 걸러졌을 것"이라고 짐작했으나 아니었고,
  다음엔 "답글이라 못 봤을 것"이라 짐작했으나 그것도 아니었음. 실제로 게시물 번호
  순서를 확인해보니, 이 게시물 바로 앞뒤에 있던 다른 게시물들은 정상적으로 수집됐다는
  걸 확인 — 수집 범위 밖으로 넘어간 게 아니라 이 게시물 하나만 화면에서 인식이 안 된
  것으로 추정. 이 게시물이 다른 정상 게시물과 다른 점은 상품 링크가 3개나 붙어있어서
  화면에서 유난히 길다는 것.
- **어떻게 처리했나**: (1) 화면을 아래로 넘기는 폭을 줄여서, 유난히 긴 게시물 하나를
  건너뛸 확률을 낮춤. (2) 그래도 확실한 원인을 못 찾아서, 게시물이 걸러질 때마다 그
  이유를 기록해두는 진단 기능을 추가 — 다음에 똑같은 문제가 또 생기면 왜 걸러졌는지
  바로 확인 가능하게.
- **결과**: 스크롤 폭을 줄인 뒤로는 이 게시물이 실제로 잡히기 시작함 — 다만 100% 원인
  확정은 아니고, 우연히 나아진 것일 수도 있어서 계속 지켜봐야 함.

### 실행하다가 실수로 옛날 기본 기간이 통째로 수집될 뻔함
- **무슨 문제/요청이 있었나**: 날짜를 안 넣고 그냥 실행했다가, 미리 정해둔 옛날 테스트용
  기본 기간으로 조용히 넘어가려는 걸 보고 직접 중단시킴. 날짜 안 넣으면 안내 문구부터
  뜨게 하는 게 낫겠다고 제안.
- **확인해보니**: 맞는 지적 — 날짜를 깜빡하고 실행했을 때 조용히 엉뚱한 기간이 수집되는
  사고로 이어질 수 있었음.
- **어떻게 처리했나**: 날짜(또는 "today")를 안 넣고 실행하면 무조건 에러 문구를 띄우고
  멈추도록 바꿈 — 옛날 기본 기간 자체를 없앰.
- **결과**: 날짜 없이 실행하면 안내 문구가 바로 뜨는 것 확인.

### 트위터가 갑자기 0건 수집됨
- **무슨 문제/요청이 있었나**: 위 사고 직후 재실행했더니 이번엔 트위터만 자사·경쟁사
  둘 다 0건으로 나옴. 로그인은 정상인데 왜 이런지 물어봄.
- **확인해보니**: 처음엔 "화면이 늦게 뜨는 문제"로 짐작했으나, 이후 재현 안 되고 다시
  정상적으로 수집됨 — 일회성 문제였던 것으로 보이나 완전히 확정은 못 함.
- **어떻게 처리했나**: 재현 안 되는 문제라 별도 수정 없이, 다시 실행해서 정상 수집되는지로
  확인.
- **결과**: 재실행 후 정상 수집 확인.

### 재고 추이 그래프가 며칠 전 시점에서 멈춰있음
- **무슨 문제/요청이 있었나**: 재고 스냅샷을 오늘도 찍고 며칠 전에도 찍었는데, 화면의
  추이 그래프는 며칠 전 시점에서 멈춰있음. 실적 수집할 때마다 재고 스냅샷도 자동으로
  같이 남겨줬으면 좋겠다고 요청.
- **확인해보니**: 재고 스냅샷을 새로 찍는 건 완전히 별도의 명령어였고, SNS 실적을
  수집하는 명령어는 이미 저장된 스냅샷을 읽기만 할 뿐 새로 찍지는 않고 있었음 — 그래서
  스냅샷 찍는 걸 깜빡하면 그래프가 그 시점에서 멈춰 보였던 것.
- **어떻게 처리했나**: SNS 실적 수집 명령어를 실행할 때마다 재고 스냅샷도 자동으로 하나
  같이 찍도록 연결. 재고 사이트 접속이 실패해도 SNS 리포트 저장 자체는 그대로 진행되게
  안전장치를 둠.
- **결과**: 실제 실행 로그로 스냅샷이 새로 찍히고 개수가 늘어나는 것 확인.

### 미매칭 항목을 채팅으로 일일이 알려주는 게 번거롭다 — 리포트에서 직접 처리하고 싶다
- **무슨 문제/요청이 있었나**: 안 묶인 게시물이 있을 때마다 매번 "몇 번 몇 번 짝지어줘"라고
  채팅으로 알려주고 리포트를 다시 받는 과정이 번거롭다며, 리포트 화면 안에서 직접 처리할
  수 있게 해달라고 요청. 서버 없이도 이런 반응형 화면을 만들 수 있지 않냐고 확인.
- **확인해보니**: 서버 없이 브라우저 안에서만 동작하는 방식으로 충분히 가능. 완전히
  새로운 게시물(스크래퍼가 아예 놓친 것)을 추가하려면 게시물 주소만으론 안 되고 내용을
  텍스트로 직접 가져와야 하는데, 게시물 상세 화면에서 전체 복사한 텍스트를 확인해보니
  그 안에 게시 시각과 좋아요·리트윗 숫자까지 정해진 순서로 다 들어있다는 걸 확인 —
  자동으로 읽어낼 수 있음.
- **어떻게 처리했나**: 리포트 화면에 이런 기능을 새로 만듦 — (1) 안 묶인 게시물마다
  "표에 추가" 버튼을 눌러 바로 표에 넣기 (2) 아예 새로운 게시물은 상세 화면에서 복사한
  텍스트를 붙여넣으면 본문·시각·좋아요·리트윗을 자동으로 읽어서 추가(자동 인식이 안
  되면 직접 입력하는 대체 방법도 마련). 추가한 내용은 그 리포트 화면 안에서만 보이는
  미리보기이고, 계속 남겨두려면 화면 하단에 나오는 저장 상자 내용을 복사해서 별도
  파일에 붙여넣으면 다음에 다시 만들 때도 계속 반영됨 — 이때 어느 상품과 같은 건지
  사람이 따로 지정할 필요 없이, 늘 쓰던 자동 인식이 다시 알아서 판단함.
- **결과**: 실제로 화면에서 눌러보면서 안 묶인 게시물 추가/새 게시물 붙여넣기 추가/잘못
  입력했을 때 안내 문구/직접 입력/저장 상자까지 전부 정상 동작하는 것 확인.

### 추가한 걸 실수로 잘못 넣었을 때 없앨 방법이 필요하다
- **무슨 문제/요청이 있었나**: 위 기능에 이어서, 표에 잘못 추가한 항목을 없애는 버튼도
  만들어달라고 요청.
- **확인해보니**: 간단히 추가 가능. 처음엔 버튼을 상품 이름 칸에 넣었다가, 실제로 눌러
  보니 칸이 좁아서 클릭이 막히는 걸 발견해서 자리를 옮김.
- **어떻게 처리했나**: 추가한 행마다 "삭제" 버튼을 붙여서(공간이 넉넉한 링크 칸 쪽으로),
  누르면 화면에서도 빠지고 저장 목록에서도 빠지게 함.
- **결과**: 실제로 눌러서 행이 없어지고 저장 상자도 다시 숨겨지는 것까지 확인.

### 예전엔 잘 잡히던 상품이 왜 이번엔 안 잡히지 + 매출 추정 숫자가 이상하다
- **무슨 문제/요청이 있었나**: 경쟁사 매출 추정 숫자가 이상하다는 의문 제기(나중에
  본인 착각이었다고 정정), 그리고 예전 리포트에선 분명 경쟁사와 비교되던 상품이 이번
  리포트에선 안 묶이고 미매칭으로 빠졌다며 원인을 봐달라고 요청.
- **확인해보니**: 매출 추정 숫자는 실제 오류가 아니라, 재고 데이터가 부족할 때 "현재
  재고를 가장 가까운 단위로 올려서 처음 한도였을 것"이라 가정하고 역산한 추정치였음
  (추정치라는 표시가 따로 붙어 있음). 처음엔 미매칭 문제도 화면 인식 문제로 짐작했으나,
  예전에 이 상품이 매칭됐던 리포트를 직접 다시 열어서 그 안에 실제로 어떤 게시물들이
  묶여 있었는지 대조해본 결과 — 전혀 다른 상품(다른 시리즈의 신작) 게시물이 함께 잘못
  묶여 있었던 걸 발견함. 자사가 여러 다른 상품 발표 게시물마다 똑같이 쓰는 문구
  ("초회특전 세트") 때문에, 서로 무관한 두 상품이 그 문구 하나로 잘못 엮였던 것.
- **어떻게 처리했나**: 그 문구를 상품 구분에서 제외하도록 목록에 추가해서 오묶음을 막음.
  겸사겸사 자사·경쟁사가 상품명을 살짝 다른 문자(영문 x와 곱셈 기호)로 써서 매칭이
  아예 안 되던 것도 같이 발견해서 통일되게 고침.
- **결과**: 두 상품 문구로 다시 확인해서 더 이상 안 겹치는 것 확인. 정리하면, "예전에
  실적이 좋아 보이던 숫자"는 사실 무관한 다른 상품 실적이 섞인 잘못된 숫자였고, "지금
  안 잡히는 것"은 데이터가 사라진 게 아니라 그 오묶음이 없어진 것 — 다만 진짜 경쟁사의
  해당 상품 게시물이 이번 수집에 실제로 있는지는 아직 별개로 확인이 필요함.

## 2026-07-15

### 상품이 너무 많이 묶이면 "확인 필요" 표시를 달아두자
- **무슨 문제/요청이 있었나**: 앞으로 여러 날짜 구간을 하나로 합쳐서 비교하려는 계획을
  얘기하다가, 게시물을 많이 섞어서 비교할수록 서로 다른 상품이 우연히 하나로 잘못 묶일
  위험이 커진다는 점을 짚음 — 매칭 방식 자체를 당장 크게 바꾸기 전에, 일단 눈에 띄게
  표시부터 해두자고 함.
- **확인해보니**: 지금 매칭 방식은 그대로 둬도, "한 묶음 안에 게시물이 유난히 많이 몰린
  경우"만 걸러서 표시해주는 안전장치를 얹는 게 간단하면서도 효과적이라고 판단.
- **어떻게 처리했나**: 자동으로 묶인 상품 하나에 게시물이 5건 이상 몰리면 리포트(엑셀·
  화면 리포트 둘 다)에 "확인 필요" 표시를 달아서 사람이 한 번 더 확인해볼 수 있게 함.
  사람이 직접 지정한 매칭은 게시물이 많아도 이 표시를 달지 않음(이미 확인된 것이므로).
- **결과**: 테스트 데이터로 게시물이 많이 몰린 경우/적은 경우를 각각 만들어서 표시가
  정확히 붙는지/안 붙는지 확인함.

### 날짜가 떨어져 있는 여러 기간을 나란히 비교하고 싶다
- **무슨 문제/요청이 있었나**: 며칠에 걸쳐 나눠서 올라간 시리즈 게시물을 경쟁사와
  비교하고 싶은데, 날짜가 쭉 이어진 게 아니라 중간중간 비어있는 경우 어떻게 비교하면
  좋을지 상의함.
- **확인해보니**: 이어진 기간이면 그냥 한 번에 통으로 수집하면 되지만, 사이에 공백이
  있는 기간들을 각각 따로 수집한 다음 나중에 합쳐 보려면, 지금까지는 매번 최근 수집분만
  남기고 이전 것을 덮어써버리는 구조라 예전 기간 데이터가 남아있지 않는 문제가 있었음.
- **어떻게 처리했나**: 수집할 때마다 그 기간의 원본 데이터를 기간별로 따로 남겨두도록
  바꾸고, 그렇게 남겨둔 여러 기간 데이터를 재수집 없이 불러와서 자사·경쟁사 전체 합계
  (게시물 수·좋아요·리트윗 등)를 기간별로 나란히 비교하는 새 명령어를 만듦. 지금은 전체
  합계만 비교하고, 상품별로 기간마다 어떻게 변했는지 보는 기능은 오묶음 위험 때문에
  다음 단계로 미룸.
- **결과**: 여러 기간을 가짜로 만들어서 같은 기간 조합으로 다시 실행하면 갱신되고, 다른
  기간 조합은 따로 남는지 확인함.

## 2026-07-13

### 예전 오류/시행착오 기록 보강
- **무슨 문제/요청이 있었나**: 오류가 생겼을 때 원인·해결 과정만 적혀 있고 "다음에 또
  이런 일이 안 생기게 하려면 뭘 조심해야 하는지"가 빠져 있다는 지적. 또 지금 기록된 것보다
  더 이전에도 실제로 겪은 문제(로그인 세션 관련 포함)가 있었을 텐데 그것도 챙겨서 남겨야
  하지 않냐는 요청.
- **확인해보니**: 기존 기록에는 "해결" 항목만 있고 "재발 방지"용 요약이 없었음. 또 지금
  기록에 없는 훨씬 이전 시점(7/4~7/9)의 실제 문제들 — 예를 들어 상품이 잘못 묶이던 문제,
  스크롤이 끝까지 안 되던 문제, 시간 표시가 잘못 나오던 문제, 재고 조회가 막히던 문제,
  이전에 실수로 파일을 지웠던 일, 로그인 세션이 다시 풀려서 재로그인해야 했던 일 등 —
  이 하나도 안 남아 있었음.
- **어떻게 처리했나**: 기존 기록 각 항목마다 "앞으로 이런 걸 조심하면 된다"는 요약을
  새로 추가함. 그리고 7/4~7/9 사이에 실제로 있었던 문제 11건을 새로 찾아서 같은 형식
  (무슨 문제 → 원인 → 해결 → 재발 방지)으로 정리해 추가함.
- **결과**: 기록 문서가 훨씬 두꺼워지고, 과거에 겪은 문제를 나중에 다시 찾아볼 때
  "그때 왜 그랬고 뭘 조심해야 하는지"까지 한 번에 볼 수 있게 됨.

### 수집 기간을 명령어에서 바로 지정하고 싶다는 요청
- **무슨 문제/요청이 있었나**: 7/10 하루치 실적만 보고 싶은데, 그러려면 파일을 직접 열어서
  날짜를 손으로 고쳐야 해서 번거롭다는 요청 — 명령어 실행할 때 날짜만 바로 넣으면
  그 기간만 수집되게 해달라는 것.
- **확인해보니**: 지금까지는 수집 기간이 파일 안에 고정값으로 박혀 있어서, 다른 기간을
  보려면 매번 그 파일을 열어 값을 수정하고 저장한 다음 실행해야 했음.
- **어떻게 처리했나**: 실행할 때 시작일과 종료일을 순서대로 적어주면 그 기간으로 바로
  돌아가도록 고침 (예: 7월 10일부터 11일까지 보고 싶으면 그 두 날짜를 명령어 뒤에 붙여서
  실행). 안 적으면 원래 설정된 기본 기간 그대로 동작.
- **결과**: 파일을 열어 고칠 필요 없이 명령어 한 줄로 원하는 기간을 바로 지정해서 실행할
  수 있게 됨.

### 엑셀 시트가 실행할 때마다 계속 쌓이던 문제
- **무슨 문제/요청이 있었나**: 테스트 삼아 여러 번 실행하다 보니 엑셀 파일 안에 시트가
  20~30개까지 계속 늘어나 있었음 — 같은 기간을 다시 돌려도 새 시트로 계속 추가되고 있었음.
- **확인해보니**: 실행할 때마다 "시트 이름이 이미 있으면 뒤에 번호를 붙여서 새로 만든다"는
  방식으로 짜여 있어서, 같은 기간으로 다시 실행해도 매번 새 시트가 하나씩 늘어나는 구조였음.
- **어떻게 처리했나**: 같은 수집 기간으로 다시 실행하면 그 기간의 기존 시트 하나만 최신
  내용으로 덮어쓰도록(교체) 바꾸고, 기간이 다르면 예전처럼 새 시트로 따로 남도록 함 —
  같은 기간을 몇 번을 다시 돌려도 시트가 하나만 유지되고, 다른 기간의 과거 기록은 그대로
  보존됨. 더불어 엑셀 파일이 다른 프로그램(엑셀 자체 등)에서 열려 있는 상태에서 저장을
  시도하면 저장이 실패할 수 있는데, 이 경우 잠깐 있다가 자동으로 몇 번 다시 시도하고, 그래도
  안 되면 "파일을 닫고 다시 실행해달라"는 안내 문구가 뜨도록 함(수집한 내용 자체는 사라지지
  않고 남아 있게 처리).
- **결과**: 같은 기간으로 반복 테스트해도 시트가 무한히 늘어나지 않고, 다른 기간 기록은
  그대로 남음. 실제로 같은 기간/다른 기간 두 경우를 각각 반복 실행해서 시트 개수가 의도한
  대로 동작하는지 확인함.

### 인스타그램 글 내용이 이상한 글자로 반복되던 문제 ("신지신지신지…")
- **무슨 문제/요청이 있었나**: 수집한 리포트를 열어보니 매칭 안 된 게시물 목록에서 원래
  글 내용 대신 특정 이름이 계속 반복되는 이상한 글자가 들어가 있었음.
- **확인해보니**: 인스타그램 게시물에서 원문을 뽑아올 때 "화면에 보이는 글자 조각 중 가장
  긴 것"을 무조건 원문이라고 판단하고 있었는데, 그 게시물 밑에 달린 팬 댓글(캐릭터 이름을
  반복해서 부르는 댓글)이 실제 원문보다 길이가 더 길어서 그게 원문으로 잘못 뽑힌 것이었음.
- **어떻게 처리했나**: 화면에 보이는 글자 조각들을 길이가 긴 순서대로 하나씩 확인하면서,
  "이 계정이 쓴 원문처럼 보이는 형태인지" 검사해서 맞는 걸 찾으면 그걸 쓰고, 끝까지 하나도
  안 맞으면 그때만 예전처럼 가장 긴 조각을 씀.
- **결과**: 실제 게시물 주소를 직접 열어서 원문과 비교해봤고, 이상한 반복 글자 없이 정상
  본문이 들어오는 걸 확인함.

### 서로 다른 상품 게시물들이 한 줄로 뭉쳐버리던 문제
- **무슨 문제/요청이 있었나**: 자사와 경쟁사 게시물 10건(각 5건씩, 트위터/인스타그램 둘 다)이
  원래는 서로 다른 상품인데 리포트에서 전부 하나의 상품 줄로 뭉쳐서 나왔음.
- **확인해보니**: 상품을 같은 것끼리 묶을 때 "게시물 문구에서 겹치는 단어가 2개 이상이면
  같은 상품으로 본다"는 방식을 쓰는데, 경쟁사가 신제품을 발표할 때마다 거의 매번 똑같이
  쓰는 정형화된 문구("원형", "최초", "추가", "추후", "색채", "조형" 등)가 있었음. 이 단어들이
  걸러지지 않고 그대로 비교 대상에 들어가다 보니, 서로 다른 캐릭터를 발표하는 게시물끼리도
  이 상용구 하나 때문에 "겹치는 단어 있음"으로 판정되어 줄줄이 하나로 엮여버린 것.
- **어떻게 처리했나**: 상품 구분에 의미 없는 상용구 목록에 이 단어들을 추가해서, 겹치는
  단어를 셀 때 더 이상 포함되지 않도록 함.
- **결과**: 사용자가 올려준 실제 수집 데이터로 직접 돌려서 확인한 결과, 트위터/인스타그램
  양쪽 모두 5개 상품이 각각 따로따로 구분되어 나왔고(예: 페르소나, 블리치, 초 가구야 공주,
  리본, 붕괴 스타레일), 매칭 안 된 게시물도 0건으로 확인됨.

### 인스타그램 좋아요 수가 계속 빈 값으로 나오던 문제 — 시도했다가 되돌린 건
- **무슨 문제/요청이 있었나**: 인스타그램 게시물의 좋아요/댓글 수가 계속 빈 값으로 나오는
  문제가 있었음.
- **확인해보니(1차 시도, 결과적으로 틀림)**: 몇 건은 성공하고 몇 건은 실패하는 걸 보고
  "가끔 뜨는 화면(로그인 유도 창 등)이 잠깐 가리는 것"이라고 짐작해서, 실패하면 화면을
  닫고 다시 시도하는 방식으로 고쳤음.
- **실제로 다시 써보니**: 오히려 이번엔 두 계정 게시물 전부(100%) 좋아요를 못 가져왔고,
  세 번씩 다시 시도하느라 실행 시간만 세 배로 늘어 아무 이득이 없었음. 처음 짐작이 틀렸던
  것으로 확인됨.
- **어떻게 처리했나**: 이 시도를 그대로 되돌려서 원래대로(한 번만 시도) 복구함. 좋아요 수가
  안 보이는 게 화면 인식 문제인지, 아니면 그 계정 자체가 좋아요 수를 숨기는 설정으로 되어
  있는 건지부터 먼저 확인이 필요한 상태.
- **결과**: 실행 시간을 낭비하던 시도는 원상 복구했고, 진짜 원인 확인(로그인한 상태로 직접
  열어봤을 때 좋아요 수가 보이는지)은 아직 사용자 쪽 확인 대기 중.

### 수집할 때마다 로그인 창(시크릿창처럼 인식)이 뜨는 문제
- **무슨 문제/요청이 있었나**: 자동 수집 프로그램이 마치 시크릿창(사생활 보호 모드)에서
  접속하는 것처럼 인식돼서 인스타그램이 자꾸 로그인 창을 띄운다는 문제 — 이전에도 여러 번
  얘기했었는데 아직 안 고쳐졌다는 지적.
- **확인해보니**: 예전에는 "자동화 프로그램임을 알려주는 신호" 중 딱 하나만 숨겨뒀었고,
  그 외에 자동화 프로그램인지 구분하는 데 쓰이는 다른 여러 특징(설치된 확장 기능 목록,
  화면 크기, 그래픽 카드 정보 등)은 그대로 노출돼 있었음.
- **어떻게 처리했나**: 그 외 특징들도 실제 사람이 쓰는 브라우저처럼 보이게 값을 채워 넣는
  작업을 추가로 함.
- **결과**: 아직 검증되지 않음 — 이 조치가 로그인 창 문제를 실제로 없애준다는 보장은 없고,
  기존에 이미 한 번 의심받은 적 있는 예전 로그인 정보를 계속 쓰면 이 조치를 해도 소용없을
  수 있어서, 로그인을 새로 한 번 다시 해서 테스트해봐야 하는 상태.

### 특정 계정 하나만 골라서 성과를 보고 싶다는 요청 (신규 기능)
- **무슨 문제/요청이 있었나**: 지금까지 만든 수집 기능을 바탕으로, 자사/경쟁사 비교가 아니라
  임의의 트위터 계정 하나(예시로 준 계정)를 지정하면 그 계정 게시물만 따로 모아서 성과를
  보고 싶다는 요청.
- **확인해보니**: 기존 기능은 항상 자사 vs 경쟁사 비교 형태로만 결과를 만들도록 되어 있어서,
  계정 하나만 단독으로 보는 용도로는 못 씀. 어떤 용도로 쓰고 싶은지 먼저 확인한 결과 "그
  계정 하나만의 성과를 단독으로 보고 싶다"는 것으로 확인됨(비교 목적 아님).
- **어떻게 처리했나**: 계정 이름 하나를 입력하면 그 계정 게시물만 모아서, 순위·합계·평균을
  보여주는 별도의 리포트를 만드는 기능을 새로 만듦. 로그인은 기존에 쓰던 내 계정 로그인
  상태를 그대로 재사용(새로 로그인할 필요 없음).
- **결과**: 예시로 준 계정으로 실제 실행해서 리포트가 정상적으로 만들어지는 것까지 확인함.

### 지표 없이 글 내용만 뽑고 싶다는 추가 요청
- **무슨 문제/요청이 있었나**: 위 계정 단독 리포트 기능에, 좋아요·리트윗 같은 숫자는 필요
  없고 그냥 "뭐라고 썼는지" 글 내용만 뽑고 싶을 때를 위한 별도 명령어를 추가해달라는 요청.
- **확인해보니**: 기존 방식은 항상 숫자 포함 순위표 형태로만 결과를 만들어서, 글 내용만
  보고 싶은 경우엔 필요 없는 정보까지 다 딸려 나옴.
- **어떻게 처리했나**: 명령어 끝에 별도 표시를 붙이면, 숫자 없이 글 내용 전체를 오래된
  순서대로 나열만 하는 파일이 따로 만들어지도록 함.
- **결과**: 요청한 형태로 텍스트 파일이 만들어지는 것까지 확인.

### 이미 수집한 결과로 엑셀도 같이 만들고 싶다는 요청
- **무슨 문제/요청이 있었나**: 방금 글 내용만 뽑는 방식으로 이미 한 번 수집을 끝냈는데,
  다시 처음부터 수집하지 않고 그 결과로 엑셀 파일도 추가로 만들 수 없냐는 요청.
- **확인해보니**: 그 수집 당시엔 숫자(좋아요/리트윗) 포함 원본 데이터를 어디에도 남겨두지
  않고 글 내용 파일만 저장하는 구조였어서, 그 한 번의 수집 결과는 다시 수집하지 않는 이상
  복구가 불가능한 상태였음 — 이 부분은 솔직하게 그대로 안내함.
- **어떻게 처리했나**: 앞으로는 수집할 때마다 방식과 상관없이 항상 원본 수집 결과(좋아요·
  리트윗 숫자 포함)를 별도로 저장해두도록 바꾸고, 그 저장해둔 원본을 다시 읽어서 엑셀이나
  글 파일을 재수집 없이 다시 만들어주는 별도 명령어도 새로 만듦. 이번 요청 건 자체는 어쩔
  수 없이 한 번 더 수집해서 처리함.
- **결과**: 이제부터는 어떤 방식으로 수집하든 나중에 "이번엔 다른 형태로도 보고 싶다"고
  하면 다시 수집할 필요 없이 저장해둔 원본으로 바로 다시 만들어낼 수 있음.

### 엑셀에서 줄바꿈이 안 보이던 문제
- **무슨 문제/요청이 있었나**: 위 계정 단독 리포트의 엑셀 파일을 열어보니, 글 내용이 여러
  줄로 써 있던 게 텍스트 파일에서는 줄바꿈이 되어 보이는데 엑셀에서는 한 줄로 붙어서 보임 —
  엑셀 칸 안에서도 줄바꿈이 그대로 보이게 해달라는 요청.
- **확인해보니**: 엑셀 칸에 글을 넣을 때 줄바꿈 표시를 공백으로 바꿔서 넣고 있었고, 설령
  줄바꿈을 그대로 둬도 그 칸에 "줄바꿈 반영해서 보여주기" 설정을 켜두지 않으면 엑셀에서
  한 줄로 뭉쳐 보이는 것도 추가 원인이었음.
- **어떻게 처리했나**: 줄바꿈을 공백으로 바꾸지 않고 그대로 넣도록 하고, 그 칸에 "줄바꿈
  반영" 설정을 켬. 글이 여러 줄이면 칸 높이도 줄 수에 맞게 넉넉히 늘려서 잘려 보이지 않게 함.
- **결과**: 확인용 데이터로 다시 만들어 열어본 결과 엑셀 칸 안에서도 줄바꿈이 그대로 보이는
  것까지 확인함.

### 인스타그램 게시물 카드가 안 뜨던 문제 + 펼침 영역이 너무 넓던 문제
- **무슨 문제/요청이 있었나**: 리포트 안에서 트위터 게시물은 실제 카드 형태로 잘 보이는데,
  인스타그램 게시물은 인터넷이 연결돼 있어도 카드로 안 바뀌고 링크만 보임. 그리고 게시물
  목록을 펼쳤을 때 나오는 영역이 화면 폭을 다 차지해서 보기 불편함.
- **확인해보니**: 인스타그램 카드로 바꿔주는 외부 스크립트가 트위터 것보다 늦게 준비되는
  경우가 있는데, 목록을 펼친 시점에 그 스크립트가 아직 준비 안 됐으면 "이미 처리 끝났다"고
  잘못 표시해버려서 나중에 다시 펼쳐도 다시 시도를 안 하고 있었음. 그래서 인터넷 연결
  자체는 되는데도 카드로 안 바뀌는 것처럼 보였던 것.
- **어떻게 처리했나**: 목록을 펼칠 때마다 그 외부 스크립트가 준비됐는지 확인해서, 아직이면
  준비될 때까지 잠깐 기다렸다가 다시 시도하도록 고침. 그리고 펼쳤을 때 나오는 자사/경쟁사
  두 영역의 좌우 비율은 그대로 두고, 두 영역을 합친 전체 폭만 원래의 5분의 3 크기로 줄임.
- **결과**: 코드 상으로는 수정 완료. 실제 인터넷 환경에서 인스타그램 카드가 정상적으로
  뜨는지는 사용자 쪽에서 직접 다시 열어서 확인 필요.

</details>
